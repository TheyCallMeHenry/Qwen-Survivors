# QWEN SURVIVORS — Progress Tracker

## Format contract (v2 compact, 2026-09-05; rework per user directive)

Rules for every future edit — this file is loaded at every session start; its size is the recurring cost.

1. **One home per fact.** Completed work is recorded in **exactly one place**: a ≤2-line checklist line (what landed + gate counts). Implementation narrative → the Session Log entry for that session (and after ~3 sessions, delete the narrative from the checklist line — this file is not an archive). Decisions/pitfalls/rationale → `docs/DECISIONS.md`. Verbatim user messages → `docs/USER-INPUT-LOG.md`. Nothing durable lives only in Resume Notes.
2. **Resume Notes = live state only:** ≤25 lines, rewritten (not appended) every session. History in it is a bug — move it to the owning home first.
3. **Session Log = append-only (D78), newest first,** each entry **≤10 lines**, written once, never rewritten. A session that needs more room writes it in its own commit message, not here.
4. **Status ≤6 bullets:** one-line active phase · gates baseline · git/Pages state · server state. Pointers to the Decisions table, never re-explanations.
5. **Decisions are data (D78-safe):** the Decisions *table* is append-only — decisions 1–99 keep their numbers forever (open phases reference them). But a decision's full text lives in `docs/DECISIONS.md` only; the table row is ≤20 words + pointer. Revisions: new number + `(revises N)`.
6. **Environment caveats → `docs/ENV.md`** — one line, never re-narrated here.
8. **Review-only findings are recorded ONCE** (user directive 2026-09-05, after a token cliff lost session 13's unrecorded 12.7 numbers): the finding goes straight into the checklist step line or a new decision row — never held only in chat. Disposable sims live in gitignored `unsloth-tmp/`.
7. Target: **this file ≤150 lines.** If it grows past that, compress on sight (never by deleting a durable fact without moving it first).

## Status — 2026-09-05

- **Phase 12 COMPLETE** (sessions 12–13c, 2026-09-05): 3 weapons + 5 synergies in-tree, SNAP_V 5 / 34-slot snap; 12.7 pool review → **D80** (fix deferred to Phase 15); 12.8 boot per-synergy E2Es green. **NEXT = Phase 20 (passive-start rule).**
- **Gates (re-run green 2026-09-05 on this tree):** `node tools/check.mjs` **33/33** · `node tools/test-logic.mjs` **656/656** · `node tools/test-boot.mjs` **PASS boot-sim runs=4**. Full baseline: Resume Notes §Gates.
- **Git:** branch `overnight-2026-08-22` = `main` = origin (pushed + **Pages carries Phase 12 COMPLETE** as of this commit; user explicit ask). Pages = the user's test channel; deploys from `main` only on explicit ask.
- **Server:** DOWN (port 47893 not listening as of last check); recipe in `docs/ENV.md`.

## Master Checklist

### Phase 0 — Scaffold & Docs (all 2026-08-20)
- [x] 0.1 Directory tree · 0.2 `package.json` (type:module) · 0.3 `docs/PLAN.md` · 0.4 `docs/PROGRESS.md` · 0.5 `README.md` · 0.6 `AGENTS.md`

### Phase 1 — Core engine (all 2026-08-20)
- [x] 1.1 `index.html` (canvas, HUD, touch UI, 7 screens) · 1.2 `css/main.css` (safe areas, touch sizing) · 1.3 `js/config.js` (all tuning) · 1.4 `utils/math.js` + `utils/bus.js` · 1.5 `core/loop.js` (fixed 60 Hz, timescale, hit-stop) · 1.6 `core/input.js` · 1.7 `check.mjs` green

### Phase 2 — Procedural art & world (all 2026-08-20)
- [x] 2.1 `art/base.js` · 2.2 `art/sky.js` · 2.3 `art/terrain.js` · 2.4 `art/characters.js` (pre-rendered frames) · 2.5 `art/items.js` (world sprites + card icons) · 2.6 `world/generate.js` (pure seeded layout) · 2.7 `world/world.js` · 2.8 `world/minimap.js`

### Phase 3 — Entities & gameplay (all 2026-08-20)
- [x] 3.1 `entities/player.js` · 3.2 `entities/enemies.js` (7 types + wraith boss, burn/blight) · 3.3 `entities/spawner.js` (pure curves) · 3.4 `entities/combat.js` · 3.5 `entities/pickups.js` · 3.6 `entities/particles.js` · 3.7 `systems/camera.js` · 3.8 `systems/lighting.js` (half-res darkness + glow) · 3.9 `core/game.js` (6-state machine) · 3.10 `test-logic.mjs` green

### Phase 4 — UI/HUD (all 2026-08-20)
- [x] 4.1 `ui/hud.js` (text-on-change) · 4.2 `ui/screens.js` (6 screens + Upgrades) · 4.3 CSS touch ≥72 px + safe areas · 4.4 `index.html` wiring verified

### Phase 5 — Audio (all 2026-08-20)
- [x] 5.1 `audio/sfx.js` (lazy ctx on gesture, bus graph) · 5.2 `audio/music.js` (track replaced by 10.6) · 5.3 Gesture unlock + mute persistence verified

### Phase 6 — Integration & launch (all 2026-08-20)
- [x] 6.1 `js/main.js` (Node-safe top level) · 6.2 `tools/serve.mjs` (47893, 0.0.0.0, traversal-safe) · 6.3 check 28/28 · 6.4 logic 95 green · 6.5 HTTP 200/MIME/404 · 6.6 Mobile audit · 6.7 Handoff + PLAN sync

### Phase 7 — Pre-sign-off hardening (all 2026-08-20)
- [x] 7.1–7.6 boot-sim: card pick via keys · all weapons + wand-off window · heart heal · score persistence + quit · touch stick + dash · all gates green

### Phase 8 — Publish & share (all 2026-08-20)
- [x] 8.1 Public repo, initial commit `03eeac2` · 8.2 GitHub Pages live (HTTP 200)

### Phase 9 — 7-request feature pass (all 2026-08-21)
- [x] 9.1 Vision ×3 (`lighting.playerR` 510) · 9.2 knockback→33% · 9.3a 5 synergies + `cardOffers` · 9.3b `core/meta.js` + Soulshards + Upgrades · 9.4 minimap +25% · 9.5 full dash i-frames · 9.6 Twin pistols / Sunder bombs / Pyre Lance · 9.7 gates 29/29 · 117/117

### Phase 10 — Post-playtest polish (10.1–10.8 done; 10.9 absorbed into Phase 25)
- [x] 10.1 Dash indicator all envs (`#btn-dash-hud`, `--cd`) · 10.2 exact-effect card text (`cardEffectText`) · 10.3 mute speaker icon (SVG) · 10.4 perf: viewport culling + in-place compaction; `[10.4-bench]` permanent · 10.5 spawn band just outside view edge · 10.6 music remake (66 BPM eldritch D-dim7, seam-proven in Node) · 10.7 empty card pool → auto-skip LEVELUP (softlock fix) · 10.8 gem pickup SFX
- [ ] 10.9 Game-wide optimization pass — **absorbed into Phase 25** (D-approved 2026-09-04)

### Phase 11 — Multiplayer & co-op (11.1–11.12 DONE 2026-08-22/23; 11.13 impl = NAS-side, D64)
- [x] 11.1 Co-op transport: zero-dep WS upgrade in `serve.mjs` (port 47893 only); pure protocol/room/D53-profile/`coopScale` in `js/net/coop.js`; E2E = real server + native WS clients
- [x] 11.2 Host-authoritative sync (`js/net/sync.js` SNAP_V, player/enemy/pickup snaps): host sim owns all players, per-step broadcast, shared seed, client interp; meta NEVER in sync (D53)
- [x] 11.3 Difficulty scaling ×(1+33%×added players) on HP/dmg/spawn curves incl. boss (Q7); solo `s=1` bit-identical
- [x] 11.4 Player leash `CFG.coop.leashR` 700; pure `leashClamp` POCS; host `_coopLeash()` per step; co-op light radius = leashR
- [x] 11.5 Level-up scoping + weapon exclusivity: first-pick ownership (`weaponOwner`), per-picker `cardOffers(cap, exclude)`, cap = 5−(N−1) (D42); passives never locked
- [x] 11.6 Characters (RESPEC): `CFG.characters` (stats user-approved; Mage starter/Warden/Ranger/Swash + ghost fallback D59), per-char art builders, full-screen select + Soulshard shop (D58), greyed-taken rule (D56)
- [x] 11.6b Exclusivity closure: synergy first-pick-wins + starting-weapon pre-ownership seeded in `_applyChar`/`_coopStartRun`; solo map never populated → invariance
- [x] 11.7 Co-op HUD corner panels (seat-0 always TL = solo invariance; seats 1/2/3 TR/BL/BR; join order; display-only)
- [x] 11.8 Per-char UI theming (`CFG.characters[*].accent` → `--char*` CSS vars on hud/touch/seat roots; fixed seat HP-fill latent bug)
- [x] 11.9 Co-op minimap ~66% + PAUSE beside it (CSS-only, all under `#hud.coop`; mute NOT moved, D72)
- [x] 11.10 Boss count = N players → N level-bosses at `B.at` (`bossCount(n)`; ×N banner; every boss lit; enemySnap pipeline)
- [x] 11.11 Solo invariance guard (test-boot one-shot: net-free, 1 player, coopS 1, cap 5, 1 Wraith, local-only ownership)
- [x] 11.12 Final gate: 3P pumped to VICTORY t=300 + pause-on-blur host-only + co-op mobile parity CSS (72 px, corner layout, safe areas)
- [ ] 11.13 Web play **implementation** — transport DECIDED = Tailscale on DS124 NAS running unmodified `serve.mjs` (D64; research + sources: `docs/RESEARCH_FINDINGS.md`). NAS-side steps need the user's NAS access
- Deferred (NOT approved — do not build without asking, D51): first-clear shard bonus · per-level shard multipliers · per-level victory lines · pufferfish skin · keyboard 1/2/3 select · boss intro moment

### Phase 12 — Flame tune + 3 weapons & 5 synergies (roster 7→10 weapons, 5→10 synergies)
- [x] 12.1 Pyre Lance verification only (acceptance fully landed in 16.3: reach 1.33×, front speed exactly 2×, `flameMomentum` 0.5 lead, mid-torso origin) — DONE 2026-08-23
- [x] 12.2 Bow & Arrow (#8): straight-flight single-target arrow (`fireArrow`), 5-level table, icon+sprite; SNAP_V 1→2/27 slots — 2026-08-23
- [x] 12.3 Snowball Launcher (#9): lob→burst AoE, accumulating slow stacks → freeze (status pipeline in `enemies.js`); fixed `applyPlayerSnap` hard-coded offsets → derived W/P/S offsets; SNAP_V 2→3 — 2026-09-04
- [x] 12.4 Ring of Chain Lightning (#10): shock stacks → stun + greedy-nearest chain (`fireShockBolt`/`applyShock`/`_chainArc`); SNAP_V 3→4 — 2026-09-04
- [x] 12.5 Status pipeline COMPLETE: slow/freeze (in 12.3) + shock/stun/chain (in 12.4) + blue-flame freeze+DoT (in 12.6); 5 s per-stack TTL (D43) — CLOSED 2026-09-05
- [x] 12.6 Five synergies (5 levels each, pair-specific offer at own-2-sources-max): flamingArrows · heartPiercer (pierce budget = synergy level) · blueFlame (L5 freeze = `freezeDur` 0.8 deliberate) · stormVolley (strike every 4th VOLLEY, both twin rounds; chains via `_chainArc` + shock stacks) · heartMagnet "Heart Compass" (hearts ride gem magnet path); SNAP_V 4→5/34 slots — DONE 2026-09-05
- [x] 12.7 Roster sync COMPLETE: README 10/10 + card-pool review (400-run Monte-Carlo on the real offer/pick pipeline @ L25 budget): weapons all reachable (first-pick 39–48%); pair-gated synergies never reached 91–100% at cap-5 (passive-required ones 0%) → **D80** numbers below; no code change (Phase 15 owns the fix) — 2026-09-05
  - D80 numbers (`unsloth-tmp/pool-sim.mjs`): cum XP→L25 = 924. Eligibility-never: blight 91% · tempest/napalm/blueFlame/stormVolley 95–96% · inferno/flamingArrows 96% · phoenix/heartPiercer/heartMagnet 100%. Fix options: (a) pair gate L3–4, (b) raise `CFG.run.maxWeapons`, (c) curated early offers, (d) accept as aspirational
- [x] 12.8 Boot per-synergy E2Es on isolated `g2`: flamingArrows (burn-on-hit vs no-burn control) · heartPiercer (bonus dmg + pierces exactly 1 extra) · blueFlame L5 (freeze+burn+slow-stack on burst, control freezes/burns nothing) · stormVolley (no strike before 4th volley · BOTH twin rounds carry it on the 4th · impact = bullet dmg + SV.dmg + chain 1 shock stack no re-proc) — DONE 2026-09-05

### Phase 13 — Multi-level expansion (COMPLETE 2026-08-21/22, 13.1–13.13; spec PLAN §3.9)
- [x] 13.1 Level framework: `js/world/levels.js` pure defs + `generateWorld(seed, levelKey)` (m01 bit-identical goldens); per-level weights/boss/diff into spawner (D55)
- [x] 13.2 Map 02 "Higan" art (sakura petals map-only, torii/shrine/pagoda/koi/Fuji, dusk palette) · 13.3 M02 roster (Oni + Ryū boss, ×1.25 stats, name-driven banners)
- [x] 13.4 Map 03 "The Drowned City" art (5145×3920 = 1.5× area A3; kelp/coral/Atlantis/god-rays/rising bubbles/fish schools) · 13.5 M03 roster (crab/goldfish/mer-people/stingray/eel/orca + Great White, ×1.56)
- [x] 13.6 Unlock persistence: cumulative wins victory-only, 3× chained (LS `qsurv.wins.v1`) · 13.7 level select (3 cards, locked visible + denied blip/shake, persist) · 13.8 view zoom 0.80/1.0 + Pause-menu Settings (mute row replaces HUD button, D48)
- [x] 13.9 per-level high scores (`scoreKeyFor`; m01 key unchanged) · 13.10 flavor: gem/heart tints + unlock banner + progress lines · 13.11 per-level audio (5 flavor voices + muffle lowpass; m01 invariance proven) · 13.12 gates · 13.13 README Levels section

### Phase 14 — In-run Soulshards counter + unified co-op earnings (queued after 21, before 25/10.9)
- [ ] 14.1 In-run counter: live projected award `floor(score/400)` (+25 victory) — no economy change (O1=A); state-gated visible-during-runs-only; change-detected DOM
- [ ] 14.2 Unified co-op earnings: one run-level total from host sim, every client accrues in full locally (D54 supersedes D53 earnings clause; meta stays player-specific)
- [ ] 14.3 Gates: test-logic pure helpers + boot solo/co-op E2E

### Phase 15 — Level-up offer coverage DEFECT (every roster weapon reachable; user playtest 2026-08-22)
Root cause NOT determined — **15.1 reproduce/quantify FIRST; do not patch from hypotheses.** Baseline on the post-Phase-20 economy. User data: Pyre Lance never offered in ≥20 runs; pistols often, bombs occasionally. (Snowball/ring "never seen" = pre-Phase-12, expected.)
- [ ] 15.1 Reproduce + quantify: per-weapon offer frequency across N seeded runs
- [ ] 15.2 Root cause (candidates UNVERIFIED): 3-of-N pool sample odds · cap-5 slot-fill blocking late new-weapon offers · owner-exclusion asymmetry
- [ ] 15.3 Fix: offer-pool design guaranteeing reachability without breaking exclusivity/cap (decision documented before implementing)
- [ ] 15.4 Regression: seeded determinism test + before/after frequency numbers in the tick note

### Phase 16 — Playtest defects (COMPLETE 2026-08-22, 16.1–16.4)
- [x] 16.1 m01 snow regression: repro quantified first (probeSnow: 90 fills on EMPTY paths), culprit 13.4 `a8c332b`, arc restored as v1.0.0; non-regression probe at 6 sites
- [x] 16.2 Spawn origin feet→mid-torso (`spawnOriginFrac` 0.55 + `Player._spawnY()`, all fire sites; garlic stays feet = ground pulse); latent fix: blades/garlic were NEVER drawn (`combat.draw` per-player loop)
- [x] 16.3 Pyre Lance flame: reach exactly 1.33× (speed 2× + life trim), `flameMomentum` 0.5 LEAD; seeded A/B bit-exact drag-integral verification
- [x] 16.4 Gates green with all Phase 16 asserts in-tree; co-op invariance verified (no projectile key in sync protocol)

### Phase 17 — Selectable run durations & boss schedule (spec PLAN §3.10, D65; queued after 15)
Durations 5(default)/10/15/20/ENDLESS; bosses at 4:00/9:00/14:00/19:00 + every 5:00 endless.
- [ ] 17.1 O resolutions (user confirm: 20-min 19:00, ENDLESS slots-vs-cadence, co-op N-bosses/event, ENDLESS victory/high-score) + `CFG.run.durations` + spawner tail past 5:00
- [ ] 17.2 Menu duration select (≥72 px, per-level persistence, default = current behavior)
- [ ] 17.3 Run machinery: victory at duration; boss events per schedule; ENDLESS count-up HUD
- [ ] 17.4 Gates: table asserts (5→[240], 10→[240,540], …) + boot E2E (10-min two bosses; ENDLESS past 5:00 no victory; 5:00 default bit-identical)

### Phase 18 — Level-up actions SKIP / BANISH / RE-ROLL (spec PLAN §3.11, D67; research `RESEARCH_FINDINGS.md` §7; queued after 17)
Screen actions (NOT items/cards/level-able); unlocked + upgraded in the meta store, max 5 uses/run each.
- [ ] 18.1 O resolutions: skip-XP ratio · banish granularity/owned-freeze · reroll exclusion (VS semantics) · Soulshard cost curve
- [ ] 18.2 Meta store: 3 entries × (unlock + levels 1→4 = uses 1→5), persisted, no data loss
- [ ] 18.3 In-run per-player counters (host-auth; co-op snapshot fields) + level-up screen buttons with live counts, hidden until unlocked
- [ ] 18.4 Mechanics: SKIP · RE-ROLL (discarded set excluded from redraw only) · BANISH (picker-scoped run-long offer exclusion, mirrors `exclude` plumbing); interplay with 10.7 empty-pool + 15.3 pool fix
- [ ] 18.5 Gates incl. co-op per-picker isolation E2E + solo invariance (unlocked-0 = today's screen)

### Phase 19 — In-run HUD: equipment icons + fuel bar (spec PLAN §3.12, D68; queued after 18)
- [ ] 19.1 Equipment row under XP bar: weapon icons + per-weapon level number live all run (items land in 21; passives inclusion = O)
- [ ] 19.2 Pyre Lance fuel bar ABOVE → **beneath the player** (world-space; D24 revised); fuel logic unchanged
- [ ] 19.3 Co-op placement vs seat panels/minimap — no overlap, mobile parity
- [ ] 19.4 Gates: boot E2E (icons at run start = starting weapon only per Phase 20; fuel bar under flame owner) + solo invariance

### Phase 20 — Passive-start rule: runs start with NO passives (D66; queued after 12, before 15)
- [ ] 20.1 `Player.reset()`: no passive grants (weapons unchanged D34) + recomputeStats baseline + test rebaselines
- [ ] 20.2 Co-op: passives never locked (D32); ghost flow + exclusivity unaffected — verify
- [ ] 20.3 Gates: reset = `passives {}` at start; first passive via level-up E2E

### Phase 21 — Extended roster: 9 weapons + 2 run items + 5 characters (PLAN §3.13 VERBATIM, D69; queued after 14)
Tank Cannon · laser beam · Wolf summon · Rolling Boulder · Web-slingers · Gatling · Baseball Bat · Frog Tongue · Cannonball; Mirror Shield · Ice Skates; Werewolf · Stone Golem · Baseball Player · Giant Toad · Wild West Gunslinger. All via D60 data-driven modularity + Decision 21 status pattern.
- [ ] 21.1 O resolutions (user confirm): per-weapon stats · item slot rules · character archetypes/stats/unlock costs · Cannonball×leash · boulder auto-kill size class
- [ ] 21.2 Weapons 1–9 (mechanics briefs in PLAN §3.13) + snapshot keys + boot exercise each
- [ ] 21.3 Items: Mirror Shield / Ice Skates — item-slot class def first (O 21.1)
- [ ] 21.4 Characters ×5 (D62 pattern; each requires its weapon from 21.2 first)
- [ ] 21.5 Roster sync: pool/icons audit, cap review, README 10→19 weapons / 2 items / 4→9 chars + ghost
- [ ] 21.6 Gates: all new content exercised; roster-count asserts rebased

### Phase 22 — Playtest defects r2 (user report 2026-08-28; user's test channel = published Pages build)
- [x] 22.1 Weapon visibility+damage audit (all 8): draw + damage coverage verified in-tree; live-run E2E observes all 8 weapons — 2026-09-04 (user's garlic/blades report = Pages staleness, fixed by unpublished 16.2)
- [x] 22.2 Gem escape from non-traversable spots: `escapeFromSpots` radial ellipse projection + per-step nudge (`CFG.gems.escapePad`); pushed `e502417` — 2026-09-04
- [x] 22.3 M03 gem vs heart contrast: HEART_PAL.m03 → coral; pushed `e502417` — 2026-09-04
- [x] 22.4 Gates green with all Phase 22 asserts — 2026-09-04
- [x] 22.5 Flame damage radius aligned to visual stream: acquisition **flat 150** all levels (was 300–380 → invisible damage gap); visible-reach replay asserts — 2026-09-04
- [x] 22.6 Death slow-mo beat REMOVED per user verdict (`deathDelay`/`deathTimescale` deleted; `_onDeath` → immediate `_gameOver(false)`; never reintroduce a DYING transient) — 2026-09-04
- [x] 22.7 Synergy surfacing: gate (own-2-sources-max) was CORRECT — draw-surampling side effect; fix = synergies weighted ×8 in the offer draw (`SYNERGY_DRAW_WEIGHT`), dedup by `kind:key`; never add an all-weapons-maxed requirement — 2026-09-04
- [ ] 22.8 Mobile "View zoom" non-functional (user report, Pages build): feature verified present+correct in-tree and in published main; real defect found = lighting half-res canvas stretches under the zoom transform. **Needs device repro** (no phone in this environment)

### Phase 23 — Projectile feel & Sunder Bombs rework (PLAN §3.14; COMPLETE 2026-09-04; tuning rule: change only what the user names)
- [x] 23.1 Bow charge-up (`CFG.weapons.bow.charge` + draw telegraph) + cadence ordering pistols < wand < bow at every level — 2026-09-04
- [x] 23.2 Sunder Bombs: radius ×1.5, damage ×2, strong centre-radial knockback, blast visual driven from true damage radius — 2026-09-04
- [x] 23.3 Over-heal synergy REPLACING Phoenix Heart in place (full-HP hearts → over-health to 200% current max, 1%/s decay, re-heart re-applies; `overHeal` field + HUD bonus segment; old phoenix machinery deleted) — 2026-09-04
- [x] 23.4 Gates green (incl. boot E2Es for all three) — 2026-09-04

### Phase 25 — Comprehensive performance & optimization pass (**DEDICATED FULL SESSION**, absorbs 10.9; queued before 2.9)
Framerate drops precipitously over long runs, esp. mobile. Most load already capped (aliveCap min(200, 60+t/12); gem/heart ring buffers 320/32; particleCap 512) — the leak story does not obviously hold.
- [ ] 25.1 **Profile first, no code changes** (D70): `[10.4-bench]` sampling a LONG run early/mid/late/post-boss — update-ms, render-ms, entity/projectile/particle counts, draw-op counts → written baseline table in this file
- [ ] 25.2 Measure named suspects explicitly: Pyre Lance 2–3 sprites/frame while any target in range (never idles); `escapeFromSpots` per-gem nudge (≤8 iters × spots × ≤320 gems/step); `enemies.grid` rebuild + separation at cap; `Lighting.resize` allocating a new canvas on view change
- [ ] 25.3 Mobile-specific: DPR caps, foreground particle counts, `aliveCap` scale-down seam for coarse pointer, per-frame allocations → GC pauses
- [ ] 25.4 Fix in descending measured cost; re-baseline after each; thresholds stay config scalars (rule 5)
- [ ] 25.5 Absorb 10.9 into this phase + queue line updated — DONE at scoping (2026-09-04)
- [ ] 25.6 Acceptance: bench table flat across a full-length run on mobile settings · all gates green · README/PROGRESS sync

### Phase 24 — Visual upgrade / improvement pass (**user 2026-09-05: DEDICATED FULL SESSION, visual rework ONLY, nothing else**; spec pending)
- [ ] 24.1 Scope + spec first: audit current art (player/enemy sprites, VFX, lighting, HUD chrome, per-level identity) → PLAN §3.15 + numbered steps; absorb 22.1 weapon-visibility findings + flame-range visual learnings
- [ ] 24.x Synergy visual identity (user item 6): every synergized weapon gets a distinct appearance. Verified today: blight/inferno/napalm = pure stat+DoT, NO projectile look change (only `tempest` spawns bolts). Build ONE shared projectile-variant mechanism (procedural art per rule 2; variant tag on projectile records that draw selects) — once, not bespoke per synergy. Spec PLAN §3.14(6)

## Resume Notes — session 13c, 2026-09-05 (live state only; rewritten each session per Format contract)

**Where we are:** **Phase 12 COMPLETE** (12.1–12.8 all ticked). Committed + pushed to `overnight-2026-08-22`, ff-merged to `main` + pushed (Pages refreshed) on explicit user ask.

**Gates baseline:** check.mjs **33/33** · test-logic **656/656** · boot `PASS boot-sim runs=4` (death + victory + m02 + m03; all weapon/status/synergy E2Es, co-op 11.x suite incl. ghost/char-sync/HUD/invariance/victory-at-t300, 16.x probes, 23.x E2Es). Run via `"/c/Program Files/nodejs/node.exe"` (node not on PATH — `docs/ENV.md`).

**NEXT = Phase 20 — Passive-start rule (D66):** runs start with NO passives. Then Phase 15 (D80 fix decision lives there) → 17 → 18 → 19 → 14 → 21 → 24/25 (dedicated sessions) → 2.9 browser sign-off.

**12.8 landed in** `tools/test-boot.mjs` (~L1448–1620, isolated `g2`): 4 synergy probes + controls + `syn128Done`. Debug fixes that mattered: probe must set the SAME synergy level it asserts (`blueFlame: 5` for the L5 row) · strike-impact probe aims at foe center (mid-torso origin ≠ feet y; ang=0 flies above hit circles) and flies the bullet in a while-loop (one tick = 12 px, target 140 px away) · struck foe takes bullet dmg + SV.dmg (both resolve).

**D80 = the numbers live on the 12.7 checklist line** (one home); decision = defer fix to Phase 15.


**Queue after Phase 12 (README order):** Phase 20 → 15 → 17 → 18 → 19 → 14 → 21 → 24/25 (dedicated sessions) → 2.9 browser sign-off → DONE. 22.8 needs device repro; 11.13 impl needs user's NAS access.

**Note:** `docs/DECISIONS.md`/`docs/ENV.md` were referenced by the Format contract but never created (session 13 lost before writing them). Until they exist, decision full texts live on their checklist line / table row; pitfalls stay here.

**Pitfalls still hot:** D74 fmtS · D75 fixture `=== 0` · D77 spy weapon-pinning · D79 storm-volley per-volley cadence · 12.4 E2E traps (unown + clear projectiles at probe-block entry; stunT `< 1e-9`; fresh emission before draw asserts) · regrid every tick in isolated probes · `Math.random=()=>0` yields spread MINIMUM (pin 0.5) · 12.8: pooled enemy status fields zeroed on spawn; `_nearest` steals aim → probe player parked at x=500; bow charge-up needs several fire passes (`_bowCharge` pre-seeded); `_bulletShots` reset before storm probe.

## Decisions (binding — table: pointer to `docs/DECISIONS.md`)

| # | Decision (≤20 words) |
| --- | --- |
| 1 | Vanilla ESM, zero deps, zero build step |
| 2 | Bounded world 4200×3200 + minimap |
| 3 | All audio = Web Audio synthesis, no asset files |
| 4 | Port 47893 only, bind 0.0.0.0 |
| 5 | Touch steer = pointer-drag floating stick; `K` dash alias |
| 6 | Lighting = radial gradients over cached half-res canvas, stateless flicker |
| 7 | Screens = per-frame `game.state` poll; bus for transients only |
| 8 | Mute owned by `hud.js` (LS `qsurv.mute`); audio re-reads never writes |
| 9 | `#touch-ui` behind `body.touch`; desktop dash = Shift/Space/K/right-click |
| 10 | Exactly 2 audio modules; `sfx.js` owns lazy ctx + shared graph |
| 11 | `startRun()` sets `input.gesture` (menu tap unlocks mobile audio) |
| 12 | `serve.mjs` listens unconditionally (no import.meta gate) |
| 13 | Touch detect = coarse ‖ ontouchstart → `body.touch`; DPR caps 2/1.5 |
| 14 | Game canvas `alpha:false`; minimap raw 1× (no DPR) |
| 15 | Third gate `tools/test-boot.mjs`: stubbed-DOM full-run sim in Node |
| 16 | test-boot one-shot paths self-verify (final assert on every flag) |
| 17 | ctx stub validates arc/ellipse args + radii, negative-tested |
| 18 | Spawner batch floor 1→2 (idle player must take damage) |
| 19 | Synergies = third card kind, own namespace, not in `maxWeapons` |
| 20 | Meta = pure `core/meta.js`, LS `qsurv.meta.v1`; shards `floor(score/400)`+25 |
| 21 | DoT via `dpsTick` — never `damageEnemy` (no white flicker) |
| 22 | New-weapon SFX via bus events from `combat.pulse`, tight GAPS |
| 23 | Bomb = parabolic lob → fuse pause → AOE |
| 24 | Flamethrower fuel model: fuel s, lengthy recharge, bar, per-enemy hit CD |
| 25 | Optimization pass LAST (now absorbed by Phase 25) |
| 26 | Empty card pool → auto-skip LEVELUP (level still granted) |
| 27 | Co-op = zero-dep WS in `serve.mjs`; host-authoritative; Pages can't host WS |
| 28 | 4 characters + hidden ghost: unique palette/silhouette/stats/starting weapon |
| 29 | Weapon+synergy exclusivity per run, first-pick-wins; passives unlocked |
| 30 | Difficulty +33% per added player on HP/dmg/spawn (max 4 total) |
| 31 | Leash = shared expanded co-op vision radius, every player sees all |
| 32 | Level-up picks player-scoped; passives never locked; synergy follows own max |
| 33 | Corner→player assignment = join order (TL/TR/BL/BR) |
| 34 | Starting weapons Wand/Garlic/Blades/Lance pre-owned; ghost = 2 unique offers |
| 35 | N players = N bosses, same +33%/player ramp |
| 36 | Web-play research: verify real date FIRST standalone; sources cited; 100% free |
| 37 | Roster 10 weapons + synergies; all Phase 11 rules apply unchanged |
| 38 | Flaming Arrows/Heart-Piercer/Blue Flame/Storm Volley (details DECISIONS.md) |
| 39 | New statuses tick via Decision 21 pattern |
| 40 | Synergy cards = 5 levels, scale with own level (supersedes D19 single-level) |
| 41 | Synergy gating pair-specific: own `requires` sources max — never all weapons |
| 42 | Co-op weapon cap 5−(N−1); base `maxWeapons` 5; synergies never count |
| 43 | Slow/shock stacks 5 s per-stack TTL (config scalar) |
| 44 | Slot-based enemy roster: roles keep mechanics, levels re-skin/re-stat |
| 45 | Map sizes: M02 = M01; M03 1.5× area ≈ 5145×3920 |
| 46 | Unlocks cumulative wins, victory-only, 3× each chained |
| 47 | Level select on main menu, locked visible + progress, ≥72 px |
| 48 | Zoom camera-view factor default 0.80 touch; HUD/minimap 1×; Pause Settings |
| 49 | M03 foreground = rising bubbles |
| 50 | Per-level difficulty chained ×1.25 per step |
| 51 | A5 flavor approved; deferred list in Phase 13 checklist |
| 52 | Phase order A7: 13 → 11 → 12 → last gates → 2.9 |
| 53 | Meta player-specific, never in sync; join handshake uploads stat profile |
| 54 | Unified co-op shard earnings; counter = projected award (supersedes D53 clause) |
| 55 | Level def = single source of truth; `World.generate` one seam into CFG.world |
| 56 | No sharing in co-op: unique chars, own cards, greyed-taken select |
| 57 | Per-char stats visible in select; solo uses per-char stats too |
| 58 | Char shop: Mage default · Ranger 1500 · Warden 3500 · Swash 7500 |
| 59 | Ghost fallback when all-starter lobby: no weapon + 2 unique offers each |
| 60 | Roster modularity: new chars/weapons/synergies = config + icon builder only |
| 61 | Select-UI research brief, default full-screen closing on confirm |
| 62 | Character roster resolved (Mage starter; Warden big+slowest; ghost tints) |
| 63 | Capture-before-reason user-input log v4 (denylist exact-text + precedence) — AGENTS.md #9 |
| 64 | 11.13 transport = Tailscale on DS124 NAS, unmodified `serve.mjs`, zero code change |
| 65 | Run durations 5/10/15/20/ENDLESS + boss schedule 4:00/9:00/14:00/19:00/+5:00 |
| 66 | Players start runs with NO passives |
| 67 | SKIP/BANISH/RE-ROLL = meta-store-unlocked screen actions, max 5 uses/run each |
| 68 | Equipment icons under XP bar w/ level numbers; fuel bar beneath player |
| 69 | Extended roster backlog verbatim PLAN §3.13 |
| 70 | Phase 16 defects (snow · mid-torso origin · flame +33%/2×/LEAD) |
| 71 | Queue revision 2026-08-22 (superseded by README/Status queue line) |
| 72 | 11.9 rescope: minimap 66% + PAUSE only; mute NOT moved |
| 73 | Phase 22 defects r2 + Pages-staleness test-channel lesson |
| 74 | Card-text `fmtS`: `toFixed(2)` + strip ONE trailing zero → formatter is spec |
| 75 | Status fixtures never init optional fields → assert `!e.burnT`, never `=== 0` |
| 76 | Blue Flame L5 freeze = `combat.freezeDur` 0.8 by design (card must deliver) |
| 77 | Test spies on `_weapons` must pin `p.weapons` exactly (start-weapon trap) |
| 78 | Session Log append-only; this file's Format contract v2 (2026-09-05) |
| 79 | Storm Volley cadence per VOLLEY; 4th volley both twin rounds carry strike |
| 80 | Pair-gated synergies ~unreachable at cap-5 (numbers on 12.7 line); fix deferred to Phase 15 |

## Session Log (append-only, newest first)

- **2026-09-05 (session 13c) — 12.8 COMPLETE → Phase 12 CLOSED:** resumed post-cliff, docs current first. Fixed 3 test-only defects in the 12.8 block: blueFlame probe set level 1 but asserted the L5 row (→ `blueFlame: 5`) · stormVolley strike probe fired ang=0 from mid-torso y over foes at feet y + single update tick couldn't cross 140 px (→ aim at center + fly-loop until impact) · struck-foe damage = bullet dmg + SV.dmg (both resolve on impact). Dead `strikes +=` reference removed. Gates: check 33/33 · logic 656/656 · boot PASS runs=4 ×2 consecutive. Zero product-code changes; no commits (rule 7). NEXT = Phase 20.

- **2026-09-05 (session 13b) — 12.7 card-pool review COMPLETE + cliff recovery:** resumed post-token-cliff with docs stale; gates re-verified first (33/33 · 656/656 · boot runs=4). Monte-Carlo (400 runs, real `cardOffers`+`applyCard`, gitignored `unsloth-tmp/pool-sim.mjs`): weapons all reachable (39–48% first-pick); pair-gated synergies eligible only ≥L21 (never 91–100%), passive-required ones 0%. Tick 12.7 + D80 options recorded (no code change — Phase 15 owns the fix decision). Format contract rule 8 added: review findings → docs same turn. NEXT = 12.8 boot E2Es for the 4 combat synergies.
- **2026-09-05 (session 13) — handoff rework + gates verify:** all three gates re-run green on the uncommitted 12.6 tree (33/33 · 656/656 · boot runs=4). Workspace hygiene: prior-session scratch (`NOTES.md`, `STATUS.md`, `unsloth-tmp/`) gitignored, not deleted (recon content preserved locally). **PROGRESS.md restructured to Format contract v2** per user directive (evidence-based context efficiency): narrative duplicated across Status/checklist/Resume/Log collapsed one-home-per-fact; decision full texts → new `docs/DECISIONS.md`; environment caveats → new `docs/ENV.md`; completed-phase checklist lines compressed to ≤2; file 566 → ~300 lines. No product-code changes; gates unchanged.
- **2026-09-05 (session 12) — Phase 12.6 Five New Synergies COMPLETE + 12.5 CLOSED:** tree arrived half-finished with test-logic CRASHING; five test-side fixes, zero product-code changes: storm spy must pin `p.weapons` (D77) · `_S_OFF` derived locally · fmtS "0.6" is spec (D74) · fixture `!burnT` (D75) · cadence per-VOLLEY both-rounds-on-4th (D79). Verified 12.6 scope: 5 synergy tables + effects, 5 icons, card lines, SNAP_V 4→5/34 slots, heart-magnet route. Gates 33/33 · 656/656 · boot runs=4. Docs synced; NEXT was 12.7-pool-review + 12.8. Deleted ~200 scratch .mjs files (rule 8).
- **2026-09-04 (session 11) — 12.4 Ring of Chain Lightning COMPLETE:** code already in-tree from session 10; boot E2E was RED (`chain burst damages foe exactly once`). Root cause: an in-flight Sunder Bomb from the earlier 16.2 probe exploded on target T2 mid-probe (ownership persists across probe blocks; cd pins don't remove airborne projectiles). Test-only fix: unown non-ring weapons + clear projectile arrays at block entry; asserts rebaselined to pipeline semantics (1 chained stack, stunT `< 1e-9`, fresh bolt before draw checks). Gates 33/33 · 604/604 · boot runs=4.
- **2026-09-04 (session 10b) — 12.3 Snowball Launcher COMPLETE:** fixed 3 RED asserts from session 9: `applyPlayerSnap` hard-coded passive/synergy offsets → derived W/P/S offsets (real wire bug caught by roster growth); cap-5 new-weapon pool rebaselined to include snowball; added dedicated coverage (+17 logic: table/card-text/flight-burst/slow→freeze pipeline; boot live E2E). Harness traps: isolated probes must regrid every tick; `_nearest` = closest foe (clear leftovers); raise maxHp alongside hp asserts. Gates 33/33 · 583/583 · boot runs=4.
- **2026-09-04 (session 10) — discovery turn:** found session-9's undocumented 12.3 code in-tree; doc-synced first, root-caused the 3 RED asserts (pool baseline + `applyPlayerSnap` off-by-one) without fixing them. Gates then: 33/33 · 563 pass/3 fail · boot runs=4.
- **2026-09-04 (session 8) — Phase 23 COMPLETE:** tree already carried full 23.1–23.3; boot failed on the 23.3 HUD assert — root: it drove over-health on the throwaway `g2` game while #hp-label is driven by the MAIN game's seat-0 player, and hud.update() early-returns outside PLAYING/LEVELUP/PAUSED; DOM-stub `style.transform` needed a getter/setter. Test-only fix. Gates 33/33 · 560/560 · boot runs=4. Deleted stray `nul` (rule 8).
- **2026-09-04 (session 7) — doc-sync after token cutoff:** verified in-tree uncommitted 22.5/22.6/22.7 match docs; gates re-run green 33/33 · 536/536 · runs=4. Fixed stale How-to-Resume (still said "finish 22.1 → 12.3", Phase 11 at 11.9) and PLAN §3.15 legacy-placement mislabel.
- **2026-09-04 (session 6) — 22.6 death slow-mo REMOVED:** `deathDelay`/`deathTimescale` deleted; `_onDeath` → shake + bus + immediate `_gameOver(false)`, no DYING transient; dead `deathT` inits cleaned; HUD condition dropped DYING. Gates 33/33 · 536/536 · runs=4.
- **2026-09-04 (session 5) — 22.7 DONE:** gate itself correct (own-2-sources-max, user re-confirmed); symptom = draw sampling. Fix: synergies ×8 weighted in pool (`SYNERGY_DRAW_WEIGHT`), dedup `kind:key`. Never add an all-weapons-maxed requirement. Gates 33/33 · 536/536 · runs=4.
- **2026-09-04 (session 4) — scoping-only:** PLAN §3.15 + Phase 25 created (perf pass, profile-first D70, absorbs 10.9); over-heal REPLACES Phoenix Heart decided (+ verified consequences incl. clamped `heal()`, no heart magnet today, ceiling vs mid-run max changes, HUD visibility; `phoenix` = canonical test fixture — key-name asserts survive an effects swap); mobile zoom investigated: present+correct in-tree AND in main; real defect = lighting canvas stretch under zoom transform → 22.8 needs device repro; AGENTS.md hygiene rule #8 added + `nul` artifact removed via extended-length path recipe. Gates 33/33 · 536/536 · runs=4.
- **2026-09-04 (session 3) — 22.5 flame-range flat 150:** acquisition was 300–380 vs ~136 px visible stream → invisible damage gap; retuned flat per user choice; verified `_nearest` nulls out-of-range (no fuel waste). test-logic +4 (visible-reach replay, range ≤ visible envelope, flipped curve assert to FLAT) + boot behavioral E2Es with negative control. Harness traps: `shadow(r)` takes a radius; isolated probes need regrid; `rand()` stub at 0 = spread MINIMUM (pin 0.5). Also scoped (docs-only) user's six playtest notes → PLAN §3.14 + Phases 23/24. Gates 33/33 · 536/536 · runs=4.
- **2026-09-04 — Phase 22 partial: 22.2 + 22.3 code + tests (committed `e502417`, docs carry-over `e5e9469`, env-caveat revision `68c455e`; pushed to origin/overnight-2026-08-22 — harness push worked, env caveat revised); **PUBLISHED to Pages same day (user explicit ask: ff-merge 4b5d0c4..7f55874 into main + push — garlic/blades-invisible report now fixed on the user test channel)** 22.2 = `escapeFromSpots` pure radial ellipse-projection in pickups.js + `Pickups.update` per-step nudge (unmagnetized only) + game.js `_pickupSpots` at both run-start sites + kill-drop escape + `CFG.gems.escapePad` 6 (host-sim, snapshot-neutral) · 22.3 = `HEART_PAL.m03` teal→coral (`#ff9d7e`/`#e2503c`), palettes exported · test-logic +16 → **532/532** · gates 33/33 · boot PASS runs=4 · **22.1 audit verdict (no code): all 8 weapons present in sim + `combat.draw`; user's garlic/blades-invisible = Pages staleness (frozen main predates in-tree 16.2 fix)** · Phase 22 NOT ticked — remaining = 22.1 test-boot coverage (per-weapon draw attribution via null-`*Img` probing + explicit garlic-damage check + gem-escape E2E) + 22.4 docs/ticks → NEXT = finish 22.1 boot tests, then 22.4 sync, then 12.3.
- **2026-08-28 — Playtest defects round 2 report (DOCS-ONLY turn per user instruction — no code changes):** user (testing on the published GitHub Pages build — their only test channel; the work branch is unpublished, so Pages = frozen `main` predating Phases 11/12/13/16 → "I don't see the features of the updated version") reports: (1) Wraith Garlic + Aegis Blades sprites/effects invisible in the published build, possibly not causing damage (uncertain), possibly others (5 weapons visually confirmed fine) — 16.2's in-tree latent fix (blades/garlic per-player draw loop) is unpublished → 22.1 = in-tree audit (sprite + damage for ALL 8 weapons; garlic damage has no explicit check yet) + publish decision (rule 7 — flag, don't commit unprompted); (2) XP gems dropped inside non-traversable spots (m01 "ice pond" lakes / m02 koi pond = ellipse colliders; m03 open water traversable) must be pushed from center to the spot's outer edge → 22.2; (3) M03 gem color ≈ heart color → 22.3 contrast retune via the 13.10 `gemHeartFor` seam · Phase 22 added to Master Checklist + D73 (queue: between 12.2 and 12.3 — defects before new content, D71 precedent) + README roadmap · gates unchanged 33/33 · 516/516 · boot PASS runs=4 → NEXT = **Phase 22** (22.1 audit first), then **12.3**.
- **2026-08-23 — Phase 12 start: 12.1 Pyre Lance verification + 12.2 Bow & Arrow (code + tests + gates):** 12.1 = verification only (16.3 acceptance fully in-tree and green: reach band 1.32–1.34× of 94.6 px, front speed exactly 2×, LEAD `flameMomentum` 0.5 ≈34 px, mid-torso origin — no remaining max-velocity ramp tuning) · 12.2: `CFG.weapons.bow` (rate 0.55→0.30 / dmg 16→42 ×5) + `CFG.combat` arrowSpeed 640 / arrowLife 1.4 / arrowR 4 / arrowKb 150 · `combat.fireArrow` straight-flight single-target (NO pierce — Heart-Piercer 12.6), aims `_nearest` within wandRange, mid-torso origin (16.2) · snapshot-neutral projectile: wire change = SNAP_V 1→2 (playerSnap 26→27 slots, bow joins 8-weapon roster, `unpackState` length check key-derived) · `icons.bow` + arrow sprite · test-logic 12.2 block (table monotonicity · card text L1/L2 · spawn/hit/no-pierce/lifetime · source-scan icon invariant) + 11.2 sync block rebased (8 keys / 27 slots) · test-boot: bow in forced-pick + arrow observation + 16.2 arrow mid-torso origin + all-8 PASS line + bench 8 weapons L5 · gates 33/33 · 516/516 · boot PASS runs=4 → NEXT = **12.3 Snowball Launcher** (weapon #9: small-AoE lob, accumulating slow stacks, 3 stacks → brief freeze).
- **2026-08-23 — 11.12 final co-op gate (code + tests + gates; overnight session):** 3P live run pumped to VICTORY at t=300 (was cut off at the boss wave t≈241) — join → run with locks (11.5) + scaling (11.3) → victory complete on host + both clients · pause-on-blur = host-only (host blur-pause / client `pause()` no-op / host resume → victory still reached) · co-op mobile parity: `.hud-btn` 72×72 (PAUSE/DASH) + ≤520 px corner layout lifts BL/BR seat panels above the bottom cluster (`calc(112px + var(--safe-b))`) + safe-area insets · CSS = test-logic content asserts (harness cannot see CSS from test-boot; regex pitfall: optional `;` goes BEFORE the rule's closing brace) · test-logic 495→499 · gates 33/33 · 499/499 · boot PASS runs=4 → NEXT = **11.13 web transport impl (NAS-side, D64 — needs user's NAS access)**. **Phase 11 complete except 11.13.**
- **2026-08-23 — 11.11 solo invariance guard (test-only + gates; overnight session):** one-shot `soloInvDone` in test-boot beside the 11.7 `soloHudDone` block (fires first PLAYING step after `bossSpawned`, run-2 m01): net-free solo (net null / netRole solo / roster+seat empty) · 1 local player, no remotes · coopS 1 · weaponCap(1) = base 5 · bossCount(1) = 1 · exactly one boss-flagged Wraith · every weaponOwner entry = local player + empty `_ownerExclusion` (map IS populated solo — self-ownership — so per-entry asserts) · HUD half already covered by 11.7 (no `#hud.coop` class + TL = local) + 11.9 solo-CSS asserts in test-logic · test-only change (tools/test-boot.mjs + bossCount import) · gates 33/33 · 495/495 · boot PASS runs=4 → NEXT = **11.12 final Phase 11 gate** (gates + multi-client E2E coverage + co-op MOBILE PARITY: ≥72 px touch, corner layout small screens, pause-on-blur; CSS changes → test-logic content asserts).
- **2026-08-23 — 11.10 boss count (code + tests + gates; overnight session):** N players = N bosses of the current level — pure `bossCount(n)` in coop.js + `_spawns` wave = N off-screen level-boss spawns at `B.at` (count fixed at spawn moment; solo = 1 draw, bit-identical) · co-op banner `AWAKENS ×N` (solo string unchanged) · stats ramp untouched (11.3 ×diff×coopS) · `_lights` now lights every live boss (was first-only) · victory = survive-to-time unchanged (boss = threat, not goal; boss SCHEDULE is Phase 17, not here) · bosses ride the existing enemySnap pipeline (no protocol change) · test-logic +3 (495/495) + test-boot 3P E2E (host t→241 → 3 Wraiths ×2400×diff×1.66 + ×3 banner + client sees 3 on the wire — injected at the END of the co-op section, after every rng-sensitive sub-E2E) + solo m02/m03 exactly-1 asserts · gates 33/33 · 495/495 · boot PASS runs=4 → NEXT = **11.11 solo invariance** (D57 re-read: no co-op overhead, solo baseline includes char select + per-char stats).
- **2026-08-23 — 11.9 co-op minimap + buttons (CSS-only + first CSS content asserts; overnight session):** `#hud.coop` `--coop-mm: clamp(91px, 16.5vmin, 157px)` = 0.66× the solo minimap at every clamp stop (±1%) · co-op minimap (already bottom-center, 11.7) takes the var width · NEW `#hud.coop #btn-pause` fixed beside the minimap (right edge 8 px left of its left edge; offset tracks `--coop-mm`) · DASH stays top-right (11.7) · mute NOT moved (D72) · solo = zero rules touched · test-logic +6 (492/492 — harness first reads main.css: solo clamp unchanged · 66% ratio · anchors · no co-op dash/mute rules) · gates 33/33 · 492/492 · boot PASS runs=4 → NEXT = **11.10 boss count** (N players = N bosses; stats ramp already in place per 11.3/Q7).
- **2026-08-23 — 11.9 rescope (D72, docs only, no code):** user — mute already in Pause-menu Settings (13.8/D48) → 11.9 = co-op minimap ~66% size + PAUSE-only reposition near the minimap (mute move dropped); USER-INPUT-LOG entry + PROGRESS checklist line + D72 decision + PLAN §3.8 + main.css comment · git line → `42f0e7b` · gates green as of 2026-08-23 (33/33 · 486/486 · boot PASS runs=4) → NEXT = **11.9 implementation** (pending go-ahead) → 11.10–11.12.
- **2026-08-23 — docs audit & full-repo sync (no code changes):** docs-vs-reality pass — all three gates re-run green (33/33 · 486/486 · boot PASS runs=4) · working tree clean on `overnight-2026-08-22` @ `2e04eda` (last code commit `6a34fa8`) · node v24.11.0 · server DOWN (47893 not listening) → PROGRESS.md: Status date + D71 queue order + Git line (`2e04eda`) + Server line corrected; 16.4 session line added (was missing); How-to-Resume baselines re-based (33 modules · 486 checks · runs=4 incl. m03 + co-op E2E) · README: Phase 11 status (11.1–11.8 done) + roster accuracy (7 weapons / 5 synergies current, 10/9 = Phase 12 target) + Phase 12 line (12.1 scope absorbed by 16.3) · PLAN.md: §3.8/§3.9 section headers de-staled (impl state) · `RESEARCH_FINDINGS.md` verified complete (§1–§7, sources cited; §6 open items = the 11.13 NAS pre-checks) → NEXT = **11.9 co-op minimap + buttons**.
- **2026-08-22 — 16.4 Phase 16 gates (verification, no code changes):** all three gates green with every Phase 16 assert in-tree — 33/33 · 486/486 · boot PASS runs=4 (m01 snow probe 16.1 · exact spawn-origin asserts 16.2 · flame length/flow A/B 16.3); co-op invariance: 16.1 render-only `Snow.draw` (host + client), 16.2/16.3 host-sim-only (no projectile key in the sync protocol) — live 3P sync E2E green in the same boot run. **Phase 16 COMPLETE** → next **11.9 co-op minimap + buttons**.
- **2026-08-22 — 16.3 Pyre Lance flame (+33% length / ≥2× flow / LEAD-while-moving; overnight session):** config solved numerically — flameSpeed 260→520 / flameSpeedVar 80→160 (front speed exactly 2×, spread ratio preserved) + flameLife 0.65→0.33 / flameLifeVar 0.15→0.075 (±ratio preserved; drag 2.0 unchanged) → mean reach exactly 1.33× (94.6→125.6 px) · `flameMomentum` 0.5 — flame inherits a share of the owner's velocity (D70 user's momentum hypothesis; owner null = no momentum) · co-op snapshot-neutral (no projectile key in sync — same as 16.2) · test-logic +6 (486/486) + test-boot E2E (seeded `Math.random = mulberry32(163)` — math.js's `rand` is a const wrapper around Math.random, combat.js imports it live: re-seeded A/B pair → identical emission, only momentum differs; bit-exact x-travel = engine recurrence replayed from the same start point; reach band 1.32–1.34×; moving shot leads the stationary control by the momentum integral ≈34 px) · gates 33/33 · 486/486 · boot PASS runs=4 → next **16.4 Phase 16 gates** (then 11.9).
- **2026-08-22 — 16.2 projectile spawn origin feet→mid-torso (code + tests + gates; overnight session):** root = `Player`'s `(x,y)` IS the feet (sprite bottom-anchored) and all 5 fire sites used it for aim + spawn → `CFG.combat.spawnOriginFrac` 0.55 + `Player._spawnY()`; wand/axe/pistols/bombs/flame aim AND spawn from mid-torso (bomb lob dist + flame `_flameAng` too); axe return + orbiter center (dmg/knockback/draw) → mid-torso; garlic stays feet-centered (ground pulse, documented); **co-op snapshot-neutral verified** (no projectile key in the sync protocol at all — host-sim-only); **latent-fix found while verifying melee: the blade orbiters + garlic ring were NEVER drawn** (`combat.draw` read never-assigned `this.player`/`this.orbitT`/`this.garlicT` — garlic radius would have been NaN → browser `arc()` throw) → `draw(ctx, t, players)` per-live-player on sim `_orbitT`/`_garlicT` + client cosmetic clock advance in `_clientUpdate` (remotes: no angle in snapshot → not drawn); test-logic +4 (480/480) + test-boot E2E (5-weapon exact mid-torso origins, blade orbit discrimination on a manually-populated grid — grid only rebuilds in `enemies.update`, first boot run failed `mid hit=false` for exactly this reason; combat.draw blade sprites + 1 garlic arc) · gates 33/33 · 480/480 · boot PASS runs=4 → next **16.3 Pyre Lance flame** (+33% length / ≥2× flow / LEAD-while-moving).
- **2026-08-22 — 16.1 m01 falling-snow regression (reproduce → fix → non-regression; overnight session):** D70 reproduce-first — new test-boot `probeSnow` (ctx-stub path-op deltas across one `snow.draw`; `drawOps` gains `ellipse`) quantified the repro BEFORE the fix: **run-1 m01 in-game snow pass drew `arc=0 ellipse=0 fill=90`** (90 fills on EMPTY paths — `beginPath()` with no path command, silent no-op in canvas AND the stub = invisible) → git forensics: **13.4 `a8c332b` swapped the snow `else ctx.arc(...)` for `else if (bubble) {...}`** (13.2's check only asserted the `snow.kind` ROUTING, never broken; kind routing + motion were fine all along) → fix = snow arc restored exactly as v1.0.0 `03eeac2` (one branch in `Snow.draw`) → non-regression probe at 6 sites (run-1 m01 in-game + m01/m02/m03 menu backdrops + m02/m03 run starts; per-kind counts snow 90 arc/90 fill · petal 90 ellipse/90 fill · bubble 180 arc/90 stroke/90 fill) · gates 33/33 · 476/476 · boot PASS runs=4 (10.4 bench baseline shifts — m01 renders now really draw the 90-flake pass) → next **16.2 projectile spawn origin feet→mid-torso**.
- **2026-08-22 — 11.8 co-op theming (code + tests + gates; overnight session):** per-char UI theming (D62 channel, req 6b/6d) — `CFG.characters[*].accent` + pure `charAccent(key,seat)` in coop.js (ghost → per-seat Pac-Man tint; unknown → starter fallback) · hud.js `applyTheme` → `--char`/`-soft`/`-line`/`-inset`/`-grad` on local roots (`#hud` + `#touch-ui`, change-detected; local key = seat-0/`chars[0]` co-op, `game.charKey` solo) + each seat-panel root on char (re)assignment · main.css: HP fill `.hp-bar .bar-fill` themed (**fixes 11.7 latent: seat-panel fills had NO background**) + dash icon/border/ring on `#btn-dash`/`#btn-dash-hud`/`.seat-dash` with legacy fallbacks · test-logic +7 (476/476) + test-boot E2E (ghost run: local + seat-1/2 = Blinky/Pinky/Inky tints; 3P: Mage local + Warden/Ranger accents + 4th joiner Swash) · gates 33/33 · 476/476 · boot PASS runs=4 → next **Phase 16 playtest defects** (16.1 m01 snow — reproduce FIRST per D70).
- **2026-08-22 — design batch documentation: playtest defects + new phases 16/17/18/19/20/21 (docs only, no code):** user input captured (log: "Batch design input: gameplay/weapon/item/character ideas + 3 bug reports (document only)" + "VS DOES have skip/reroll/banish…" correction) → VS level-up-action research (`RESEARCH_FINDINGS.md` §7: wiki Skip/Banish/Reroll pages + 4 corroborating sources; user-corrected model: level-up-screen STATS, not cards; skip→~66% next-level XP per user / 20% per Reroll-Banish use per wiki; reroll excludes discarded set from redraw only; banish = rest-of-run removal) → new PLAN §3.10 (run durations 5/10/15/20/ENDLESS + boss schedule 4:00/9:00/14:00/19:00 + every 5:00 endless; 15-min = 3 bosses) · §3.11 (SKIP/BANISH/RE-ROLL — meta-store unlock, max 5 uses/run/each, NOT equippable/selectable/level-able) · §3.12 (equipment icons + level numbers under XP bar; fuel bar beneath player — revises D24) · §3.13 (9 weapons + 2 items + 5 characters VERBATIM) + §3.4 passive-start + §3.7 persistence + 3 risk rows → new PROGRESS phases 16 (defects: m01 snow · projectile origin feet→mid-torso · flame +33%/≥2×/LEAD — supersedes 12.1) · 17 (run durations) · 18 (level-up actions) · 19 (HUD) · 20 (passive-start, before 15 for economy baseline) · 21 (extended roster) + D65–D71 + queue revision (11.8 → 16 → 11.9–11.13 → 12 → 20 → 15 → 17 → 18 → 19 → 14 → 21 → 10.9 → 2.9); no code changes, gates not re-run (docs-only).
- **2026-08-22 — 11.7 co-op HUD corners (code + tests + gates; post-compact session):** `#hud-left` = ALWAYS the SEAT-0 panel (solo invariance 11.11) + 3 generated corner panels for seats 1/2/3 = TR/BL/BR (`#hud-seat-1/2/3`) — char name + 2× roster/ghost face (per-seat tint cache) + HP bar/label/low pulse + LV badge + XP fill + dash `--cd` ring, display-only (input buttons follow the LOCAL player); visible count = `min(4, players)` in join order (A5); `#hud.coop` CSS + minimap → bottom-center in co-op; `initHud` returns `{ update, panels }`; test-boot E2E (solo: no coop class + TL = local; 3P: TL = host data + TR Warden/BL Ranger named + HP/LV tracked + BR off; mid-run 4th joiner → BR Swashbuckler; leave → hidden) — the failing 4th-joiner assert was a missing explicit `hudHost.update()` before it (`hostG` has no render loop in test-boot; the real app's render hook drives the HUD per frame); gates 33/33 · 469/469 · boot PASS runs=4 → next **11.8 co-op theming**.
- **2026-08-22 — 11.6b exclusivity closure (code + tests + gates; post-compact session):** D29 respec done — synergy first-pick-wins (`cardOffers` synergy branch honors `exclude` — one Set serves weapon+synergy keys, namespaces disjoint; `pickCard` + `_remoteLevelUps` register `kind==='synergy'` level-1 picks into `weaponOwner`) + starting-weapon pre-ownership (seeded in `_applyChar` for remotes incl. mid-run joiners + `_coopStartRun` for the host's own starter → starting weapons excluded from OTHERS' offers, owner keeps own upgrades; `_ownerExclusion` gains `!pl.synergies[k]` guard; solo map never populated → invariance); test-logic +5 (469/469: owned synergy never offered at max sources ×3 seeds, unowned control, rng-parity no-perturb) + test-boot 11.6b E2E in the live 3P char-sync run (pre-ownership wand/garlic/blades → their seats + per-seat exclusion; forced host blight pick → ownership + no self-exclusion; pl1 pool = {phoenix only} with cap+passives maxed — `{wand:5,garlic:5,axe:5}` gates blight+phoenix only); test-boot 11.6.3 ghost-section flake fix (first boot run of the session failed the strict `weaponOwner` count assert — a live pump auto-pick legitimately adds a 5th owner entry; same class as the 11.5 flake fix → assert the 4 true first-picks, not the count); gates 33/33 · 469/469 · boot PASS runs=4 (×2) → next **11.7 co-op HUD corners**.
- **2026-08-22 — 11.6.4 co-op character sync/E2E (code + tests + gates; post-compact session):** D53 join profile carries the selected char (`joinProfile(meta, chars, sel)`); pure `selChar`/`assignChars`/`resolveChars` in `coop.js` (seat-order unique, D56; all-starter → every seat ghost, D59 wins) + host `_applyAssign` (replaces `_applyGhostFlow`: per-seat spawn at run start + mid-run re-assignment — taken pick → next unlocked char, else ghost with fresh seeded deal; host entry patched for seat-0 authority) + `_applyChar`/`buildGhost(color)` per-seat tint + client renders host-assigned char (LS untouched) + taken-select greyed/denied (D56) + `_netState` seat guard (state outruns joiner's roster) + `_applyAssign` no-ghost early-return fixed; test-logic +11 (464/464) + test-boot E2E (3P distinct per-seat stats; mid-run late-joiner mage→swash reassignment, client renders it; greyed-select subtest) + 11.6.3 (b) re-baselined to D56; gates 33/33 · 464/464 · boot PASS runs=4 → next **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.3 ghost fallback flow (code + tests + gates):** D59 all-starter lobby → EVERY seat ghosts (faceless sheet, per-seat Pac-Man tint, baseline 100/×1.0/265, NO starting weapon) + 2 UNIQUE starting-weapon offers per seat, never duplicated — pure `sanitizeChars/allStarterLobby/ghostColor/allocateGhostOffers/joinProfile` in `coop.js`; host `_applyGhostFlow()` (run start + mid-run roster; remotes auto-pick `offers[i][0]`, host entry 2-card LEVELUP, `weaponOwner` per pick, mid-run joiner ghosts in) + client self-detect from `netRoster`; two E2E-caught bugs fixed (ghost seats kept starter wand/stale maxHp → `weapons = {}` + `recomputeStats` in ghost branches; `allStarterLobby` called with already-unwrapped profiles → always-ghosted → callers pass roster entries); test-logic +30 (453/453) + test-boot E2E (a) 3P all-starter + mid-run 4th joiner (4 distinct owners) + (b) fresh-room mixed lobby no-ghost; gates 33/33 · 453/453 · boot PASS runs=4 → next **11.6.4 co-op sync/E2E** + **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.2 character select UI (code + tests + gates):** full-screen 2b select (D61) + D58 Soulshard shop + D57 per-card stats + run-start character application — screens.js select overlay (Character button → #screen-select → Confirm closes; per-card roster art ×2 + HP/dmg/speed + start weapon + desc; locked shows `Locked · cost ◆`, one tap buys + selects; denied blip + shake; wallet line; `taken` hook for 11.6.4 greyed-out) · pure meta.js `loadChars/saveChars/isCharUnlocked/buyChar` + `loadSelectedChar/saveSelectedChar` (`qsurv.chars.v1`; ghost/unknown never unlocked/selectable) · `game.setCharacter/buyCharacter` (persisted + roster art/flash/shadow swap; Player ctor from `roster[charKey]`) · index.html button + screen · css `.char-card` · test-logic +16 (423/423) + test-boot E2E (open/lock/buy+select 3500/reject 7500 > 6499/persist/confirm/restore → runs 1–4 baseline intact) · gates 33/33 · 423/423 · boot PASS runs=4 → next **11.6.3 ghost flow** + **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.0 + 11.6.1 characters: select-UI research + roster config/art/stats (code + tests; O USER-APPROVED 2026-08-22 with 3 revisions: mage 60hp/275sp, ranger 110hp/320sp, warden 230sp):** 11.6.0 = 2 sources cited (Wikipedia *Pac-Man* ghost colors → `CFG.ghostColors`; NN/g *10 Usability Heuristics* #1/#3/#4/#8) → 2b full-screen select confirmed; 11.6.1 = `CFG.characters` table (D58 costs; **O stat values USER-APPROVED with 3 revisions** — mage 60hp/275sp, ranger 110hp/320sp, warden 230sp; final: mage 60/×1.35/275 · warden 150/×0.85/230 · ranger 110/×1.0/320 · swash 90/×1.15/300 · ghost 100/×1.0/265) + `CFG.meta.charKey` + `player.js` charDef/setCharacter/reset-start-weapon/per-char-speed/recomputeStats + 4 new art builders + `buildRoster` (mage bit-identical to original playerFrame) + test rebaseline (solo default now mage → meta-L1 maxHp 80 (60+20); applyCard block → ghost-baseline objects; startWeapons → char-weapon check) + new 11.6.1 test-logic block (23) + test-boot roster section (D62 area mage<warden<brute, tint rebuild) · gates 33/33 · 407/407 (+23) · boot PASS runs=4 (maxHp 80) → next **11.6.2 select UI** + **11.6b exclusivity closure**.
- **2026-08-22 — process rule: user-input documentation, capture-before-reason v4 — kick-off false-capture fix (docs only, no code):** v3 session bug (own, 16:05 turn): user directive "add kick-off prompt to do-not-document list" was LOGGED but the AGENTS.md denylist edit never landed → new session (user snippet) then false-captured the kick-off prompt: denylist had only the category label "kick-off", the message's actionable instructions read as substantive, and the unscoped "when in doubt, document" tiebreaker out-argued the denylist → root cause = uncapped tiebreaker + label-not-text match + first-call append mechanics left to the model (snippet deliberation) → v4 fixes: exact-text kick-off entry (banner-tolerant) + **Precedence** line (denylist match → never document; tiebreaker does NOT apply to denylist matches; template + substantive additions → document) + append mechanic (insert after stable `## Entries (newest first)` line, no prior read) → AGENTS.md #8 + D63 (v4) + log-header mirror. NOT logged: user's diagnostic prompt this turn ("DO NOT DOCUMENT" banner).
- **2026-08-22 — process rule: user-input documentation, capture-before-reason v3 (docs only, no code):** user-directed web research BEFORE rule revision (real-world date verified standalone FIRST: 2026-08-22 15:41 EDT) — evidence: Chroma context-rot 18-model study (focused ~300t ≫ ~113k full history on same question → re-injectable verbatim log; lower needle-question similarity degrades faster → verbatim not paraphrase), RULER (nominal window ≠ effective; ~half of ≥32K-claiming models fail at 32K on harder tasks), lost-in-the-middle (mid-context recall), 2026 arXiv (premature termination vs context length; SLEUTH context dilution + protocol adherence up to +19; RCWT budget displacement), Anthropic context engineering (compaction silently drops critical items; structured note-taking = named mitigation) → rule revised ask-first → **capture-before-reason** (non-denylisted user msg → FIRST tool call = log append, no pre-write reasoning; verbatim no-omissions format; pointer pattern reference-by-title; Context ≤20 words; denylist + pending-question nuance; explicit user opt-out; when-in-doubt=document) → AGENTS.md non-negotiable 8 + D63 (v3) + log-header mirror + format notes; this session's prompts NOT logged per user "DO NOT DOCUMENT" banners.
- **2026-08-22 — process rule: user-input documentation, ask-first v2 (docs only, no code):** user rule (set 14:05, refined 14:18/14:53) — when SUBSTANTIVE human-user input/feedback/responses arrive (directives; decisions/approvals incl. corrections; answers; bug reports + corrections; playtest feedback; phase recommendations), agent IMMEDIATELY asks whether to document before ANY other action; yes → `docs/USER-INPUT-LOG.md` (verbatim, unmodified, timestamped + classified) then act; no → proceed; NEVER ask/document: trivial messages ("continue") + commonly-repeated template prompts (kick-off / closing-handoff prompts, "Stage, commit, push.", "proceed"-family) + agent-side content; unsure → ask → AGENTS.md non-negotiable 8 + D63 (v2) + `docs/USER-INPUT-LOG.md` created (entry #1 = 14:05 rule, meta-line trimmed per feedback; entry #2 = 14:18 scope clarification; 14:53 trigger-v2 input NOT logged per user "No").
- **2026-08-22 — 11.6.1 approvals + Phase 15 defect logged (docs only, no code):** user answered the blocking O's — (1) roster resolved D62: **Mage = original/default sprite (starter)**; Warden = NEW larger heavy-armor sprite (>original, <brute-enemy size) + SLOWEST of the 4; Ranger/Swashbuckler new sprites; ghost = sheet-ghost with per-player UNIQUE Pacman color → in-run UI colors; (2) unlock model D58 RESOLVED = Soulshard shop **Mage default · Ranger 1500 · Warden 3500 · Swashbuckler 7500**; ONLY remaining O = per-char stat values (propose in 11.6.1). NEW DEFECT (user playtest): **Pyre Lance (flamethrower) NEVER in level-up offers in ≥20 recent runs (~24 h)**; pistols frequent, bombs occasional, base weapons fine; (snowball/ring never-seen = Phase 12, unimplemented — expected, NOT the bug) → **Phase 15** (15.1 reproduce/quantify → 15.2 root cause — mechanisms logged UNVERIFIED: 3-of-N pool sample, cap-5 slot-fill blocks late new-weapon offers, exclusion interplay → 15.3 fix → 15.4 regression) queued after Phase 12 (fix once on the final 10-weapon roster). PLAN §3.8 (mapping/unlock/ghost resolved) + D58 rev + D62 + 11.6 checklist + README n/a updated.
- **2026-08-22 — 11.6 RESPEC (docs only, no code):** user answers — NO duplicate characters/weapons (1a–1e), ghost fallback + 2-unique starting-weapon offers (4b), per-char base stats visible in select (1e/4 archetypes), select UI 2a/2b + VERY-brief sources-cited research (2c/2d), solo select confirmed, character unlock meta progression distinct from map unlocks (4a, model = O), character + weapon/synergy roster modularity (1f/5) → D28/D29/D34 revised + D56–D61 new + PLAN §3.8 characters bullet rewritten + §3.4 roster modularity + 11.6 rewritten (sub-steps 11.6.0 research → 11.6.4 sync/E2E) + new 11.6b (synergy exclusivity + starting-weapon pre-ownership) + 11.11 re-read (D57: invariance = no co-op overhead) + README roadmap 11 line. OPEN (block 11.6.1): archetype→weapon mapping + names + stat values, unlock model.
- **2026-08-22 — 11.5 boot-gate flake fix (test-only, no game code):** root-caused boot failure `remote first-pick owns wand` — `reset()` grants `startWeapons:['wand']` with NO `weaponOwner` registration, the 11.5 E2E asserted ownership on ALL gained keys (incl. upgrades), and pump rng-picks leaked into the forced-pick section (cap + garlic-race variants); fix: deterministic section baseline (all players → `{wand:1}`, `weaponOwner={}`) + ownership assert on true first-picks only · gates re-verified 33/33 · 384/384 · boot PASS runs=4.
- **2026-08-22 — session resume (post-compact, docs only):** state verified vs docs (11.1–11.5 done; gates 33/33 · 384/384 · boot runs=4); plan set **11.6 characters** = 11.6.1 roster config+art (`CFG.characters` + 4 builders in `art/characters.js`) → 11.6.2 selection UI + run-start default weapon → 11.6.3 co-op profile def + E2E + solo guard; 3 open decisions (duplicate chars / start-weapon-vs-exclusivity / 11.11 invariance read) proposed w/ defaults, awaiting user.
- **2026-08-22 — 11.5.4 co-op exclusivity E2E + 11.5 close-out (code + gates):** test-boot 11.5 E2E in the live 3P run (host real-pick garlic → ownership/no self-exclusion/remote exclusion; passive pick isolation; remote auto-pick ownership + never-garlic; cap ≤3) + PASS-line record · gates 33/33 · 384/384 · boot PASS runs=4 · **11.5 TICKED** → next **11.6 characters** (RESPEC'd 2026-08-22; 11.6.1 answers IN: D58 unlock shop RESOLVED Mage/Ranger 1500/Warden 3500/Swashbuckler 7500 + D62 roster: Mage=original sprite (starter) · Warden=new large armor, slowest · Ranger/Swashbuckler new sprites · ghost=sheet + per-player Pacman color → UI; ONLY O left = per-char stat values) — see 11.6 checklist + D28/D29/D34 rev + D56–D62: archetypes + per-char stats visible in select + co-op unique-character + greyed-out + Soulshard shop unlock + ghost fallback + 2-unique-offers + roster modularity; 11.6.0 = VERY-brief sources-cited select-UI research, default 2b full-screen; sub-steps 11.6.0→11.6.4 in checklist) + **11.6b synergy/starting-weapon exclusivity closure (D29 respec) BEFORE 11.7.** NEW DEFECT tracked as **Phase 15** (user playtest: Pyre Lance never offered in ≥20 runs — root cause pending, 15.1 reproduce first; queued after Phase 12).
- **2026-08-22 — 11.5.2/11.5.3 per-player cardOffers + host weapon ownership (code + gates):** `cardOffers(..., cap, exclude)` (exclusion = other-owned keys never offered new/upgrades; passives never locked; per-picker synergy gating) + `game.js` `weaponOwner` first-picker map (reset per run, registered at both pick sites, persists through leave) + `_ownerExclusion(pl)` + all 3 offer sites pass `weaponCap(players.length)` · test-logic 11.5.2 block (6) · gates 33/33 · 384/384 · boot PASS runs=4 (solo level-ups invariance). NEXT: 11.5.4 co-op multi-client E2E (exclusion + per-picker picks live) + 11.5 tick.
- **2026-08-22 — 11.5.1 base maxWeapons 4→5 + equip-cap helper (code + gates):** `CFG.run.maxWeapons` 5 + pure `weaponCap(n)` in `js/net/coop.js` (D42 cap table) · test-logic card-blocks re-based to cap 5 (trio pool {inferno,tempest,phoenix}) + 11.5.1 block (3) · gates 33/33 · 378/378 · boot PASS runs=4.
- **2026-08-22 — 11.4 player leash (code + gates):** `CFG.coop.leashR` 700 (A2 shared expanded vision radius) + pure `leashClamp` POCS in `js/net/coop.js` (radius-R disk intersection; 8-iter cap) · host `_coopLeash()` per `_playingUpdate` (live only, world re-clamp, solo no-op) · `_lights()` co-op vision = leashR (solo 510 intact) · test-logic 11.4 block (8 checks) + test-boot 3P E2E (1.5R teleport → pairwise ≤ leashR convergence poll) · gates 33/33 · 375/375 · boot PASS runs=4 (×2). Also fixed: 11.2 checklist tick was missing (recorded done in Status/log all along).
- **2026-08-22 — 11.3 difficulty scaling (code + gates):** `enemies.coopS = coopScale(players)` in host sim (set in `startRun` + `_netRoster` mid-run join/leave) · spawner curves take `s` (aliveCap/batch ×s, interval ÷diff; solo default `s=1` bit-identical) · `Enemies.spawn` HP/dmg ×coopS incl. boss (Q7) · test-logic 11.3 block (coopScale values/clamp, curve scaling, spawn + boss HP/dmg) → 367/367 · test-boot 3P E2E: `coopS=1.66` armed at `startRun`, every non-boss maxHp = base×S · E2E robustness at the new density: `keepAlive()` (revive seats + `pickCard(0)` — idle host player hits LEVELUP) in every pump loop + position-tracking as convergence poll (knockback transients) · gates 33/33 · 367/367 · boot PASS runs=4.
- **2026-08-22 — 11.2 host-authoritative sync (code + gates):** `js/net/sync.js` (SNAP_V=1: key tables in CFG order, playerSnap 26 slots r1, enemySnap + E_FLAG_*, pickupSnaps stable pool slots, stateMsg time=r2, unpackState) + `conn.js` CoopConn · combat/enemies/player owner kill-credit + players-array refactor · game.js co-op mode (host: remotes from D53 profiles, per-step stateMsg broadcast, mid-run-join reconcile, auto card-pick remote level-ups; client: no sim, sendInput every step, arrival-time interp, dead-player spectate) · serve.mjs roster per-seat profiles + state/runstart relays · UI #btn-coop/#coop-status/#btn-coop-leave · gates 33/33 · 360/360 (+22) · boot PASS runs=4 + 11.2 E2E (real relay ephemeral port 0; host+2 clients; shared seed; client tracks host; KeyD drives host remote; leave → reconcile → run continues). E2E gotcha fixed in-test: pump steps BEFORE the first-state wait (host broadcasts only inside `update()`); sync pumps must `await tick()` to let WS round-trips land.
- **2026-08-22 — 11.1 co-op transport (code + gates):** zero-dep RFC 6455 WebSocket upgrade + frame codec in `tools/serve.mjs` (import-safe entry guard — `resolve(argv[1])` vs `fileURLToPath`; SHA-1 accept; masked/partial/oversized frame handling, 64 KB cap) + `attachCoopRoom` (the one room: first hello opens at host seat 0 with host levelKey, join/leave/full/host-leave-close, input/state/runstart relayed to the OTHER seated clients, roster broadcast) + pure protocol/room/D53 `profileFromMeta`/`coopScale` in `js/net/coop.js` + `CFG.coop {maxPlayers:4, perPlayer:0.33}` · test-logic 11.1 block (room semantics, D53 profile, codec round-trip/partial/oversized, RFC accept vector, scaling clamp) + test-boot E2E: real serve.mjs on ephemeral loopback port 0 + Node-native WS clients (host seat 0 / seats in join order / 5th→full / leave+roster / host-leave→closed / room re-opens) · gates 31/31 · 338/338 · boot PASS runs=4.
- **2026-08-22 — 11.13 web-play research + transport decision (docs only):** binding protocol complete (date-verified FIRST per D36; 8 sources cited; 100%-free; hype-avoidant) → `docs/RESEARCH_FINDINGS.md` (current host-authoritative/relay model validated; CF-DO free-tier math + input-coalescing fix; Tailscale/Oracle facts; WebRTC rejected); follow-up: DS124 NAS hosting verified feasible (ARM/no-Docker caveat, Tailscale-on-Synology official) → **D64: 11.13 transport = Tailscale on the user's DS124 NAS** (installed + connected 2026-08-22, user-confirmed) running unmodified `serve.mjs` as boot-start task; players join the tailnet; next code task = **11.6.4 co-op sync/E2E** (11.13 impl after 11.12).
- **2026-08-22 — 13.13 README + Phase 13 close-out (docs-only):** README **Levels** section (3-map table: Evernight Wood 4200×3200 / Higan 4200×3200 / The Drowned City 5145×3920; chained 3× victory unlocks; rosters + boss names verified against `characters.js` slot re-skins; per-level identity line) + Roadmap Phase 13 line → DONE 2026-08-22. **Phase 13 COMPLETE (13.1–13.13)** — next session: Phase 11 multiplayer (11.1–11.13 spec; 11.13 web-play research = binding protocol step).
- **2026-08-22 — 13.12 Phase-13 gates (verification, no code changes):** all three green — 30/30 · 316/316 · boot PASS runs=4 (M02/M03 real runs, unlock flow, boss, victory, Settings mute/zoom E2E, per-level scores, flavor); mobile parity CSS-verified (`.lv-card`/`.set-row` min-height 72 px, touch zoom default 0.80, safe-area/pause-on-blur invariants untouched).
- **2026-08-22 — 13.11 per-level audio (code + gates):** `music.js` `FLAVOR` data (higan: temple bell D2 + 2.76× inharmonic partial 1/loop, chime pings D5/F5/B4 3/loop, taiko base 6/loop → boss 8/loop s8 0/4 @ 55 Hz ×1.6 gain; drowned: 3 rising bubble chirps 0.6→1.0×, 1 whale glide 160→240→150 Hz lowpass-600 3.5 s/loop) + 5 voices on the 10.6 lattice, m01 = no entry → zero flavor oscs; muffle = level→lowpass→musicBus (`muffleOff` 12k no-op / `muffleM03` 600 Hz, retuned per `startMusic`); `CFG.audio` gains bell .05/chime .04/taiko .09/bubble .03/whale .06; test-logic: FLAVOR shape block (5 checks) + 4 fake-ctx pumps (m01 invariance 49/60/96/294, higan 24 bell oscs/36 chimes/72 taiko, higan-boss 96 taiko, drowned 36 bubbles/12 whales; `t0` cutoff excludes margin-scheduled loop-13 bar-0 hits) — 316/316; gates 30/30 · 316/316 · boot PASS runs=4.
- **2026-08-22 — 13.10 per-level flavor (code + gates):** `items.js` GEM_PAL/HEART_PAL + `gemHeartFor(levelKey)` (m01 palette = original; buildItems drops gem/heart); `game._reskinPickups()` on construct/startRun/previewLevel; `_gameOver` detects the newly-unlocked level (before/after `isUnlocked` around `recordWin`) → 'NEW MAP UNLOCKED' banner after DAWN BREAKS; `screens.js` banner queue (drains after `CFG.ui.bannerMs` 2600 in per-frame `update`; concurrent emits queue instead of clobbering) + game-over `Unlocks` row (next locked level, X/3 wins) · test-boot E2E: synthetic `_gameOver(true)` at wins.m01=2 → banner exactly once + 6th stat row 'The Drowned City — 0/3 wins' (stub DOM: assert via `children`, textContent does NOT aggregate), no dup on 2nd victory, wins/scores state restored for the run flow; pickup reskin asserts on m02/m03 runs + boot-time `gemHeartFor` shape check · gates 30/30 · 303/303 · boot PASS runs=4.
- **2026-08-22 — 13.9 per-level high scores (code + gates):** pure `scoreKeyFor(levelKey)` in `ui/screens.js` — m01 keeps original `qsurv.hiscores.v1` (no data loss), siblings `qsurv.hiscores.<level>.v1`; gameover ranks/saves within the run's level (`game.levelKey`) list; scores screen + clear follow `game.selectedLevelKey` · test-logic 13.9 block (key derivation / m01 separation / in-list ranking, LS stub) + test-boot E2E (m02/m03 victory → own list with run score; m01 list untouched after earlier clear) · gates 30/30 · 303/303 · boot PASS runs=4.
- **2026-08-22 — session resume (post-compact; docs verified, gates re-run green, no code changes):** baseline confirmed 30/30 · 296/296 · PASS boot-sim runs=4 (matches Status); plan: **13.9 per-level high scores** → 13.10 flavor → 13.11 audio within budget → 13.12/13.13 close-out (Phase 13 complete).
- **2026-08-22 — 13.8 mobile view zoom + Pause-menu Settings (code + gates):** camera-view factor — `CFG.zoom` (`touch` 0.80 / `full` 1.0 / LS key `qsurv.zoom.v1`); `meta.js` pure `defaultZoom`/`loadZoom`/`saveZoom` (invalid/absent → device default); `game.js` loads zoom in constructor (touch via `document.body.classList.contains('touch')`, D13) + `resize`→`_applyZoom` (camera view + half-res lighting = canvas/zoom; vignette stays 1× canvas) + public `setZoom` (persists); `render` wraps the frame in a view-space translate so the enlarged view is centered on the canvas (no-op at zoom 1; vignette + minimap outside it = 1×); `index.html` drops `#btn-mute` → new Pause-menu **Settings** rows `#set-zoom`/`#set-mute` (≥72 px) · `hud.js` owns both rows (zoom → `game.setZoom`; mute → `input.queueMute`, M key + `qsurv.mute` LS unchanged, D8; `syncZoom`/`syncMute` on 'mute' bus) · `css` `.set-row` styles + dead `#btn-mute` rules removed · README Play controls synced · test-logic 13.8 block (defaults 0.80/1.0, touch/desktop fallback, LS round-trip both directions, invalid→default) + test-boot E2E (desktop default 1.0 → view=canvas; toggle → 0.80 → view 1600×1000 + persist; toggle back → 1.0; Settings mute row → `qsurv.mute` on/off + `.muted` class) · gates 30/30 · 296/296 · boot PASS runs=4.
- **2026-08-22 — 13.7 level select (code + gates):** `ui/screens.js` `renderLevels` — 3 radio cards on the main menu (palette chip + name + locked-req text + X/3 pips from `game.wins`/`isUnlocked`); locked tap → `denied` blip + `lv-shake` (selection unchanged); unlocked select → `game.selectedLevelKey` + `saveSelectedLevel` (LS `qsurv.level.v1`) + `game.previewLevel` (backdrop regenerates) · `meta.js` pure helpers `loadSelectedLevel`/`saveSelectedLevel` · `game.js` constructor clamps selection to unlocked + seeds `level`/`levelKey` (so backdrop + no-arg `startRun` honor the persisted selection) · `sfx.js` `denied` recipe (descending double blip) · `.screen` gains safe-area padding (mobile parity) · test-logic 13.7 block (defaults/round-trip/invalid-key) + test-boot E2E (3 cards, m01 sel + m02/m03 locked at 0 wins, locked m03 denied blip+shake, simulate 3× m01 wins → m02 unlocked → select m02 → preview+persist, clean reset so run-1..4 flow unaffected) · gates 30/30 · 290/290 · boot PASS runs=4.
- **2026-08-21 — 13.6 unlock persistence (code + gates):** `meta.js` pure helpers `defaultWins`/`loadWins`/`saveWins`/`recordWin`/`isUnlocked` (LS key `qsurv.wins.v1` = CFG.meta.winsKey; meta.js LS pattern) · `game.js` loads wins in constructor + records/saves on victory-only in `_gameOver` (deaths don't count) · test-logic 13.6 block (defaults / increment / 3×-each + chained unlock rules / LS round-trip via stubbed localStorage) · gates 30/30 · 286/286 · boot PASS runs=4.
- **2026-08-21 — 13.5 Map 03 roster (code + gates):** `buildCharacters('m03')` Drowned City slot skins — crab (small chaser), goldfish (bat), mer-people (goblin; merman + mermaid), stingray (wolf), electric eel (cultist; jagged zap sprite, NOT orb), orca (brute) + Great White Shark boss (same charge skeleton; belly-flash frame-1 windup pose) · `shark` in CFG.enemies (wraith base stats, boss) · m03 `weights` (m01 role curve) + boss `shark`@240 `name:'THE GREAT WHITE'` in levels.js (was null) · ×1.56 stats via existing `enemies.diff` (no new code) · test-logic 13.5 block (base/×1.56 stats, banner name, weights, pickType early/late) + test-boot M03 run 4 (skin asserts, spawned ×1.56, 'THE GREAT WHITE AWAKENS', victory; keep-alive via run 3/4 steer branch) · gates 30/30 · 279/279 · boot PASS runs=4.
- **2026-08-21 — 13.4 Map 03 "The Drowned City" art (code + gates):** layoutM03 5145×3920 (kelp/coral/anemone/broken-columns/dome+spire/trident/shipwreck/vent sprites; m03 sand tiles own rng streams `mulberry32(5250+i*131)`) · m03 sky = surface-glow "moon" + 7 god-ray shafts (new optional `sky.godrays`, screen-space pass under lighting) + surface-shimmer near-ridge, empty far-ridge · rising-bubble foreground (D49) via Snow kind `'bubble'` + `CFG.perf.bubbleColor` · 3 open-water fish schools (koi t-driven orbits, 1×1 empty-img seam in world.js) · lights = 8 anemones + city dome · palette skyTop `#04283e`/skyHorizon `#0a4a66` · golden 319 trees/370 decor/370 colliders/1009 decals seed 20260823 · test-boot m03 backdrop block (decor img + size assertion) · m01/m02 goldens hold · gates 30/30 · 269/269 · boot PASS (runs=3).
- **2026-08-21 — 13.3 Map 02 roster (code + gates):** `buildCharacters('m02')` Higan skins tanuki/hō-ōi/shikome/kitsune/oni/miko (same footprints) + segmented-tail Ryū in default set · `enemies.diff` ×1.25 hp/dmg at spawn (A4) · m02 `weights` (m01 role curve) + boss `ryu`@240 `name:'RYŪ'` + name-driven AWAKENS/FALLS banners (m01 text byte-identical) · test-logic 13.3 block (stat scaling / banner names / weights) · test-boot real M02 run 3 (keep-alive + banner capture: skins, ×1.25 stats, Ryū spawn + 'RYŪ AWAKENS', victory) · gates 30/30 · 255/255 · boot PASS (m01 golden 276/335/328 intact).
- **2026-08-21 — 13.2 Map 02 "Higan" art (code + gates):** layoutM02 in levels.js (torii-ring landmark + shrine village + pagoda + koi pond + bamboo + lanterns + stone paths + m02 lights) · m02 art pack in terrain.js (cherry/cherryBig/bamboo/torii/shrine/pagoda/lantern/stone-slab/koi+koiPond + m02 tiles/decals on own rng streams) · m02 sky in sky.js (Fuji silhouette + stratus + full moon) · per-level sky cache + palette-driven sky/ground/lighting base (world.js/lighting.js/game.js) · kind-aware foreground petals (particles.js + CFG.perf.petalColor) · tests: test-logic m02 block (golden 274/324/331 seed 20260822) + test-boot M02 menu-backdrop E2E + m01 snow regression · gates 30/30 · 243/243 · boot PASS (m01 golden 276/335/328 intact).

- **2026-08-21 — 13.1 level framework (code + gates):** new `js/world/levels.js` (pure defs: size/diff/menuSeed/foreground/audio/unlock/palette + m01 layout moved verbatim) · `generateWorld(seed, levelKey)` dispatch · per-level spawner weights/boss/diff scaling (D55) · per-level bounds seam in `World.generate` · per-level minimap base + menu-backdrop level hook (last-level preview) · gates 30/30 · 231/231 · boot PASS.
- **2026-08-21 — Phase 14 O1 resolved + placement approved (docs only, no code):** counter source = **A** — live projected award `floor(score/400)` (+25 on victory), no economy change; queue slot approved (after Phase 12, before 10.9 LAST). D54 amended + 14.1 + README + resume notes synced.
- **2026-08-21 — Phase 14 spec'd (docs only, no code):** in-run real-time Soulshards HUD counter (run-only display, single unified view) + unified co-op earnings (all active players in a run receive the same amount; each local LS accrues it in full) → checklist 14.1–14.3 + D54 (revises D53 earnings clause) + PLAN §3.4/§3.8 + README; O1 open: counter source = live projected award vs collectible soul gems.
- **2026-08-21 — 10.7 softlock + 10.8 gem SFX (code + gates):** 10.7 offers computed first at both LEVELUP entry sites — empty pool grants levels silently, stays PLAYING (boot-sim E2E rides the bench all-max roster: multi-queue entry + mid-queue exhaustion, auto-pick gated per `synActive` pattern); 10.8 `gem` sfx recipe (bright blip, GAPS 0.05) + `gem` bus emit at the XP-consume site (boot-sim forced collect fires the event); gates 29/29 · 219/219 · boot PASS.
- **2026-08-21 — session resume (post-compact, docs verified, no code, no other changes):** Phase 13 doc state confirmed in sync (PLAN §3.9 + §3.8/§3.7/§3.1/§5, checklist 13.1–13.13 unchecked, D44–53, README); gates at last-run baselines 29/29 · 219/219 · boot PASS; awaiting user start for 10.7.
- **2026-08-21 — Phase 11 clarification (docs only, no code):** meta progression (Soulshards + Upgrades) is **player-specific** — never carried over to/shared with/applied to other players during co-op; each client applies its own local meta to its own character; shard earnings accrue locally only; host sim gets each player's meta-derived stat profile at join (handshake, sim input only) → D53 + PLAN §3.8 co-op rules + 11.2.
- **2026-08-21 — Phase 13 decisions A1–A8 (docs only, no code):** Map 02 skins approved + M03 small chaser = **crab** (A1/A2); M03 scale → **1.5× area ≈ 5145×3920** (A3); per-level difficulty chained **×1.25** (A4); A5 flavor approved (per-level high scores/audio/gem tints/backdrop preview/dragon tail/koi + fish schools/unlock banner/denied blip) — first-clear bonus, shard multipliers, victory lines, pufferfish, keyboard select, boss intro **deferred**; zoom default **0.80** + new **Pause-menu Settings** (zoom + mute; mute **replaces** HUD `#btn-mute`) (A6); order **10.7/10.8 → 13 → 11 → 12 → 10.9 (last) → 2.9** (A7); map names tentatively approved (A8). Decisions 44–52 revised/added; PLAN §3.8/§3.9 + README synced.
- **2026-08-21 — Phase 13 spec'd (docs only, no code):** 3-map expansion — Map 02 "Higan" (traditional Japan, 4200×3200: sakura petals map-only, Oni brute, Ryū dragon boss) + Map 03 "The Drowned City" (underwater, 8400×6400: goldfish/merfolk/stingray/eel/orca + Great White Shark boss, small rising bubbles foreground) — cumulative-win unlocks (3×, victory-only), main-menu level select with locked-visible-progress, mobile view zoom ≈25% → PLAN §3.9 + checklist 13.1–13.12 + decisions 44–49; candidate embellishments pending user decision.
- **2026-08-21 — 10.6 music remake:** 66 BPM eldritch track in D-dim7 {D, F, Ab, B} (sub-root crawl + detuned-saw drone layer + sparse heartbeat + one lone beat-pair color tone per bar over a dark delay + run-gated filtered-noise texture; wind/howl ambience unchanged, all tuning in `CFG.audio`); seamless-seam proof in test-logic: fake-`AudioContext` clock pumped 12 loops + margin — exact BAR lattice (no gap/double/drift) + exact per-voice counts (213→219 checks; old MUSIC data checks replaced).
- **2026-08-21 — 10.5 spawn distance:** `spawnPoint` → thin band just outside the view edge (4 sides, `spawnPad` 15 px / 8 retries / clamped fallback `spawnFallback` 30 px — all config); root-caused the boot-sim go-menu flake: input edge (dash/card) set on the last PLAYING frame survived `_gameOver`/`toMenu` (both call `clearTransient`, which only cleared keys+sticks) → `_menuUpdate` auto-started a run; fix: `clearTransient` now clears all four edge channels (also correct on blur/tab-switch); harness: run-1 keep-alive iframes (mirrors run-2) + run-2 `pumpUntil` victory (clock freezes in LEVELUP — fixed pumps break); boot-sim 17/17 consecutive PASS.
- **2026-08-21 — Phase 11/12 spec refinement 4 (docs only, no code):** base `maxWeapons` **4→5** (solo runs now get 5 standard weapon slots) → co-op cap table **1P=5, 2P=4, 3P=3, 4P=2**, so a 4P player can still own 1 weapon pair → paired synergy achievable (synergies never count toward the cap); decision 42 revised; 11.5 updated incl. test-logic expectations 4→5.
- **2026-08-21 — Phase 11/12 spec refinement 3 (docs only, no code):** synergies → **5 levels** like standard weapons (supersedes D19 single-level; stay in pool until own max; per-level effect scaling) + pair-specific gating confirmed (offer pool opens the moment the synergy's own 2 sources max — NOT all weapons) + co-op equip cap **4−(N−1)** per player with synergies excluded (base `maxWeapons=4` verified in code) + slow/shock stacks **5 s per-stack TTL** (playtest-tunable); decisions 40–43; PLAN §3.4/§3.8 + README updated.
- **2026-08-21 — Phase 12 spec'd (docs only, no code):** Pyre Lance tune (faster max-velocity ramp, emission origin feet→chest/abdomen, stream +25%) + 3 new weapons (Bow & Arrow, Snowball Launcher — slow stacks→freeze, Ring of Chain Lightning — shock stacks→stun+chain burst) + 4 new synergies (Flaming Arrows, Heart-Piercer, Blue Flame, Storm Volley w/ proposed chain/shock extras) → Phase 12 checklist 12.1–12.8; roster 7→10 weapons, 5→9 synergies; decisions 37–39; README weapon list + roadmap updated.
- **2026-08-21 — Phase 11 spec refinement 2 (docs only, no code):** Q7 resolved (N players = N Wraiths; Wraith stats get the same +33%/player ramp) + 11.13 research protocol made binding (verify real-world date BEFORE web research — standalone, non-batchable, non-negotiable; sources-cited, evidence-based, data/outcomes-driven best practices for self-hosted, fully game-state-synced web-game multiplayer servers; 100% cost-free only); decisions 35 updated + 36 added.
- **2026-08-21 — Phase 11 spec refinement (docs only, no code):** Q1–Q6 answered (A1: 33%/player on HP/damage/spawn-count; A2: leash = shared expanded vision radius; A3: picks player-scoped, passives NOT locked; A4: LAN + web play — Pages can't host WS → 11.13 research; A5: join-order corners; A6: Wand/Garlic/Aegis Blades/Pyre Lance starters) + new boss-clone request → **Q7** (one-per-player vs 1+N); checklist renumbered (11.10 boss, 11.11 solo-invariance, 11.12 gates, 11.13 web play); decisions 27 updated + 30 revised + 31–35 added.
- **2026-08-21 — Phase 11 multiplayer scope doc'd (docs only, no code):** 6 user requests (real-time multiplayer, difficulty scaling per added player, player leash, per-run weapon exclusivity, 4 characters, co-op HUD) → Phase 11 checklist 11.1–11.11 + decisions 27–30; PLAN.md §1/§2/§3.1/§3.8/§5 updated (non-goals now permit the LAN WebSocket room); open questions Q1–Q6 (ramp magnitude, leash radius, passive lock, LAN scope, corner assignment, starter weapons) await user answers; README roadmap line.
- **2026-08-21 — Doc review + state verification (docs only, no code changes):** all three gates re-run green (29/29 · 207/207 · PASS boot-sim); git state documented in Status (Phase 9 + 10.1–10.4 uncommitted since `667364f`); pending items re-verified against code; `isolate-*-v8.log` (2.6 MB V8 crash dump from a crashed node process) catalogued as a safe-to-delete artifact.
- **2026-08-21 — 10.4 multi-hit perf:** permanent worst-case bench in test-boot (`[10.4-bench]`; `DEBUG_BOOT=1` → `[10.4-sec]` section timers, ctx draw-op counters) → root causes: no viewport culling + death-frame list/grid churn (NOT the per-hit floater/`_dot`/flame-scan suspects); fixes: cull rect (`world.cullPad`) through pickups/shadows/Y-sort/particles + `_ai` dead-skip + in-place compaction. Before/after: `r:drawOne` 75.9→53.8 calls/render frame (−29%), `u:enemies` 0.069→0.057 ms/frame (−17%).
- **2026-08-21 — 10.3 mute speaker icon:** inline SVG (speaker + 3 waves; slash + `--danger` off-state, `aria-label` Mute/Unmute); also fixed a latent seed-dependent touch-stick flake (no-op `collidersNear` for the 1 s steer window — the dash test pushes the player into a seed-placed hut).
- **2026-08-21 — 10.2 exact-effect card descs:** pure `cardEffectText(kind,key,level)` (player.js); mandatory `p.card-effect` line first in every card + `p.meta-effect` on Upgrades rows; phoenix desc fixed ('…heal 10 HP every 8 kills.'); +90 logic checks (117→207).
- **2026-08-21 — 10.1 dash indicator (all envs):** `#btn-dash-hud` in `#hud-buttons` (desktop had NO indicator); same change-detected `--cd` as `#btn-dash`; clickable (`queueDash`); boot-sim asserts both `--cd` match mid-dash.
- **2026-08-21 — Phase 9 COMPLETE:** boot-sim extended (meta/Upgrades buy → maxHp 120, all 7 weapons, burn DoT, dash i-frames start+mid, synergy blight draw E2E); README updated; gates 29/29 · 117/117 · PASS.
- **2026-08-20 — Phase 10 scope doc'd (9 user requests, docs only) + Phase 9 re-verified against code (docs only; 2 resume-note errors fixed: synergy test pool, auto-pick vs synergy E2E).**
- **2026-08-20 — Phase 9 implemented:** config scalars + 3 weapon tables + `CFG.synergies`/`CFG.meta`; new `core/meta.js`; player/combat/enemies (synergy namespace, 3 weapons, DoT pipeline, 4-arg `cardOffers`).
- **2026-08-20 — Published:** public repo + Pages live (user asked private; a private repo's Pages is login-walled → public won the "share it" goal; flip-back in Status).
- **2026-08-20 — Playtest fix: `ellipse()` 6-arg boot crash** — 21 call sites gained the missing `rotation` arg; ctx stub hardened (Decision 17), negative-tested.
- **2026-08-20 — Phase 7:** boot-sim coverage (key card pick, all weapons + wand-off window, heart heal, scores/quit, touch stick + dash) — one-shots self-verify (Decision 16).
- **2026-08-20 — Playtest fix: "Start Game does nothing"** — terrain.js boot crash (1 ReferenceError + 6 TDZ self-shadow renames); new gate `test-boot.mjs` (Decision 15); spawner batch floor 1→2 (Decision 18).
- **2026-08-20 — Phase 6 COMPLETE:** `main.js` bootstrap + `tools/serve.mjs`; in-process HTTP driver caught 2 real serve bugs (traversal check, path join); serve listens unconditionally (Decision 12).
- **2026-08-20 — Phase 5 COMPLETE:** `sfx.js` (lazy ctx on gesture, bus graph, 9 recipes) + `music.js` (92 BPM D-minor loop + wind + howls — track to be replaced by 10.6); gesture hook in `startRun` (Decision 11).
- **2026-08-20 — Phase 4 COMPLETE:** `hud.js` + `screens.js` (state-poll screens, Decision 7; mute ownership Decision 8); CSS touch targets ≥72 px.
- **2026-08-20 — Phase 3 COMPLETE:** entities (player/enemies/spawner/combat/pickups/particles) + camera + lighting + game state machine (23 modules, 83 checks).
- **2026-08-20 — Phase 2 COMPLETE:** art/world/minimap modules (13 modules import-clean).
- **2026-08-20 — Full codebase review (docs only):** contracts verified; README steer/dash wording fixed; PLAN dash speed 680→690 (config is the tuning truth).

## Environment & Known Caveats

See `docs/ENV.md` (moved out of this file 2026-09-05 per Format contract §6 — read it before running anything non-gate: node path, two-working-copies flag, push caveats, artifact list, unverified-browser note).

## How to Resume a Session

1. Read `AGENTS.md` → this file (Status → Master Checklist active phases → **Resume Notes**) → `docs/ENV.md`. Architecture: `docs/PLAN.md` §3–§4; decisions + pitfalls: `docs/DECISIONS.md`; user's own words: `docs/USER-INPUT-LOG.md`. The code is the API record — read the module before changing it.
2. Work **NEXT** from Resume Notes (currently: finish 12.7 pool review → 12.8 boot E2Es; then Phase 20 → 15 → 17 → 18 → 19 → 14 → 21 → 24/25 dedicated sessions → 2.9). New scope → Master Checklist first.
3. Validate with **all three gates** before any tick: `"/c/Program Files/nodejs/node.exe" tools/check.mjs` (33 modules) · `tools/test-logic.mjs` (**656**) · `tools/test-boot.mjs` (`PASS boot-sim runs=4`).
4. On completion: tick + date + ≤2-line note in the checklist, ≤10-line Session Log entry, rewrite Resume Notes (≤25 lines), update Status — *before* declaring done (Format contract §1–§4).
