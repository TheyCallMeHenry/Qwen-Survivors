# QWEN SURVIVORS — Progress Tracker

> **RULE:** a task/step/phase is NOT complete until its box is ticked here with a date + one-line note — update *before* declaring done. Plan: `docs/PLAN.md` · Session rules: `AGENTS.md`.
> **Keep this file lean — it is loaded every session:** Status + Master Checklist + *active* resume notes only. One line per session in the log. No API dumps, no line refs, no kept-for-reference step blocks — the code is the implementation record.

## Status — 2026-08-22

- **Active: Phase 11 — multiplayer/co-op.** **11.1 DONE 2026-08-22** — zero-dep WS upgrade + frame codec in `tools/serve.mjs` (import-safe; entry guard; port 47893 only), pure protocol + room machine + D53 profile + `coopScale` in `js/net/coop.js`, `CFG.coop {maxPlayers:4, perPlayer:0.33}`; test-boot E2E = real server on ephemeral loopback port + native WS clients (join/seat/full/leave+roster/host-leave close/re-open). **11.2 DONE 2026-08-22** — host-authoritative sync: host sim owns all players (remotes from D53 profiles), per-step `state` broadcast via relay (`js/net/sync.js` SNAP_V=1), shared seed via runstart, client no-sim + arrival-time interp, solo invariance. **11.3 DONE 2026-08-22** — difficulty scaling in host sim: `enemies.coopS = coopScale(players)` (set at run start + mid-run roster), spawn curves take `s` (aliveCap/batch ×s, interval ÷s), spawn HP/dmg ×coopS (boss included, Q7); solo `s=1` default = bit-identical. Room handshake carries the level key. **11.4 DONE 2026-08-22** — player leash: `CFG.coop.leashR` 700 (expanded from solo 510), pure `leashClamp` POCS in `js/net/coop.js`, host `_coopLeash()` per step (live players, world re-clamp; solo no-op), co-op vision radius = leashR in `_lights`. **11.5 DONE 2026-08-22** — level-up scoping + weapon exclusivity: base `maxWeapons` 4→5 + pure `weaponCap` (1P=5/2P=4/3P=3/4P=2); per-player `cardOffers(cap, exclude)` + host `weaponOwner` first-picker map (persists through leave); solo invariance; test-boot 11.5 E2E (live 3P: ownership, remote exclusion, per-picker picks, cap). **11.6.0/11.6.1 DONE 2026-08-22** — select-UI research (2b full-screen confirmed, 2 sources cited) + `CFG.characters` roster (per-char **O stat values USER-APPROVED 2026-08-22**, revisions: mage 60hp/275sp · ranger 110hp/320sp · warden 230sp) + `art/characters.js` builders (warden/ranger/swash/ghost; mage = original playerFrame) + per-char stats in `player.js` + `buildRoster`. **11.6.2 DONE 2026-08-22** — full-screen select (2b, closes on confirm, D61) in `screens.js` (menu **Character** button → #screen-select; per-card 2× roster art + D57 stats (HP/dmg/speed) + start-weapon line + desc; **D58 Soulshard shop**: locked cards show `Locked · cost ◆`, one tap buys + selects when affordable, wallet line, denied blip + shake; solo — co-op greyed-out `taken` hook for 11.6.4); pure `loadChars/saveChars/isCharUnlocked/buyChar` + `loadSelectedChar/saveSelectedChar` in `meta.js` (`qsurv.chars.v1` / `qsurv.character.v1`); `game.setCharacter`/`buyCharacter` (persisted; roster art/flash/shadow swap; `Player.reset`/startRun apply per-char stats + starting weapon; solo default = starter, D58); test-logic +16 + test-boot E2E (open/lock/buy+select/reject/persist/confirm/close/restore). **11.6.3 DONE 2026-08-22** — ghost fallback (D59/D62): all-starter lobby → EVERY seat ghosts (faceless sheet, per-seat Pac-Man tint, baseline 100/×1.0/265, NO starting weapon) with 2 UNIQUE starting-weapon offers per seat, never duplicated across players; pure helpers in `js/net/coop.js` (`sanitizeChars`/`allStarterLobby`/`ghostColor`/`allocateGhostOffers` seeded 2/2/2/1 at 4P/`joinProfile` = D53 profile + chars); host `_applyGhostFlow()` (run start + mid-run roster: ghost sheet + stats recompute + weapons cleared + per-seat disjoint pair; remotes auto-pick `offers[i][0]`, host opens on its entry 2-card LEVELUP; `weaponOwner` registered; mid-run joiner ghosts in) + client self-detect from `netRoster`; test-logic +30 + test-boot E2E (a) 3P all-starter + mid-run 4th joiner (4 distinct owners) + (b) mixed lobby NO ghosting (fresh room); gates 33/33 · 453/453 · boot PASS runs=4. **11.6.4 DONE 2026-08-22** — co-op character sync/E2E (D53/D56/D57/D59): D53 join profile carries the selected char (`joinProfile(meta, chars, sel)`); pure `selChar`/`assignChars`/`resolveChars` in `coop.js` (unique per seat, seat-order priority; all-starter lobby → every seat ghost, D59 wins); host `_applyAssign` per-seat spawn + mid-run re-assignment (taken pick → next unlocked char, else ghost with fresh seeded deal); per-seat ghost tint via `buildGhost(color)`; client renders the host-assigned char (LS selection untouched); taken-char select UI greyed + denied (D56); test-logic +11 (464/464) + test-boot E2E (3P distinct chars with per-seat stats; mid-run late-joiner reassigned mage→swash, client renders it; greyed-select subtest) + 11.6.3 (b) re-baselined to D56 per-seat ghosting; gates 33/33 · 464/464 · boot PASS runs=4. **11.6b DONE 2026-08-22** — exclusivity closure (D29 respec): `cardOffers` synergy branch honors `exclude` (one Set serves weapon+synergy keys — namespaces disjoint); first-pick registration extended to synergies at both pick sites; **starting-weapon pre-ownership** seeded in `_applyChar` (remotes + mid-run joiners) + `_coopStartRun` (host's own starter) → starting weapons excluded from OTHERS' offers, owner keeps own upgrades, solo map never populated → invariance; test-logic +5 (469/469) + test-boot 11.6b E2E in the live 3P run (pre-owned wand/garlic/blades, per-seat exclusion, forced host blight pick → ownership, pl1 pool = phoenix only with blight sources maxed); gates 33/33 · 469/469 · boot PASS runs=4 ×2; **11.7 DONE 2026-08-22** — co-op HUD corners: `#hud-left` is ALWAYS the SEAT-0 panel (solo invariance); 3 generated corner panels (seats 1/2/3 = TR/BL/BR; `#hud-seat-1/2/3`) — char name + 2× roster/ghost face (per-seat tint cache) + HP bar/label + low pulse + LV badge + XP fill + dash `--cd` ring, display-only (input buttons follow the LOCAL player); visible count = player count, join order (A5); `#hud.coop` CSS toggle + minimap → bottom-center in co-op; test-boot E2E (solo: no coop class + TL = local; 3P: TL = host data + TR/BL named/tracked + BR off; mid-run 4th joiner → BR Swashbuckler; leave → roster 3 + BR hidden); gates 33/33 · 469/469 · boot PASS runs=4. **11.8 DONE 2026-08-22** — per-char UI theming (D62 channel): `CFG.characters[*].accent` (mage #9fe8ff · warden #ffb454 · ranger #a4ffc9 · swash #e8b45a; ghost null → per-seat Pac-Man tint) + pure `charAccent(key,seat)` in coop.js + hud.js `applyTheme` sets `--char`/`--char-soft`/`--char-line`/`--char-inset`/`--char-grad` on the local roots (`#hud` + `#touch-ui` — dash controls follow the local player) and each seat-panel root, consumed by main.css on the HP fill + dash icon/border/ring with legacy fallbacks; **fixed 11.7 latent defect: seat-panel HP fills had NO background (was id-only `#hp-fill`) — `.hp-bar .bar-fill` now carries the themed fill**; test-logic +7 (476/476) + test-boot E2E (ghost run: local + seat-1/2 panels = per-seat tints Pinky/Inky; 3P: Mage local + Warden/Ranger accents, 4th joiner → Swash accent). **16.1 DONE 2026-08-22** — m01 falling-snow regression (D70: reproduce/quantify FIRST — test-boot `probeSnow` path-op deltas quantified the repro BEFORE the fix: m01 snow pass drew `arc=0 ellipse=0 fill=90` = 90 fills on EMPTY paths, zero visible flakes) → root cause via git forensics: **13.4 (`a8c332b`) replaced the snow `else ctx.arc(...)` with `else if (bubble) {...}`, leaving `beginPath()`+`fill()` on an empty path — a silent no-op in canvas AND in the stub (why every gate stayed green since 13.4)** → fix = restored the snow arc exactly as v1.0.0 (`03eeac2`) · non-regression = `probeSnow` at 6 sites (run-1 m01 in-game + m01/m02/m03 menu backdrops + m02/m03 run starts: snow = 90 arc/90 fill · petal = 90 ellipse/90 fill · bubble = 180 arc/90 stroke/90 fill); 10.4 bench baseline shifts (the 90-flake m01 snow pass now really draws — 10.9 measures against post-16.1 numbers). **16.2 DONE 2026-08-22** — projectile spawn origin feet→mid-torso: `CFG.combat.spawnOriginFrac` 0.55 + `Player._spawnY()` — the player's `(x,y)` IS the feet (sprite bottom-anchored) and all 5 fire sites used it for aim + spawn; now wand/axe/pistols/bombs/flame aim AND spawn from mid-torso (bomb lob dist + flame `_flameAng` too), axe return + orbiter center (dmg/knockback/draw) follow; garlic stays feet-centered (ground pulse, documented); co-op snapshot-neutral (NO projectile key exists in the sync protocol — host-sim-only); **latent-fix: blade orbiters + garlic pulse ring were NEVER drawn** (`combat.draw` read never-assigned `this.player`/`this.orbitT`/`this.garlicT` — NaN radius → browser `arc()` throw) → `draw(ctx, t, players)` per-live-player on sim clocks + client cosmetic clock in `_clientUpdate` (remote blades: no angle in snapshot → not drawn); test-logic +4 (480/480) + test-boot E2E (5-weapon exact mid-torso origins + blade orbit discrimination + combat.draw blade/garlic) · gates 33/33 · 480/480 · boot PASS runs=4; next **16.3 Pyre Lance flame**. **11.13 web-play research DONE 2026-08-22** (date-verified FIRST per D36; 8 sources cited) — findings: `docs/RESEARCH_FINDINGS.md` (read on demand, NOT in this doc); transport **DECIDED 2026-08-22 (D64): Tailscale on the DS124 NAS** (Option A2 — Tailscale installed + connected per user); impl after 11.12 per checklist order. Phase 13 (13.1–13.13) COMPLETE; Phase 12 spec'd (after 11). Order per A7: **11 → 12 → 14 → 10.9 (LAST, D25) → 2.9**. Deferred (NOT building): first-clear bonus, shard multipliers, per-level victory line, pufferfish, keyboard 1/2/3 select, boss intro moment.
- **Gates (all three green before any tick):** `node tools/check.mjs` **33/33** · `node tools/test-logic.mjs` **476/476** · `node tools/test-boot.mjs` **`PASS boot-sim` (runs=4 + 11.1/11.2 E2E + 11.3 scaling + 11.4 leash + 11.5 exclusivity + 11.6.2 select UI + 11.6.3 ghost E2E (a)+(b) + 11.6.4 char sync E2E + 11.6b pre-ownership/synergy ownership E2E + 11.7 co-op HUD corner E2E + 11.8 theming (ghost per-seat tints + per-char accents, local + seat roots) + 16.1 foreground path probe (m01 snow 90 arc · m02 petal 90 ellipse · m03 bubble 180 arc — 6 sites) + 16.2 spawn-origin E2E (5 weapons exact mid-torso origins + blade orbit discrimination + combat.draw blade/garlic latent-fix))**.
- **Overall:** Phases 0–9 done (published) + 10.1–10.8 + 13.1–13.13 done (Phase 13 COMPLETE) + **11.1–11.8 done** (11.6 + exclusivity closure + co-op HUD corners + per-char theming complete); next **Phase 16 playtest defects (2026-08-22 user report; 16.1 reproduce FIRST per D70)** → 11.9–11.12 → **11.13 web transport impl (NAS-side, D64)** → **Phase 12 weapons (12.1 REVISED → superseded by 16.3)** → **Phase 20 passive-start rule** → **Phase 15 offer-coverage defect fix** → **Phase 17 run durations & boss schedule** → **Phase 18 level-up actions** → **Phase 19 HUD equip/fuel** → **Phase 14** → **Phase 21 extended roster** → **10.9 optimization (LAST, D25)** → **2.9 user browser sign-off** (re-verify post-co-op) → project DONE.
- **Published:** public repo `TheyCallMeHenry/Qwen-Survivors` (initial commit `03eeac2` + `667364f` Phase 8 docs) · Pages **https://theycallmehenry.github.io/Qwen-Survivors/** (the share link; repo must stay public — flip back: `gh repo edit --visibility private`).
- **Git:** working branch `overnight-2026-08-22` (from `main` @ `4b5d0c4`; **main frozen until user rejoins** — overnight mandate). Last commit = `1dc1cae` (16.1 m01 snow regression fix; prior `34ae9a9` 11.8 co-op theming, `691ce0e` overnight baseline). `human-user-notes_AI-agent-ignore/` gitignored (public repo). Pages auto-deploys from **main** (overnight branch does not touch Pages until the user merges). Cadence: stage/commit/push on the branch after each task + docs update (overnight mandate).
- **Server:** detached node on **47893** (log `server.log`; dies on reboot). Check before starting another: `curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:47893/` → 200 = already up.

## Master Checklist

### Phase 0 — Scaffold & Docs (all 2026-08-20)
- [x] 0.1 Directory tree (PLAN §3.1)
- [x] 0.2 `package.json` (type:module, check/test/serve scripts)
- [x] 0.3 `docs/PLAN.md`
- [x] 0.4 `docs/PROGRESS.md`
- [x] 0.5 `README.md`
- [x] 0.6 `AGENTS.md`

### Phase 1 — Core engine (all 2026-08-20)
- [x] 1.1 `index.html` (canvas, HUD, touch UI, 7 screens)
- [x] 1.2 `css/main.css` (safe areas, touch sizing)
- [x] 1.3 `js/config.js` (all tuning)
- [x] 1.4 `utils/math.js` + `utils/bus.js`
- [x] 1.5 `core/loop.js` (fixed 60 Hz, timescale, hit-stop)
- [x] 1.6 `core/input.js` (keys, drag/touch sticks, dash/pause/mute, card keys)
- [x] 1.7 `check.mjs` green on Phase 1 files

### Phase 2 — Procedural art & world (all 2026-08-20)
- [x] 2.1 `art/base.js` (canvas/sprite helpers)
- [x] 2.2 `art/sky.js` (clouds/stars/moon/ridges)
- [x] 2.3 `art/terrain.js` (tiles, decals, props, mountains, lakes, vignette)
- [x] 2.4 `art/characters.js` (player + 7 enemies, pre-rendered frames)
- [x] 2.5 `art/items.js` (world sprites + card icons)
- [x] 2.6 `world/generate.js` (pure seeded layout)
- [x] 2.7 `world/world.js` (background pipeline + light data)
- [x] 2.8 `world/minimap.js` (264×202 base + live layer)
- [ ] 2.9 Browser sign-off (art/world/lighting/minimap visuals) — **after Phase 10**

### Phase 3 — Entities & gameplay (all 2026-08-20)
- [x] 3.1 `entities/player.js` (movement, dash i-frames, weapons, cards, meta stats)
- [x] 3.2 `entities/enemies.js` (7 types, AI, wraith boss, burn/blight status)
- [x] 3.3 `entities/spawner.js` (pure curves)
- [x] 3.4 `entities/combat.js` (bolts/axes/orbiters/garlic + bullets/bombs/flames, DoT)
- [x] 3.5 `entities/pickups.js` (gems/hearts, magnet)
- [x] 3.6 `entities/particles.js` (pooled + snow)
- [x] 3.7 `systems/camera.js`
- [x] 3.8 `systems/lighting.js` (half-res darkness + glow)
- [x] 3.9 `core/game.js` (6-state machine, scoring, runs)
- [x] 3.10 `test-logic.mjs` green

### Phase 4 — UI/HUD (all 2026-08-20)
- [x] 4.1 `ui/hud.js` (bars, text-on-change, dash ring, mute)
- [x] 4.2 `ui/screens.js` (6 screens + Upgrades, cards, scores)
- [x] 4.3 CSS final (touch ≥72 px, safe areas, pulse)
- [x] 4.4 `index.html` wiring verified

### Phase 5 — Audio (all 2026-08-20)
- [x] 5.1 `audio/sfx.js` (lazy ctx on gesture, bus graph, recipes, mute)
- [x] 5.2 `audio/music.js` (loop + wind + howls) — track replaced by 10.6
- [x] 5.3 Gesture unlock + mute persistence verified

### Phase 6 — Integration & launch (all 2026-08-20)
- [x] 6.1 `js/main.js` bootstrap (Node-safe top level)
- [x] 6.2 `tools/serve.mjs` (47893, 0.0.0.0, MIME map, traversal-safe)
- [x] 6.3 `check.mjs` 28/28 clean
- [x] 6.4 `test-logic.mjs` 95 green
- [x] 6.5 HTTP 200/MIME/404 verified
- [x] 6.6 Mobile audit pass
- [x] 6.7 Handoff + PLAN sync

### Phase 7 — Pre-sign-off hardening (all 2026-08-20)
- [x] 7.1 boot-sim: card pick via keys 1–3
- [x] 7.2 boot-sim: all weapons exercised + wand-off kill window
- [x] 7.3 boot-sim: heart pickup heal
- [x] 7.4 boot-sim: score persistence + quit flow
- [x] 7.5 boot-sim: touch stick steering + dash button
- [x] 7.6 All three gates green

### Phase 8 — Publish & share (all 2026-08-20)
- [x] 8.1 Public repo + initial commit `03eeac2` (gates green on pushed tree)
- [x] 8.2 GitHub Pages live, HTTP 200 verified

### Phase 9 — 7-request feature pass (all 2026-08-21)
- [x] 9.1 Vision ×3 (`lighting.playerR` 170→510)
- [x] 9.2 Hit knockback → 33% (230→76)
- [x] 9.3a Synergies: 5 fused cards gated on `requires`-all-max; `cardOffers` 4-arg
- [x] 9.3b Meta: `core/meta.js` + Soulshards + Upgrades screen + `buyMeta`
- [x] 9.4 Minimap +25% (CSS-only)
- [x] 9.5 Full i-frames through dash (boot-sim E2E)
- [x] 9.6a Twin pistols (2 bullets at nearest 2)
- [x] 9.6b Sunder bombs (lob → fuse → AOE)
- [x] 9.6c Pyre Lance (flame trail + burn DoT + fuel/recharge)
- [x] 9.7 Gates 29/29 · 117/117 · PASS + README updated

### Phase 10 — Post-playtest polish & optimization (10.1–10.8 done; 10.9 runs LAST per D25/A7)
- [x] 10.1 Dash indicator in ALL environments — 2026-08-21 `#btn-dash-hud`, shared `--cd`, clickable
- [x] 10.2 Exact-effect card descriptions — 2026-08-21 pure `cardEffectText` + mandatory card line + `meta-effect` rows; +90 logic checks
- [x] 10.3 `#btn-mute` speaker icon — 2026-08-21 inline SVG, slash off-state, aria-label
- [x] 10.4 Multi-hit perf — 2026-08-21 viewport culling + in-place compaction (drawOne −29%); bench + section timers permanent in test-boot
- [x] 10.5 Spawn points nearer view edge — 2026-08-21 band placement just outside the view edge (`spawnPad` 15 px / fallback 30 px, config) + input `clearTransient` now clears dash/pause/mute/card edges (stale edge auto-started a run from menu) + harness keep-alive iframes + pump-until-victory
- [x] 10.6 Music remake (eldritch, seamless indefinite loop) — 2026-08-21 66 BPM D-dim7 loop: sub-root crawl + detuned-saw drone + sparse heartbeat + lone beat-pair color tones over dark delay + run-gated noise texture (tuning in `CFG.audio`); seam proven in Node (12-loop fake-clock pump: exact BAR lattice + exact voice counts)
- [x] 10.7 All-cards-owned softlock → auto-skip empty LEVELUP — 2026-08-21 offers computed first at BOTH entry sites (`_startLevelUp` + `pickCard` mid-queue re-draw); empty pool grants levels silently, stays PLAYING; boot-sim E2E: multi-queue entry + mid-queue exhaustion (lone phoenix pick via real pipeline)
- [x] 10.8 Gem pickup SFX — 2026-08-21 `gem` recipe (bright sine blip 1318→1976 Hz, 0.09 s decay, GAPS 0.05) + `gem` bus emit where XP is consumed; boot-sim forced-collect E2E
- [ ] 10.9 Game-wide optimization pass (LAST)

### Phase 11 — Multiplayer & co-op run (spec'd 2026-08-21; Q1–Q6 answered same day; begins after Phase 13 per A7; answer PLAN §3.8 Q7 first)

- [x] 11.1 Co-op transport: zero-dep WebSocket upgrade in `tools/serve.mjs` (port 47893 only, 0.0.0.0, LAN); 1 room = 1 run; 1–4 clients; join/leave/full/closed semantics (req 1) (2026-08-22 — RFC 6455 upgrade + frame codec in serve.mjs (import-safe, entry guard; SHA-1 accept keyed to RFC test vector); pure protocol/room/D53-profile/`coopScale` in `js/net/coop.js`; E2E = real server on ephemeral loopback port + native WS clients: host seat / seats in join order / 5th→full / leave+roster / host-leave close / room re-open)
- [x] 11.2 Host-authoritative sync: host runs the 60 Hz sim; clients send inputs; host broadcasts state per step (positions, HP/XP/level, weapon/card picks + levels, dash, kills/score); shared world seed; client interpolation; **meta NEVER in the sync — player-specific (D53): join-handshake uploads each player's own meta-derived stat profile (sim input only)**; pure protocol in `js/net` (Node-testable) (req 1) (2026-08-22 — `js/net/sync.js` SNAP_V=1 (key tables in CFG order, playerSnap 26 slots r1, enemySnap + E_FLAG_*, pickupSnaps stable pool slots, stateMsg time=r2, unpackState) + `conn.js` CoopConn; host sim owns all players (remotes from D53 profiles, auto card-pick level-ups), per-step state broadcast via relay, shared seed via runstart, client no-sim + arrival-time interp (`CFG.coop.interpLag`); owner kill-credit through combat; test-logic 22-check sync block + test-boot 11.2 E2E: real relay, host+2 clients, client tracks host, KeyD drives host remote, leave → reconcile → run continues)
- [x] 11.3 Difficulty scaling: enemy **HP, damage to players, spawn count/on-screen** (batch size, interval, alive cap) × (1 + 33% × added players) — ×1.33 at 2P, ×1.66 at 3P, ~×2.0 at 4P; **Wraith boss stats (HP/damage) get the same ramp**; config scalars + pure helper (A1 + Q7) (2026-08-22 — `enemies.coopS` set in host `startRun` + `_netRoster` (live ramp on mid-run join/leave), spawner curves take `s` (cap/batch ×s, interval ÷s), `Enemies.spawn` HP/dmg ×coopS (boss included); solo `s=1` default bit-identical; test-logic 11.3 block (7 checks) + test-boot 3P E2E: coopS=1.66 armed + every non-boss maxHp = base×S)
- [x] 11.4 Player leash: all players held within the shared **expanded co-op vision radius** of each other (every player sees every other; config value, expanded from solo 510) — swarming stays consistent, no single player aggro-ing/killing a disproportionate share (A2) (2026-08-22 — `CFG.coop.leashR` 700 (expanded from solo 510); pure `leashClamp` POCS projection (radius-R disks around every other live player) in `js/net/coop.js`; host `_coopLeash()` per `_playingUpdate` step (world-margin re-clamp; solo `players.length===1` no-op); co-op player light radius = leashR in `_lights` (solo keeps 510); test-logic 11.4 block (8 checks) + test-boot E2E: 1.5R teleport → pairwise ≤ leashR convergence poll)
- [x] 11.5 Level-up scoping + weapon exclusivity: first picker owns a weapon + its upgrades for the run; other offers exclude it; **all picks (weapons/passives/synergies) affect only the picker** — passives NOT locked (anyone may pick), synergy availability follows per-player weapon max (A3); **base `maxWeapons` raised 4→5** (solo impact: solo runs get 5 standard weapon slots); **max equipable STANDARD weapons per player = 5 − (N−1)** (1P=5, 2P=4, 3P=3, 4P=2 — at 4P each player can still own 1 weapon pair → paired synergy achievable), **synergy weapons NEVER count toward the cap** (item 3); test-logic maxWeapons expectations 4→5 (2026-08-22 — `CFG.run.maxWeapons` 4→5 + pure `weaponCap(n)` in `js/net/coop.js`; `cardOffers(..., cap, exclude)` per-picker (other-owned keys never offered, passives never locked, synergy gating = picker's own dicts); host `weaponOwner` first-picker map (reset per run in `_coopStartRun`, registered at `pickCard` + `_remoteLevelUps`, persists through leave); all 3 offer sites pass `weaponCap(players.length)` + `_ownerExclusion(pl)`; solo: cap 5 = default, empty exclusion → invariance; test-logic 11.5.1/11.5.2 blocks (9) + card-blocks re-based to cap 5; test-boot 11.5 E2E in the live 3P run: host real-pick garlic → ownership + no self-exclusion + remote exclusion, passive pick isolation, remote auto-pick ownership + no garlic, cap ≤3 all players; gates 33/33 · 384/384 · boot PASS) — **11.5b PENDING (D29 respec 2026-08-22): extend first-pick exclusivity to SYNERGIES + starting-weapon pre-ownership rule (see 11.6b; implemented WITH 11.6/11.6b)**
- [x] 11.6 Characters (RESPEC'd 2026-08-22): 4 playable characters + hidden ghost — unique palette (visual tracking), theme/visual style/silhouette, **own base stats (4 archetypes: glass-cannon mage / tanky lower-medium dmg / fast balanced ranger / upper-medium swashbuckler — names + mapping RESOLVED D62: Mage=original sprite (starter) · Warden=new large armor + slowest · Ranger=new fast · Swashbuckler=new agile)** clearly visible in select (D57); unique starting weapons **Moonbolt Wand, Wraith Garlic, Aegis Blades, Pyre Lance** (D34); **per-char STAT VALUES user-approved 2026-08-22 (O closed): mage 60hp/×1.35/275sp · warden 150/×0.85/230 · ranger 110/×1.0/320 · swash 90/×1.15/300 · ghost 100/×1.0/265**; **co-op: unique character per player — taken ones greyed-out; select available in solo too** (D56); **character unlock = Soulshard shop: Mage default · Ranger 1500 · Warden 3500 · Swashbuckler 7500 (D58 RESOLVED)**; **ghost fallback: all-starter-lobby → sheet-ghost (per-player UNIQUE Pacman color → in-run UI colors; no default weapon; average stats; 2 UNIQUE starting-weapon card offers per player; D59 + D62)**; **data-driven roster modularity (D60)**; sub-steps: **11.6.0 VERY-BRIEF sources-cited select-UI research (D61; default 2b full-screen)** → 11.6.1 roster config + art (D62 sprites: Warden large-armor <brute-size, ghost sheet) + per-char stat values (propose O → approve) → 11.6.2 select UI (solo + co-op greyed-out, stats shown) + unlock progression + run-start starting weapon → 11.6.3 ghost flow (all-starter detection + 2-unique-offers allocation, never duplicated) **DONE 2026-08-22** (pure helpers in `coop.js` + host `_applyGhostFlow` + client self-detect + entry LEVELUP; test-logic +30 + test-boot E2E (a)/(b); gates 33/33 · 453/453 · boot PASS) → 11.6.4 co-op sync (character id + stats + unlocks in D53 join profile; host spawns remotes from it; E2E: uniqueness enforcement incl. late-join, greyed-out, ghost 2-offer, per-char stats) + solo invariance guard (11.11 re-read: no co-op overhead; per-char stats baseline) + gates **11.6.4 DONE 2026-08-22** (D53 profile charKey; pure `selChar`/`assignChars`/`resolveChars`; host `_applyAssign` per-seat spawn + mid-run re-assignment; per-seat ghost tint; client renders host-assigned char; taken-select greyed + denied; E2E incl. late-join reassignment; gates 33/33 · 464/464 · boot PASS)
- [x] 11.6b Weapon/synergy exclusivity closure (D29/D56, respec 2026-08-22): synergy first-pick-wins ownership (owner = only player who sees it + its upgrades; extends `weaponOwner` pattern) + starting-weapon pre-ownership (granted never picked; owner map seeded so starting weapons are excluded from OTHERS' offers — solo: own starting weapon still upgradable; co-op: character uniqueness guarantees no shared starting weapon) + test-logic/test-boot coverage (2026-08-22 — `cardOffers` synergy branch honors `exclude` (weapon+synergy key namespaces disjoint — one Set); registration at BOTH pick sites extended to `kind === 'synergy'` (level 1); pre-ownership seeded in `_applyChar` (remotes, incl. mid-run joiners) + `_coopStartRun` (host's own starter); `_ownerExclusion` gains `!pl.synergies[k]` guard; solo invariance = map never populated solo → empty exclusion; test-logic 11.6b block (5: owned synergy never offered at max sources ×3 seeds, unowned control, rng-parity no-perturb) + test-boot 11.6b E2E in the live 3P char-sync run (run-start pre-ownership wand/garlic/blades → their seats, per-seat exclusion + no self-exclusion, forced host blight pick → ownership + no self-exclusion, pl1 pool {phoenix only} with blight sources maxed); gates 33/33 · 469/469 · boot PASS runs=4 ×2)
- [x] 11.7 Co-op HUD corners: per-player panels (HP bar, level, dash cooldown, all character-specific UI) — 1P=TL, 2P=TL+TR, 3P=TL+TR+BL, 4P=all four; visible count = current player count; **assignment = join order** (A5) **DONE 2026-08-22** (`#hud-left` = ALWAYS the SEAT-0 panel, solo invariance; `hud.js` generates the 3 corner panels seats 1/2/3 = TR/BL/BR (`#hud-seat-1/2/3`): char name + 2× roster/ghost face (per-seat tint cache) + HP bar/label/low pulse + LV badge + XP fill + dash `--cd` ring, display-only — input buttons follow the LOCAL player; visible count = `min(4, players)` in join order; `#hud.coop` CSS + minimap → bottom-center in co-op; test-boot E2E: solo invariance + 3P TL=host/TR Warden/BL Ranger tracked + BR off + 4th joiner → BR Swashbuckler + leave → hidden; gates 33/33 · 469/469 · boot PASS runs=4)
- [x] 11.8 Co-op theming: HP bar fill + dash icon/button/cooldown indicator colored per selected character's palette (req 6b, 6d) (2026-08-22 — `CFG.characters[*].accent` + pure `charAccent(key,seat)` in coop.js (ghost → per-seat tint, D62; unknown → starter fallback); hud.js `applyTheme` → `--char`/`-soft`/`-line`/`-inset`/`-grad` on local roots (`#hud` + `#touch-ui` dash controls, change-detected) + per seat-panel root (on char (re)assignment); main.css: HP fill `.hp-bar .bar-fill` themed (ALSO FIXES 11.7 latent: seat-panel fills had no background) + dash icon/border/ring on `#btn-dash`/`#btn-dash-hud`/`.seat-dash` with legacy fallbacks; test-logic +7 (476/476) + test-boot E2E (ghost run per-seat tints; 3P Mage/Warden/Ranger + 4th joiner Swash) · gates 33/33 · 476/476 · boot PASS runs=4)
- [ ] 11.9 Co-op minimap + buttons: minimap bottom-center at ~66% of single-player size; pause + mute repositioned near the minimap (req 6a)
- [ ] 11.10 Boss count: **N players = N bosses of the current level** (M01 Wraith / M02 Oni / M03 Great White Shark; 1P=1, 2P=2, 3P=3, 4P=4); boss stats get the same +33%/player ramp as other enemies (Q7 resolved 2026-08-21; per-level boss per Phase 13)
- [ ] 11.11 Solo invariance: 1-player run has NO co-op overhead (TL-only panel, default minimap/buttons, 1 Wraith, no co-op overhead) (guards req 6) — **D57 re-read (2026-08-22):** "identical" = no co-op overhead, NOT Phase-10 stat/sprite identity — the solo baseline includes character select + the selected character's stats/starting weapon
- [ ] 11.12 Gates: check.mjs (new modules import-clean) · test-logic (scaling/leash/lock/characters/boss-count/sync protocol) · test-boot (multi-client co-op E2E: join → run with locks + scaling → victory) + co-op mobile parity (touch ≥72 px, corner layout on small screens, pause-on-blur)
- [ ] 11.13 Web play (internet) — research + implementation after 11.12 (A4; §3.8). **Binding research protocol (non-negotiable):** (1) BEFORE any web search/research: verify **today's real-world actual date** — standalone step, CANNOT be batched with the research steps; (2) then research the most current, evidence-based, data/outcomes-driven, **sources-cited** best practices for **self-hosted, fully game-state synchronized** web-game multiplayer / multiplayer servers; (3) **100% cost-free solutions only**; (4) cite sources (title + URL) in the docs update — **research DONE 2026-08-22 (`docs/RESEARCH_FINDINGS.md`); transport decided 2026-08-22 = D64 (Tailscale on DS124 NAS; unmodified `serve.mjs`; players join tailnet)**

### Phase 12 — Flame tune + 3 new weapons & 4 new synergies (spec'd 2026-08-21; begins after Phase 11)

Roster: 7 → **10 weapons**, 5 → **9 synergies**. All Phase 11 co-op rules (weapon exclusivity 11.5, difficulty scaling 11.3, characters 11.6) apply to the new weapons unchanged. New statuses tick via the Decision 21 pattern (never `damageEnemy` — no white flicker); every new icon must exist in `buildIcons()`; exact-effect descriptions per the 10.2 rule.

- [ ] 12.1 Pyre Lance (flamethrower) tune — **REVISED 2026-08-22 (superseded by 16.3):** flame fix moved to Phase 16.3 (total length **+33%** per user vs current build, vs originally-planned +25% → **O: reconcile length baseline at 16.3**; flow/extend speed **≥2×**; LEAD-not-trail while moving; emission origin via 16.2's common spawn offset). 12.1 = post-16.3 final verification against the 16.3 acceptance + any remaining max-velocity ramp tuning (config scalars)
- [ ] 12.2 **Bow & Arrow** (weapon #8): fast single-target arrows at nearest enemy; levels 1–5 table + icon + exact-effect descriptions
- [ ] 12.3 **Snowball Launcher** (weapon #9; icon: frozen rocket launcher): lobbed snowball with **small AoE on impact** on the targeted enemy; applies **accumulating slow** stacks; **3 stacks → brief freeze**
- [ ] 12.4 **Ring of Chain Lightning** (weapon #10; icon: jeweled finger ring w/ lightning bolt visible within the gem): applies **accumulating shock** stacks; **3 stacks → brief stun + branching electricity burst to surrounding enemies (moderate AoE)**; weapon levels increase the number of enemies the lightning **jumps/chains** to
- [ ] 12.5 Status pipeline (pure helpers): slow stacks → freeze (brief movement lock); shock stacks → stun + chain burst; **blue flame** = frozen-in-place + fire DoT simultaneously; **slow/shock stack TTL = 5 s per stack** (independent per-stack expiry; config scalar — expected playtest tuning) — all tick via the Decision 21 pattern
- [ ] 12.6 New synergies (**5 levels — same stage count as standard weapons**, offered the moment their own 2 sources max — pair-specific, NO global all-weapons lock): **Flaming Arrows** (Bow + Pyre Lance — arrows apply burn), **Heart-Piercer** (Bow + max-HP passive — greater damage + pierce through enemies to hit beyond the initial target; pierce count scales with the **synergy's own level**), **Blue Flame** (Snowball + Pyre Lance — freezes enemies in place AND does the fire DoT), **Storm Volley** (working name; Twin Fangs + Ring of Chain Lightning — lightning strike for significant added damage on **every 4th shot**, strike scales with level + proposed logical extras: strike chains per the ring's jump count, chained enemies gain 1 shock stack)
- [ ] 12.7 Roster sync: 10 weapons in card pool + icons in `buildIcons()`; `maxWeapons`/card-pool review at 10 weapons, base 5 (config); README weapon list 7→10 + synergies 5→9
- [ ] 12.8 Gates: check.mjs · test-logic (new weapon curves, slow/shock stacking, freeze/stun durations, 4 new synergy gates) · test-boot (ALL **10** weapons exercised + new-synergy E2Es) — extends the 11.12 multi-client sim if Phase 11 is already green

### Phase 13 — Multi-level expansion: 3 maps + level select + mobile view zoom (spec'd 2026-08-21; A1–A8 answered 2026-08-21; begins after 10.7/10.8 — then Phase 11 per A7)

Spec: PLAN §3.9. **Slot-based roster:** 6 enemy roles + boss keep their mechanics; each level re-skins + re-stats per slot. **Per-level difficulty chained ×1.25** (M01 1.0 / M02 ≈1.25 / M03 ≈1.56 — enemy HP, damage to players, spawn batch/interval/alive cap; A4). Binding: M02 same scale (4200×3200) + **Oni** (brute) + **Ryū dragon** (boss) + **sakura petals (map-only)** + approved skins **tanuki/hō-ōi/shikome/kitsune/miko** (A1) · M03 **1.5× area ≈ 5145×3920** (A3) + goldfish/merfolk/stingray/eel/orca/shark + **crab** small chaser (A2) + **small rising bubbles** foreground · unlocks = cumulative wins, 3× each, victory-only · level select on main menu, locked levels visible + non-selectable + requirement + progress · zoom default **0.80** + **Pause-menu Settings** (zoom + mute; mute **replaces** HUD `#btn-mute`) (A6) · A5 flavor approved (PLAN §3.9).

- [x] 13.1 Level framework: `js/world/levels.js` (pure defs: size/seed/palette/layout hook/weights/boss/particles/audio token/**per-level difficulty scalar (A4)**) + `generateWorld(seed, levelKey)` (Map 01 output identical) + per-level minimap + **per-level menu backdrop preview (A5)** (2026-08-21 — levels.js pure defs + m01 layout moved verbatim; per-level weights/boss/diff into spawner (D55); m01 bit-identical, golden pinned in test-logic)
- [x] 13.2 Map 02 "Higan" art: cherry trees, **sakura petals (Map 02 only — replace falling snow)**, torii/pagoda/shrine/stone-lantern/bamboo/koi pond (A5)/stone paths, Fuji silhouette, dusk-pink palette + full moon, torii-ring landmark (2026-08-21 — layoutM02 + full procedural art pack; palette-driven sky/ground/lighting/foreground wired; m01 bit-identical, golden holds)
- [x] 13.3 Map 02 roster: **Oni** (brute) + **Ryū dragon** (boss; segmented tail A5) — user spec + approved skins **tanuki / hō-ōi / shikome / kitsune / miko** (A1) + per-level stat tables (×1.25 difficulty, A4) + weights (2026-08-21 — `buildCharacters('m02')` Higan skin set (same footprints) + ryu in default set · `enemies.diff` ×1.25 hp/dmg at spawn (A4) · m02 weights (m01 role curve) + boss `ryu`@240 `name:'RYŪ'` + name-driven AWAKENS/FALLS banners (m01 text unchanged) · real m02 run 3 in test-boot: skins/×1.25/Ryū spawn + banner/victory)
- [x] 13.4 Map 03 "The Drowned City" art (**5145×3920**, 1.5× area — A3): kelp forests, Atlantis city (colonnade/dome/trident statue/shipwreck), coral + bioluminescent anemones, god-rays, **small rising bubbles as foreground particles (replace snow/petals — drift upward)**, decorative fish schools (A5), deep-blue palette + vent bubbles (2026-08-21 — layoutM03 5145×3920: kelp/coral/anemone/columns/dome+spire/trident/shipwreck/vent sprites + m03 sand tiles on own rng streams · m03 sky = surface-glow "moon" + 7 god-ray shafts (new optional `sky.godrays`) + shimmer near-ridge, empty far-ridge · rising-bubble foreground (D49) via Snow kind `'bubble'` + `CFG.perf.bubbleColor` · 3 open-water fish schools (koi t-driven orbits, 1×1 empty-img seam) · lights = 8 anemones + city dome · palette skyTop `#04283e`/skyHorizon `#0a4a66` · m01/m02 goldens hold · gates 30/30 · 269/269 · boot PASS runs=3)
- [x] 13.5 Map 03 roster: **crab** (small chaser — A2), **goldfish** (bat), **mer-people** (goblin; merman + mermaid skins), **stingray** (wolf), **electric eel** (cultist; lightning zap sprite), **orca** (brute), **Great White Shark** (boss; same charge skeleton, belly-flash windup) + stat tables (×1.56 vs M01, A4) + weights (2026-08-21 — `buildCharacters('m03')` slot skins crab/goldfish/merman+mermaid/stingray/eel (jagged zap sprite, NOT orb)/orca + shark boss (belly-flash frame-1 windup pose) · `shark` in CFG.enemies (wraith base stats) · m03 `weights` (m01 role curve) + boss `shark`@240 `name:'THE GREAT WHITE'` in levels.js (was null) · ×1.56 via existing `enemies.diff` (no new code) · test-logic 13.5 block (stats/weights/pickType) + test-boot M03 run 4 (skins/×1.56/'THE GREAT WHITE AWAKENS'/victory) · gates 30/30 · 279/279 · boot PASS runs=4)
- [x] 13.6 Unlock persistence: cumulative wins, victory-only (deaths don't count) — Map 02 = 3× M1, Map 03 = 3× M2 — meta.js LS pattern, pure helpers + test-logic (2026-08-21 — `meta.js` pure helpers `defaultWins`/`loadWins`/`saveWins`/`recordWin`/`isUnlocked` (LS key `qsurv.wins.v1`, CFG.meta.winsKey) · `game.js` constructor loads wins + `_gameOver` records+saves on victory only · test-logic 13.6 block (defaults, increment, 3×/chained unlock rules, LS round-trip via stub) · gates 30/30 · 286/286 · boot PASS runs=4)
- [x] 13.7 Level select on main menu: 3 cards (radio) + Start begins selection; locked visible + non-selectable + requirement + X/3 progress; **denied blip + card shake** on locked tap (A5); ≥72 px touch + safe areas; persist last selection (2026-08-22 — `ui/screens.js` `renderLevels` (3 radio cards: palette chip + name + locked-req + X/3 pips; locked tap → `denied` blip + `lv-shake`), select → `game.previewLevel` (backdrop) + `saveSelectedLevel` (LS `qsurv.level.v1`); `game.js` constructor clamps selection to unlocked + seeds `level`/`levelKey`; `sfx.js` `denied` recipe; `.screen` safe-area padding; gates 30/30 · 290/290 · boot PASS runs=4)
- [x] 13.8 Mobile view zoom + **Pause-menu Settings** (A6): camera-view factor **default 0.80** touch (≈56% area) / 1.0 desktop, toggle 0.80 ↔ 1.0 all devices, persisted; new Settings section in the Pause menu (zoom + mute toggles, ≥72 px rows); mute entry **replaces** the HUD `#btn-mute` button (M key + `qsurv.mute` LS unchanged, D8); HUD DOM + minimap stay 1× (2026-08-22 — `CFG.zoom` + `meta.js` pure `defaultZoom`/`loadZoom`/`saveZoom` (LS `qsurv.zoom.v1`, raw string); `game.js` zoom from constructor (`body.touch` detection) + `_applyZoom` (camera view = canvas/zoom; half-res lighting tracks the view; vignette 1× canvas) + `setZoom`; render wraps the view transform (enlarged view centered on the canvas — no-op at 1.0); `#btn-mute` removed → pause Settings rows `#set-zoom`/`#set-mute` (≥72 px, `hud.js` owns both; mute LS + M key unchanged, D8); test-logic 13.8 block (defaults/round-trip/invalid) + test-boot E2E (desktop default 1.0, toggle → 1600×1000 view + persist, toggle back, mute row → `qsurv.mute` on/off) · gates 30/30 · 296/296 · boot PASS runs=4)
- [x] 13.9 **Per-level high scores (A5):** one LS list per level (existing `qsurv.hiscores.v1` = M01 — no data loss); scores screen shows the selected level's list; game over ranks within the run's level (2026-08-22 — pure `scoreKeyFor(levelKey)` in screens.js (m01 = original key, siblings `qsurv.hiscores.<level>.v1`); gameover ranks/saves within `game.levelKey`'s list; scores screen + clear follow `game.selectedLevelKey`; test-logic 13.9 block (key derivation + m01 separation + in-list ranking) + test-boot E2E (m02/m03 victory → own list with run score; m01 list untouched) · gates 30/30 · 303/303 · boot PASS runs=4)
- [x] 13.10 Per-level flavor (A5): **gem/heart tints** (M02 gold-pink / M03 cyan) + **unlock banner** at threshold ("NEW MAP UNLOCKED") + unlock-progress lines on game-over/victory (per-level victory lines — deferred, D51) (2026-08-22 — `items.js` GEM_PAL/HEART_PAL + `gemHeartFor(levelKey)` (m01 palette byte-identical; buildItems drops gem/heart); `game.js` `_reskinPickups` on construct/startRun/previewLevel + unlock detection in `_gameOver` (before/after `isUnlocked` → 'NEW MAP UNLOCKED' banner after DAWN BREAKS); `screens.js` banner queue (CFG.ui.bannerMs 2600, drains in per-frame `update`) + game-over `Unlocks` row (next locked level); test-boot E2E: synthetic `_gameOver(true)` at wins.m01=2 → banner once + 6th row 'The Drowned City — 0/3 wins', no dup on 2nd victory, m01 scores cleaned; pickup reskin asserts on m02/m03 runs · gates 30/30 · 303/303 · boot PASS runs=4)
- [x] 13.11 Per-level audio (2026-08-22, A5): FLAVOR data + 5 flavor voices on the 10.6 lattice (higan bell D2 + inharmonic partial / chime pings D5/F5/B4 / taiko base 6 per loop → boss 8 per loop s8 0/4; drowned 3 rising bubbles + 1 whale glide per loop); muffle = level→lowpass→musicBus (12k pass-through m01/m02, 600 Hz m03, retuned in startMusic); m01 invariance re-verified (49/60/96/294) + 4 fake-ctx seam pumps (m01 / higan / higan-boss / drowned) — all green
- [x] 13.12 Gates (2026-08-22): all three green — `check.mjs` 30/30 (no new modules; 13.11 extended existing) · `test-logic` 316/316 (level defs/unlocks/stat tables/scores/FLAVOR blocks) · `test-boot` PASS runs=4 (M02/M03 real runs: unlock flow, boss, victory; Settings mute/zoom E2E; per-level scores; flavor) · mobile parity: `.lv-card`/`.set-row` min-height 72 px, touch zoom default 0.80, safe-area/pause-on-blur invariants untouched
- [x] 13.13 README (2026-08-22, docs-only): **Levels** section (3-map table — names, scale, chained unlocks, rosters + boss names; per-level identity line: tints/music/foreground/backdrop/scores) + Roadmap Phase 13 line flipped to **DONE 2026-08-22**
- **Phase 11 cross-ref (A7: 13 before 11):** co-op room handshake carries the level key from the start (11.1/11.2); boss count per-level (11.10: N players = N bosses of the current level)
- **Deferred (NOT approved — do not build without asking, D51):** first-clear Soulshard bonus · per-level shard multipliers · per-level victory lines · pufferfish skin · keyboard 1/2/3 select · per-level boss intro moment

### Phase 14 — In-run Soulshards counter + unified co-op earnings (spec'd 2026-08-21; queued after Phase 15, before 10.9 (LAST))

- [ ] 14.1 In-run Soulshards counter (in-GUI currency counter): live-updating Soulshards display, visible **during runs only** (state-gated, hidden off-run); change-detected DOM per the `hud.js` pattern; safe-area aware; **source = live projected award `floor(score/400)` (+25 on victory) — no economy change (O1 resolved: A, 2026-08-21)**.
- [ ] 14.2 Unified co-op earnings: all active players in a run receive the **same** Soulshard amount — one unified counter (no per-player counters); host sim computes one run-level total (formula = implementation detail, defined with the 11.2 protocol); each client accrues it in full to its own local LS (revises D53 earnings clause; meta stats/upgrades stay player-specific — D54).
- [ ] 14.3 Gates: test-logic (pure helper for projected award / unified total) · test-boot (solo: counter updates live in-run; co-op E2E: all clients receive identical totals — extends the 11.12 multi-client sim)

### Phase 15 — Level-up offer coverage: every roster weapon reachable (DEFECT — user playtest 2026-08-22; queued after Phase 12, before Phase 14)

**Root cause NOT determined yet — 15.1 reproduce/quantify FIRST; do not patch from the hypotheses below.** Order after Phase 12 deliberately: Phase 12 grows the roster 7→10 (bias gets worse), and 12.8 already extends the gates 7→10 — fix once on the final roster.
- [ ] 15.1 Reproduce + quantify: per-weapon offer-frequency measurement across N seeded runs (instrumented `cardOffers` count or harness pump) — target: every current (then 10) weapon appears in offers with sane frequency
- [ ] 15.2 Root cause: candidates (recorded 2026-08-22, UNVERIFIED): (a) `cardOffers` samples **3 of ~N pool** (N = owned-weapon upgrades + new weapons ≤ cap 5 (solo) + 5 passives + gated synergies ≈ 8–12+) → low per-weapon per-offer odds; (b) once a player holds 5 standard weapons (cap), **no further new-weapon offers exist** — roster 7 > cap 5, so any weapon never shown before the slots fill is unreachable that run; (c) interplay with 11.5/11.6b exclusion (owner-exclusions shrink the pool asymmetrically)
- [ ] 15.3 Fix: offer-pool design guaranteeing sane per-run reachability for every roster weapon without breaking exclusivity/cap rules (design = decision at 15.2, documented before implementing)
- [ ] 15.4 Regression: test-logic determinism (seeded rng: every weapon key offered ≥1× over K runs) + gates + before/after frequency numbers in the tick note

**User observation (playtest data for 15.1):** in level-up offers — pistols (Twin Fangs) appear often; bombs (Sunder Bombs) occasionally; original-base weapons appear; **Pyre Lance (flamethrower) has NEVER appeared in ≥20 recent runs (~24 h)**; earlier era: pistols + flame seen, bombs rarely. Clarification for future sessions: **Snowball Launcher + Ring of Chain Lightning do not exist yet (Phase 12, unimplemented)** — "never seen" for those two is expected, NOT part of the bug.

### Phase 16 — Playtest defects (user report 2026-08-22; defects in the CURRENT build — queued after 11.8, before 11.9)

- [x] 16.1 Map 01 falling-snow regression: no falling snow visible in Map 01 anymore — reproduce (m01 run), root-cause (per-level foreground-particle kind routing since Phase 13 — 13.2's "m01 snow regression" check passed at the time; check kind selection world/particles + menu backdrop), fix + non-regression (m01 snow · m02 sakura · m03 bubbles) + gates (2026-08-22 — D70 obeyed: repro QUANTIFIED first via new test-boot `probeSnow` (ctx-stub path-op deltas across one snow pass) → `arc=0 ellipse=0 fill=90` at the user-visible run-1 m01 start = 90 fills on empty paths (invisible); git forensics: **13.4 `a8c332b` swapped the snow `else ctx.arc(...)` for `else if (bubble) {...}`** — empty-path `fill()` is a silent no-op in canvas AND in the stub, so every gate stayed green since 13.4 while the flakes were invisible; 13.2's "regression check" only asserted the `snow.kind` ROUTING, which was never broken; fix = the snow arc restored exactly as v1.0.0 `03eeac2` (one branch in `Snow.draw`); non-regression = probe at 6 sites: run-1 m01 in-game + m01/m02/m03 menu backdrops + m02/m03 run starts (per-kind path counts: snow 90 arc/90 fill · petal 90 ellipse/90 fill · bubble 180 arc/90 stroke/90 fill); gates 33/33 · 476/476 · boot PASS runs=4; note: 10.4 bench baseline shifts — the m01 snow pass now really draws (+90 arc/+90 fill per frame in m01 renders — 10.9 measures against post-16.1 numbers)
- [x] 16.2 Weapon projectile origin: projectiles appear to originate from the player's FEET — move the common projectile spawn origin to ~mid-torso height (shared spawn offset, config scalar; verify per weapon incl. melee/aura; co-op snapshot-neutral) + gates (2026-08-22 — root: the player's `(x,y)` IS the feet (sprite drawn bottom-anchored, `drawImage(img, -w/2, -h)`), and every fire site used it directly → `CFG.combat.spawnOriginFrac` 0.55 (fraction of the character sprite height above the feet, per-char: mage 35.2 / warden 36.3 / ranger 33 / swash 34.1) + `Player._spawnY()`; the 5 fire sites (wand/axe/pistols/bombs/flame) now aim AND spawn from mid-torso (angles + bomb lob distance + flame `_flameAng` all from the spawn origin); axe return target + orbiter orbit center + orbiter knockback + blade draw center → mid-torso; **garlic stays feet-centered (ground pulse aura, documented decision — no fired projectile)**; **co-op snapshot-neutral verified: no projectile key exists in the sync protocol at all** (projectiles are host-sim-only — the origin shift changes values, never the format); **latent-fix found while verifying melee: the blade orbiters + garlic pulse ring were NEVER drawn** — `combat.draw` read `this.player`/`this.orbitT`/`this.garlicT`, none ever assigned (the garlic radius would have been NaN and thrown in a real browser) → `draw(ctx, t, players)` now loops the live players using each player's sim `_orbitT`/`_garlicT` (drawn position == damage position); client: the local player's orbit/garlic clocks are advanced cosmetically in `_clientUpdate` (client never sims; `t` never advances there), remote blades not drawn (no angle in the snapshot — consistent with no projectile ever crossing the wire) · test-logic +4 (480/480: frac band, _spawnY exact per-char, taller→higher) + test-boot E2E (controlled m01 game: wand/axe/pistols×2/bomb-arc/flame first projectiles spawn at EXACT mid-torso (bit-exact origin asserts), blade orbit discrimination — enemy at feet-level orbit point NOT sheared, mid-torso one sheared — with the spatial grid populated manually (grid only rebuilds in `enemies.update`), combat.draw drew blade sprites + 1 garlic ring arc) · gates 33/33 · 480/480 · boot PASS runs=4)
- [ ] 16.3 Pyre Lance flame: (a) total stream length **+33%** (user vs current build) (b) flame extends/flows toward targets **≥2× faster** (currently often barely appears before killing the target) (c) while moving, flame must LEAD the aim direction, not trail behind the player (user hypothesis: directional momentum/inertia; implementation approach = agent O at 16.3) — config scalars + gates; **absorbs 12.1's length/origin/speed scope (12.1 revised below)**
- [ ] 16.4 Gates: check.mjs · test-logic · test-boot (m01 snow present in a real m01 run; spawn-origin offset assert; flame length/flow asserts where the stub allows) — co-op invariance: fixes are sim-side, solo + co-op both run the host sim

### Phase 17 — Selectable run durations & boss schedule (spec'd 2026-08-22; PLAN §3.10; queued after Phase 15)

User spec (as-is): durations **5 (default) / 10 / 15 / 20 (max set-time) / ENDLESS** (until death); boss events at **4:00 / 9:00 / 14:00 / 19:00** + **every 5:00 during ENDLESS**; 15-min example = exactly 3 bosses (4:00, 9:00, 14:00) — "the same pattern applies to all of the listed potentially selectable run durations." O's per PLAN §3.10 (20-min 19:00 confirm; ENDLESS fixed-slots-vs-5-min-cadence; co-op N-bosses-per-event; per-level boss per event; ENDLESS victory/high-score semantics).

- [ ] 17.1 O resolutions (user confirm) + config: per-duration boss-time table (`CFG.run.durations` + boss times) + spawner difficulty tail past 5:00 (ramp cap/hold) + ENDLESS = death-only end (no "DAWN BREAKS")
- [ ] 17.2 Main-menu duration select UI (5 options; default 5:00 = current behavior; per-level persistence §3.7; ≥72 px + safe areas)
- [ ] 17.3 Run machinery: victory = survive to selected duration (5:00 default → solo invariance); boss event(s) at each scheduled time (co-op: N per player, D35 pattern per 17.1 O); per-level boss for each event (Phase 13) per 17.1 O; ENDLESS HUD/timer (count-up, no end line) + high-score inclusion per 17.1 O
- [ ] 17.4 Gates: test-logic (boss-time table: 5min→[240], 10→[240,540], 15→[240,540,840], 20→[240,540,840,1140], endless→schedule fn; spawner tail) · test-boot (10-min run E2E: boss at 240 + 540; ENDLESS: alive past 5:00, no victory, boss cadence; 5:00 default run = bit-identical to today's solo baseline)

### Phase 18 — Level-up actions: SKIP / BANISH / RE-ROLL (spec'd 2026-08-22; PLAN §3.11; research: `RESEARCH_FINDINGS.md` §7; queued after Phase 17)

User spec (binding, as-is): **"SKIP", "BANISH", and "RE-ROLL" options on the level-up screen** — each **initially unlocked via the meta-progression store in the Main Menu**, then **upgraded within that store to grant additional uses during runs (maximum 5 uses per run *each*)**. **NOT equippable items, NOT selectable cards, CANNOT be leveled-up mid-run/during level-up screens.** VS-model context (research §7): level-up-screen buttons spending a player stat; skip → gain XP instead (~66% next level per user / 20% per Reroll-Banish use per wiki — our ratio O); reroll → fresh set (discarded set excluded from the redraw only); banish → card removed from offers for the rest of the run (owned-item freeze O).

- [ ] 18.1 O resolutions (user confirm per §7.3): skip-XP ratio (candidate: no-XP free pass) · banish granularity (weapon / passive / synergy keys; owned-item freeze?) · reroll pool exclusion (VS semantics: discarded set excluded from redraw only) · Soulshard cost curve (unlock + upgrades 1→4)
- [ ] 18.2 Meta store: 3 new entries in the Main Menu Upgrades (Soulshard economy, meta.js pattern) — per feature: unlock + upgrade levels 1→4 = uses 1→5; persisted (`qsurv.meta.v1` extension or sibling key — O at 18.2, no data loss)
- [ ] 18.3 In-run use counters (per player, host-authoritative; co-op per-step snapshot fields — dash-state channel) + level-up screen UI: 3 action buttons + live use counts (≥72 px, exact-effect tooltips per 10.2 rule, hidden until unlocked, spent-state disabled)
- [ ] 18.4 Mechanics: SKIP (level granted, no card; XP per 18.1) · RE-ROLL (redraw; exclusion per 18.1) · BANISH (picker-scoped permanent-for-run offer exclusion — mirrors 11.5/11.6b `exclude` plumbing; never offered to the banning picker incl. upgrades) + interplay with 10.7 empty-pool auto-skip + 15.3 offer-pool fix (actions consume/alter offers, not the pool design)
- [ ] 18.5 Gates: test-logic (use counters, cost curve, banish exclusion purity, reroll exclusion) · test-boot (solo: unlock→use each action E2E through the real LEVELUP pipeline; co-op E2E: per-picker counters, banished key excluded only for the banning seat, host authoritative) · solo invariance (unlocked-0 = today's screen, no buttons)

### Phase 19 — In-run HUD: equipment icons + Pyre Lance fuel bar (spec'd 2026-08-22; PLAN §3.12; queued after Phase 18)

User spec (as-is): (1) display currently equipped **weapon and item icons beneath the character XP bar**, with a **level number next to each weapon's icon** shown **at all times throughout the run**; (2) **Pyre Lance ammo/fuel displayed beneath the player character** (currently above — revises D24).

- [ ] 19.1 Equipment row under the XP bar: weapon icons + per-weapon level number, live all run (change-detected DOM per `hud.js` pattern); "item" icons = run-equipable items (roster lands Phase 21 — row renders weapons-only until then); passive-icon inclusion = O at 19.1 (user said "weapon and item")
- [ ] 19.2 Pyre Lance fuel bar relocated ABOVE → **beneath the player character** (world-space; D24 revised); verify readability vs ground tiles + co-op; fuel logic unchanged
- [ ] 19.3 Co-op: placement vs 11.7 corner panels (per-seat O) + 11.9 minimap/button reposition — no overlap on small screens (mobile parity: ≥72 px / safe areas where touch-relevant)
- [ ] 19.4 Gates: test-boot E2E (solo: icons appear at run start = character starting weapon only (Phase 20 invariance), level numbers track picks; fuel bar present beneath player for flame owner) · co-op E2E (per-seat rows; no overlap) · solo invariance for non-flame rows (row is NEW UI — visible in solo by design, user spec)

### Phase 20 — Passive-start rule: players start runs with NO passives (spec'd 2026-08-22; D66; queued after Phase 12, before Phase 15)

User spec (binding, as-is): "Passive items should **NOT** default to level 1 for the player upon starting a new run. The player should need to select them from the level-up offerings. This also ensures multiplayer sessions do not encounter issues with overlapping passives/items." Impact: `Player.reset()` stops granting L1 passives (character starting weapons UNCHANGED — D34); recomputeStats baseline rework (no passive multipliers at run start); card-offer economics shift (passives become acquired, not given) — **Phase 15's 15.1 frequency measurement MUST baseline on the post-Phase-20 economy** (hence 20 before 15).

- [ ] 20.1 `Player.reset()`/run-start: no passive grants (weapons only) + stats recompute baseline (char × meta, passives = {}) + test rebaseline (meta/applyCard blocks that assume L1 passives)
- [ ] 20.2 Co-op: per-picker passive picks unaffected (D32 — passives never locked); verify ghost flow (11.6.3: ghost gets 2 weapon offers, no passives — unchanged) + exclusivity closure (11.6b: passives unaffected)
- [ ] 20.3 Gates: test-logic (reset = no passives; recomputeStats) · test-boot (run start: passives = {} for all seats; first passive arrives via level-up pick E2E) · solo + co-op invariance otherwise

### Phase 21 — Extended roster: 9 weapons + 2 run items + 5 characters (spec'd 2026-08-22; PLAN §3.13 VERBATIM; queued after Phase 14, before 10.9)

User spec (verbatim, PLAN §3.13): **Weapons:** Tank Cannon · Continuous laser beam · Wolf "summon"/companion · Rolling Boulder · Web-slingers · Gatling Gun · Baseball Bat · Frog Tongue · Cannonball (player-propulsion). **Run-Equip-able Items:** Mirror Shield · Ice Skates. **Playable Characters:** Werewolf (Wolf summon) · Stone Golem (Rolling Boulder) · Baseball Player (Baseball Bat) · Giant Toad (Frog Tongue) · Wild West Gunslinger (Twin Fangs). Specifics deferred by user ("document as-is for now") — per-item O's at each sub-step. All landing via the D60 data-driven modularity rule (config rows + card text + icon builders — NO pipeline/combat/sync code changes); all Phase 11 co-op rules apply unchanged; new DoT/CC statuses (bleed/poison/hold) tick via the Decision 21 pattern (no white flicker).

- [ ] 21.1 O resolutions (user confirm): per-weapon stat values · item rules (do items occupy weapon slots? equip cap? stacking?) · per-character archetypes/stats + Soulshard unlock costs (D58 pattern) · Cannonball × co-op leash (11.4) interplay · "smallest sized enemies" size class for Rolling Boulder auto-kill (existing size taxonomy)
- [ ] 21.2 Weapons 1–9, one sub-step each (or small groups): config stat table + `buildIcons()` icon + exact-effect card text (10.2) + combat impl (new mechanics: pierce-then-blast (Tank Cannon), beam (laser), bounded autonomous summon (Wolf — 3-span leash, 2 s pounce CD, bleed DoT), cardinal-only single-active projectile (Boulder — 6-hit decay, crumble, smallest-size auto-kill), web AoE hold (Web-slingers), reload-magazine burst (Gatling — 40 bullets / 3 s reload), melee knockback + 1/500 firework homerun (Bat), latch-drag + poison + consume-heal (Tongue), player-propulsion ricochet (Cannonball — 10 s CD, +1 bounce/level, 6-hit cap, invulnerable during)) + co-op snapshot key + boot-sim exercise
- [ ] 21.3 Items (Mirror Shield — 3 projectile blocks/ricochets per 5 s; Ice Skates — slip movement + speed + inertia/level + 6 HP max impact both-ways): item slot class definition (O 21.1) + config + icons + card text + mechanics + co-op
- [ ] 21.4 Characters 5 (Werewolf/Stone Golem/Baseball Player/Giant Toad/Wild West Gunslinger): D62 pattern (palette/silhouette builder + per-char stats visible in select + unique starting weapon + Soulshard cost D58 pattern + ghost-flow invariance) — weapon-pairing constraint: Werewolf/Stone Golem/Baseball Player/Giant Toad require their weapon from 21.2 to exist first
- [ ] 21.5 Roster sync: card pool + `buildIcons()` audit + maxWeapons/cap review + README roster update (weapons 10→19, items 2, characters 4→9 + ghost)
- [ ] 21.6 Gates: check.mjs · test-logic (all new stat tables, status TTLs, item CD math) · test-boot (ALL new weapons/items/characters exercised; new-status E2Es; co-op E2E extends 11.12 multi-client sim) — roster-count assertions re-based 10→19/2/9

## Resume Notes — 16.3 Pyre Lance flame next (continue here; 11.1–11.8 + 16.1 + 16.2 done; O stat values approved 2026-08-22)

**16.2 DONE (2026-08-22):** projectile spawn origin feet→mid-torso. Root: `Player`'s `(x,y)` IS the feet (sprite bottom-anchored: `drawImage(img, -def.w/2, -def.h)` in `Player.draw`), and all 5 fire sites in `Player._weapons` used `this.x, this.y` for both aim angles and spawn. Fix: `CFG.combat.spawnOriginFrac` 0.55 (fraction of the character sprite height above the feet — per-char: mage 35.2 / warden 36.3 / ranger 33 / swash 34.1 px) + `Player._spawnY()`; wand/axe/pistols/bombs/flame now aim AND spawn from mid-torso (bomb lob `dist` and flame `_flameAng` measured from the spawn origin too); axe return target, orbiter orbit center (damage + knockback + draw), all → mid-torso. **Garlic deliberately feet-centered** (ground pulse aura, not a fired projectile — moving it up shrinks feet-level coverage; documented decision). **Co-op snapshot-neutral verified**: no projectile key exists in the sync protocol (`js/net/sync.js` has playerSnap/enemySnap/pickupSnaps only) — projectiles are host-sim-only; the origin shift changes values, never the format. **Latent-fix (melee verify, 16.2 spec):** the blade orbiters + garlic pulse ring were NEVER drawn — `combat.draw` read `this.player`/`this.orbitT`/`this.garlicT`, none ever assigned (garlic radius would have been NaN → `arc()` throw in a real browser once it ever drew) → `draw(ctx, t, players)` loops live players using each player's sim `_orbitT`/`_garlicT` (drawn position == damage position, host); client advances the LOCAL player's orbit/garlic clocks cosmetically in `_clientUpdate` (client never sims and `game.t` never advances there); remote blades not drawn (no angle in the snapshot — consistent with no projectile crossing the wire). Tests: test-logic +4 (480/480) + test-boot controlled-m01 E2E (first projectiles of all 5 weapons spawn at the EXACT mid-torso — bit-exact origin asserts; blade orbit discrimination with a manually-populated spatial grid (the grid only rebuilds in `enemies.update` — first boot run failed `mid hit=false` for exactly this reason); combat.draw drew ≥n blade sprites + exactly 1 garlic ring arc). Gates 33/33 · 480/480 · boot PASS runs=4. **NEXT = 16.3 Pyre Lance flame** (total stream length +33% vs current build · flow/extend ≥2× faster · LEAD-not-trail while moving — implementation approach = agent O at 16.3; emission origin already = 16.2's mid-torso) → 16.4 gates.

**16.1 DONE (2026-08-22):** m01 falling-snow regression (D70 reproduce-first obeyed) — new test-boot `probeSnow(what)` helper (ctx-stub `drawOps` gained an `ellipse` counter; probe = delta of arc/ellipse/fill/stroke across ONE `snow.draw` call on the game's real ctx/camera): quantified repro BEFORE the fix = **run-1 m01 in-game snow pass drew `arc=0 ellipse=0 fill=90`** (90 `fill()` on EMPTY paths — `beginPath()` with no path command; silent no-op in canvas AND in the stub = invisible in the browser too, why every gate stayed green). Git forensics: `03eeac2` v1.0.0 had `ctx.arc(sx, sy, f.sz, 0, TAU)` for snow → 13.2 kept it (`else ctx.arc`) → **13.4 `a8c332b` restructured it as `if (petal) ellipse; else if (bubble) {...}` — the snow arc was swallowed** (the string survives only in the bubble branch, which is why `git log -S` on the arc string looks innocent). The 13.2 "m01 snow regression" test only asserted `snow.kind === 'snow'` ROUTING (never broken) — kind routing + motion were fine all along (m01='snow'/'petal'/'bubble' in levels.js, `snow.reset(undefined, level.foreground)` at startRun/client-start/menu-backdrop; motion via `update(dt)`). Fix = one branch in `Snow.draw`: `else ctx.arc(sx, sy, f.sz, 0, TAU);` before the shared `fill()` (exactly v1.0.0). Non-regression = probe at 6 sites (run-1 m01 start · m01/m02/m03 menu backdrops · m02/m03 run starts) asserting per-kind path counts (snow 90 arc/90 fill · petal 90 ellipse/90 fill · bubble 180 arc/90 stroke/90 fill). 10.4 bench shifts (m01 renders now really draw the 90-flake pass — 10.9 measures against post-16.1). **NEXT = 16.2 projectile spawn origin feet→mid-torso** (shared spawn offset, config scalar; verify per weapon incl. melee/aura; co-op snapshot-neutral) → 16.3 flame → 16.4 gates.

**11.8 DONE (2026-08-22):** per-char UI theming (D62 channel; req 6b/6d) — `CFG.characters[*].accent` (mage `#9fe8ff` · warden `#ffb454` · ranger `#a4ffc9` · swash `#e8b45a`; ghost `null`) + pure `charAccent(key, seat)` in `coop.js` (ghost → `ghostColor(seat)`; unknown → starter fallback); `hud.js` `applyTheme` sets `--char`/`--char-soft`/`--char-line`/`--char-inset`/`--char-grad` (hexToRgba 1/0.16/0.55/0.10 + soft→line gradient) on the LOCAL roots (`#hud` + `#touch-ui` — dash controls follow the local player) with `themeKey` change detection, local key = `chars[0]` in co-op / `game.charKey` solo (seat-0/TL rule), and on each seat-panel root inside `syncFace` after the `pan._key` update (covers mid-run reassignment). main.css: `.hp-bar .bar-fill` themed fill (`linear-gradient(180deg, var(--char-grad))`) — **this ALSO FIXES the 11.7 latent defect: seat-panel HP fills had NO background at all (was id-only `#hp-fill`, legacy hex gradient invisible-on-its-own id)** — + dash icon/border/ring accent on `#btn-dash`/`#btn-dash-hud`/`.seat-dash` (`.dash-icon`/`#btn-dash-hud svg`/`.seat-dash svg` fill + border + box-shadow; `#btn-dash-hud` gains a 60% opacity bg so the themed icon reads on it), ALL with legacy color fallbacks (solo default = mage accent, D57 solo baseline includes the selected char). test-logic +7 (476/476: accent table incl. ghost null, charAccent ghost/unknown/valid, css consumers) + test-boot E2E in the EXISTING sections (11.6.3 (a) ghost run: `hudG = initHud(hostG)` after `pickCard(0)` — local roots = Blinky tint line `rgba(255,75,75,0.65)` + seat panels Pinky/Inky; 11.7 section: Mage local incl. exact `--char-grad` string, Warden `rgba(255,180,84,0.16)` soft, Ranger, 4th joiner → Swash on BR). Gates 33/33 · 476/476 · boot PASS runs=4. **NEXT = Phase 16 playtest defects** (16.1 m01 snow regression — REPRODUCE/QUANTIFY FIRST per D70, no hypothesis patches · 16.2 projectile spawn origin feet→mid-torso config scalar · 16.3 Pyre Lance flame +33% length / ≥2× flow / LEAD-while-moving (agent O; supersedes 12.1's scope) · 16.4 gates).

**11.7 DONE (2026-08-22):** co-op HUD corners — `#hud-left` = ALWAYS the SEAT-0 panel (solo invariance 11.11: solo seat 0 = local player → zero visual/behavior change; co-op TL = host on the host browser, remote host on client browsers — TL source = seat-0 player, NOT always `game.player`); `initHud` now builds 3 corner panels for seats 1/2/3 = TR/BL/BR (`#hud-seat-1/2/3`, class `seat-panel` + `off` until occupied; appended to `#hud`), each = char name (`CFG.characters[key].name`) + 2× face (roster `idle[0]`; ghost = `buildGhost(ghostColor(seat))` cached per seat) + HP bar/label + `low` pulse + LV badge + XP fill + dash `--cd` ring — display-only (input `#btn-dash`/`#btn-dash-hud`/`#btn-pause` stay LOCAL-player-driven); visible count = `min(4, players)` in join order (A5): 1P=TL, 2P=TL+TR, 3P=TL+TR+BL, 4P=all four; per-seat data via `seatPlayer(j)` (host: `players` index = seat; client: same mapping as `_netState`/`_interp`), per-seat char = `resolveChars(netRoster)[seat]`; `#hud` gains `coop` class in co-op (CSS: minimap → bottom-center, avoiding the seat-1 TR collision — 11.9 resizes it); `initHud` returns `{ update, panels }` (panels = E2E handles — DOM stub `querySelector` returns a fresh el, so asserts use the returned refs). test-boot E2E: solo runs (no `coop` class, TL = local) + 3P (coop class, TL = host hp/lv, TR Warden + HP/LV tracking, BL Ranger, BR off) + mid-run 4th joiner → BR Swashbuckler visible + leave → roster 3 + BR hidden, others undisturbed. Test gotcha (fixed): the 4th-joiner assert needs an explicit `hudHost.update()` first — `hostG` has no render loop in test-boot (the real app's render hook drives `update()` per frame), so panel visibility only advances on explicit calls. **NEXT = 11.8 co-op theming** (HP fill + dash icon/ring colored per selected char palette, req 6b/6d).
**11.6b DONE (2026-08-22):** exclusivity closure (D29 respec; closes the 11.5b PENDING) — (1) **synergy first-pick-wins:** `cardOffers` (player.js) synergy branch honors the same `exclude` Set as weapons — one Set serves both namespaces (weapons wand/garlic/axe/blades/pistols/flame/bombs · synergies blight/tempest/inferno/napalm/phoenix — disjoint, no cross-filtering); `game.js` registers `kind === 'synergy'` (level 1) first-picks into `this.weaponOwner` (cardKey → player; shared by weapons AND synergies) at BOTH pick sites (`pickCard` + `_remoteLevelUps`) — owner = the only player who sees that synergy + its upgrades; (2) **starting-weapon pre-ownership:** char-granted weapons are never picked, so the map is seeded: `_applyChar(pl, key, seat)` (the remote char-apply chokepoint: run start + mid-run joiners) seeds the char's starting weapon (mage=wand · warden=garlic · ranger=blades · swash=flame · ghost=null), `_coopStartRun` seeds the host's own starter → starting weapons excluded from OTHERS' offers; the owner keeps upgrades to their own starter; (3) `_ownerExclusion(pl)` gains a `!(pl.synergies||{})[k]` guard (never exclude your OWN synergy); (4) **solo invariance:** both seed sites are co-op-only → `weaponOwner` never populated solo → exclusion always empty → solo runs bit-identical (own starting weapon still upgradable in solo). Tests: test-logic 11.6b block (5 checks → 469/469: owned synergy never offered at max sources ×3 rng seeds; unowned blight IS still offerable (control — no over-exclusion); excluding a synergy key does NOT shift the weapon draw — rng parity with/without the synergy in `exclude`) + test-boot 11.6b E2E inside the live 3P char-sync (11.6.4) run section: run-start pre-ownership (`weaponOwner.wand/garlic/blades` → their seats), per-seat exclusion (other seats' starters excluded, own not), forced host blight pick (forced card bypasses gating — same pattern as the 11.5 garlic pick) → ownership + no self-exclusion, then pl1 cap+passives maxed (`{wand:5,garlic:5,axe:5}` = cap 3 filled, gates blight(wand+garlic)+phoenix(passives) ONLY) → pool asserted = **phoenix only** (owned blight absent); pl1 baseline restored after. Gates 33/33 · 469/469 (+5) · boot PASS runs=4 (×2 consecutive, WS-flake safe). **Flake fix (this session, test-only):** first boot run failed the 11.6.3 ghost-section strict `weaponOwner` count assert (`=== 4`) — a remote leveling up during the live pump auto-picks via `_remoteLevelUps` and legitimately registers a 5th owner; 11.6b is behavior-neutral here (ghost `c.weapon = null` → no pre-ownership seed; synergy guard inert for weapon keys) → assertion now checks the 4 true first-picks (host entry / both remote starts / joiner start) map to their distinct pickers, no strict count (11.5 precedent). **NEXT = 11.7 co-op HUD corners** (per-player panels: HP bar, level, dash cooldown, all character-specific UI; 1P=TL, 2P=TL+TR, 3P=TL+TR+BL, 4P=all four; visible count = current player count; assignment = join order, A5).

**11.6.4 DONE (2026-08-22):** co-op character sync/E2E (D53/D56/D57/D59) — D53 join profile carries the selected char (`joinProfile(meta, chars, sel)`; 2-arg legacy → starter fallback); pure `coop.js` `selChar(profile)` / `assignChars(roster)` (seat-order unique; displaced seat → next unlocked char; exhausted → ghost) / `resolveChars(roster)` (all-starter lobby → EVERY seat ghost, D59 wins; else assignChars); `game.js`: `setCharacter` patches the host's own roster entry (seat-0 authoritative), `_applyChar` (per-char base stats + starting weapon + sheet swap, ghost via `buildGhost(ghostColor(seat))`, hp refill, no LS persist), `_remotePlayer(profile, key, seat)` (leaves `_assign` unset so the pair gets dealt), `_applyAssign` (replaces `_applyGhostFlow`: host-authoritative per-seat resolution at run start + every mid-run roster change — displaced seat gets its next unlocked char, ghost seats get the seeded 2-offer deal, re-ghosted seat = fresh deal, host-ghost entry LEVELUP only on first-time ghost), `_netRoster` mid-run join = provisional `selChar` + `_applyAssign`, client `_startClientRun` renders `resolveChars(netRoster)[seat]`; `screens.js` taken-Set from `game.netRoster` (other seats via `selChar`) + `roster` bus re-render + denied tap. E2E-caught bugs (fixed): (1) client state broadcast outran the joiner's roster (`seat === -1` → `applyPlayerSnap` on `remote[-1]` crash) → `_netState` seat guard (skip until roster arrives; next per-step state overwrites); (2) `_applyAssign` early-returned when no seat ghosted → mid-run joiner kept its provisional pick → `offers` allocation made ghost-conditional instead. Tests: test-logic 11.6.4 block (+11 → 464/464: selChar, joinProfile charKey, assignChars contested/fallback/4P-exhaustion/normalize, resolveChars both rules) + test-boot 11.6.4 E2E (3P distinct chars: per-seat maxHp/dmgMul/starting weapon + uniqueness; mid-run c3 with TAKEN pick + swash unlocked → host reassigns swash (90 hp, Pyre Lance), `resolveChars` = mage,warden,ranger,swash, joiner client renders swash) + greyed-select subtest (taken class on warden/ranger + denied tap) + 11.6.3 (b) re-baselined to D56 (two starter-only seats both holding mage → host keeps mage, both remotes ghost with disjoint pairs, state stays PLAYING). Gates 33/33 · 464/464 · boot PASS runs=4 (solo runs 1–4 baseline unchanged). **NEXT = 11.6b exclusivity closure** (synergy first-pick ownership + starting-weapon pre-ownership, D29 respec), then 11.7–11.12.

**11.6.3 DONE (2026-08-22):** ghost fallback E2E (D59/D62) — pure helpers in `js/net/coop.js`: `sanitizeChars` / `allStarterLobby(seats)` (roster entries `{profile:{chars}}` or raw lists; ≥2 seats, every seat starter-only) / `ghostColor(seat)` (Pac-Man tint from `CFG.ghostColors`, wraps) / `allocateGhostOffers(n, rng)` (seeded shuffle of the 7 weapons → 2 per seat in seat order; 4P = 2/2/2/1) + `joinProfile(meta, chars)` = D53 profile + sanitized chars. `game.js`: host `_applyGhostFlow()` at run start + on mid-run roster change — all-starter → every seat ghosts (sheet swap + `recomputeStats` baseline + `weapons = {}` per D59 no-starting-weapon + per-seat disjoint pair; remotes auto-pick `offers[i][0]` with `_ghostOffers` re-armed to the full pair so the next level-up offers the held remainder; host opens on its entry 2-card LEVELUP, spent via `pickCard`; `weaponOwner` registered for every pick; mid-run joiner ghosts in with the remainder pair; de-ghost on close/detach). Client `_startClientRun` self-detects from `netRoster` → seat-tinted sheet. E2E-caught bugs (fixed): (1) ghost seats kept the starter `wand` + stale maxHp (`setCharacter` neither clears weapons nor recomputes) → explicit `weapons = {}` + `recomputeStats` in all three ghost branches; (2) `allStarterLobby` was called with ALREADY-UNWRAPPED profiles (`roster.map(e => e.profile)`) → `s.profile` undefined → every seat read as legacy-starter → ALWAYS ghosted (mixed-lobby E2E caught it) → callers now pass roster entries. Tests: test-logic 11.6.3 block (+30 → 453/453) + test-boot 11.6.3 E2E (a) all-starter 3P real-relay run (host entry pair = 2× level-1 unique weapons, no starting weapon, baseline 100+meta HP; remotes auto-pick disjoint; mid-run 4th joiner ghosts in with the 4th unique weapon; 4 distinct owners; client renders its own ghost seat) + (b) fresh room mixed lobby → NO ghosting, starter wand kept. Gates 33/33 · 453/453 · boot PASS runs=4 (solo runs 1–4 baseline unchanged).

**11.6.2 DONE (2026-08-22):** full-screen select (2b, D61) — menu **Character** button (`#btn-character`) opens `#screen-select` (new screen in the overlay system alongside scores/upgrades; closes on **Confirm**). `renderChars()` in screens.js: 4 cards from `CFG.characters.order` — 2× roster idle art (per-char canvas), name, **D57 stats line** `HP x · Dmg ×y · Speed z`, `Starts with: <weapon name>` (CFG.weapons lookup), archetype desc; selected = `sel` radio state; locked (D58) = `locked` + `Locked · <cost> ◆` req line, **one tap buys + selects** when affordable (denied blip + shake otherwise — same pattern as level cards); wallet line `#char-shards` (`<n> ◆`); re-render on `meta` bus. Co-op: `taken` Set hook in renderChars (greyed-out `taken` class + TAKEN line + denied on click) — **wired in 11.6.4**. Pure `meta.js` (Node-tested): `defaultChars`/`loadChars`/`saveChars`/`isCharUnlocked` (ghost/unknown never "unlocked")/`buyChar` (pure spend; starter + already-unlocked + unknown = no-op, no double spend) + `loadSelectedChar`/`saveSelectedChar` (playable keys only — ghost rejected; invalid → starter). `CFG.meta.charListKey` = `qsurv.chars.v1` (LS key `qsurv.character.v1` pre-existed 11.6.1). `game.js`: ctor builds `this.roster = buildRoster(CFG.ghostColors[0])` (local ghost tint = seat 0, D62), loads `chars`/`charKey` (clamp to unlocked → starter), Player constructed from `roster[charKey]` + `setCharacter` + flash/shadow from the sheet; **`setCharacter(key)`** = validate (playable + unlocked) → `player.setCharacter` + persist + roster art/flash/shadow swap + `char` bus event; **`buyCharacter(key)`** = `buyChar` → persist chars + meta (`meta` bus). `startRun` unchanged — `player.reset()` already applies per-char hp + starting weapon (11.6.1) so run-start application = selection persistence. index.html: Character button (menu, after Start) + select screen markup; css `.char-card` grid (≥72px, locked dashed + desat, taken grayscale, `lv-shake` reused). Tests: test-logic 11.6.2 block (16: buyChar spend/no-op paths, loadChars filter/corrupt, selected-char round-trip + ghost/unknown reject) + test-boot E2E (fresh LS: open → 4 cards + starter sel + costs + wallet 9999 → warden buy+select (shards 6499, LS persisted, re-render sel/unlocked) → swash 7500 > 6499 denied+shake (selection/shards untouched) → confirm closes to menu (state stays MENU) → **restore clean state** (mage + ['mage'] chars + shards 0 + LS) so runs 1–4 baseline holds (run-2 maxHp 80 assert re-verified in the same boot)). Gates: 33/33 · 423/423 (+16) · boot PASS runs=4. **NEXT = 11.6.3 ghost flow** (all-starter-lobby detection + per-seat ghost assignment via `CFG.ghostColors` + 2 UNIQUE starting-weapon offers per ghosted player, never duplicated across players — D59/D62; ghost is co-op-only, never in the select list) + **11.6b exclusivity closure** (synergy first-pick ownership + starting-weapon pre-ownership, D29 respec).

**11.6.0/11.6.1 DONE (2026-08-22):** 11.6.0 select-UI research (D61; 2 sources cited): Wikipedia *Pac-Man* (4 ghost colors Blinky/Pinky/Inky/Clyde → D62 per-seat tints = `CFG.ghostColors`) + NN/g *10 Usability Heuristics* (`nngroup.com/articles/ten-usability-heuristics/`; #1 visibility/#3 user control/#4 consistency/#8 minimalism support 2b) → **2b confirmed: full-screen select closing on confirm**. 11.6.1: `CFG.characters` table (order mage/warden/ranger/swash + ghost; D58 costs 0/1500/3500/7500; start weapons wand/garlic/blades/flame, ghost none; **O stat values USER-APPROVED 2026-08-22** — mage 60hp/1.35dmg/275sp · warden 150/0.85/230 · ranger 110/1.0/320 · swash 90/1.15/300 · ghost 100/1.0/265 baseline); `CFG.meta.charKey` LS key. `player.js`: `charDef(key)` (mage fallback so plain objects = solo default), `setCharacter` (unknown ignored), `reset()` = char base hp + starting weapon, `update()` char base speed, `recomputeStats` = char base × passives × meta. `art/characters.js`: new builders `wardenFrame` (58×66 plate armor)/`rangerFrame` (52×60)/`swashFrame` (54×62)/`ghostFrame(color,dy,legL,legR)` (56×64 scalloped sheet, 7-arg ellipse eyes) + `buildRoster(ghostColor)` → 5 sheets `{w,h,shadowR,idle[2],run[4]}` (mage = original `playerFrame`, bit-identity). Tests: startWeapons check → character-weapon check; meta block rebased 120→80 (mage 60+20 = solo default, post-approval); applyCard block moved to `charKey:'ghost'` baseline objects (D59 100/1.0 keeps passive-mechanics asserts unchanged); NEW 11.6.1 test-logic block (23: table invariants/costs/weapons/ghost-baseline, charDef+setCharacter, per-char reset + recomputeStats incl. plain objects) + test-boot roster section (frames/sizes; D62 area: mage 3584 < warden 3828 < brute 3840; tint rebuild no-crash). Gates: 33/33 · 407/407 (+23) · boot PASS runs=4 (maxHp 80 post-approval; 95 pre-approval). **NEXT = 11.6.2 select UI** (solo + co-op greyed-out D56, stats visible D57, D58 unlock shop, `player.setCharacter` at startRun, roster art/flash/shadow swap, D53 profile charKey = 11.6.4) + **11.6b exclusivity closure** (synergy ownership + starting-weapon pre-ownership).

**11.5 DONE (2026-08-22):** 11.5.1 `CFG.run.maxWeapons` 4→5 + pure `weaponCap(n)` (base − (N−1), clamped 1..maxPlayers → 1P=5/2P=4/3P=3/4P=2; synergies never count). 11.5.2/11.5.3 `cardOffers(weapons, passives, synergies, rng, cap, exclude)` — `exclude` = other-owned weapon keys never offered (new OR upgrades), passives never locked, synergy gating per-picker; host `weaponOwner` weaponKey→player (reset per run in `_coopStartRun`, registered at both pick sites, persists through leave = "for the run"), `_ownerExclusion(pl)`, all 3 offer sites pass `weaponCap(players.length)` + exclusion; solo: cap 5 = default + empty exclusion → invariance. test-logic: 11.5.1 block (3) + 11.5.2 block (6) + card-blocks re-based to cap 5 (`duo`→`trio` {inferno,tempest,phoenix}). test-boot 11.5 E2E (live 3P, before c2 leave): host real-pick garlic → `weaponOwner` + no self-exclusion + `_ownerExclusion(pl1)` has garlic; passive pick isolation (remote dicts untouched); remote auto-pick via `_remoteLevelUps` (passives maxed → no garlic ever, new-pick ownership, passives restored + `recomputeStats`); cap ≤3 all players. Gates: 33/33 · 384/384 · boot PASS runs=4 (E2E listed in PASS line).

**11.4 DONE (2026-08-22):** `cardOffers(weapons, passives, synergies, rng, cap, exclude)` in player.js — `cap` = player's standard-weapon slots (default `CFG.run.maxWeapons`), `exclude` = Set of other-owned weapon keys (never offered: new OR upgrades); passives never locked; synergy gating stays per-picker (own dicts). Host sim (`game.js`): `this.weaponOwner` weaponKey→player map (reset in `_coopStartRun`; persists through leave — "for the run"), first-picker registration at BOTH pick sites (`pickCard` + `_remoteLevelUps` auto-pick), `_ownerExclusion(pl)`, all 3 offer call sites pass `weaponCap(this.players.length)` + exclusion. Solo: cap = 5 = default, exclusion always empty → invariance. test-logic 11.5.2 block (6: narrow-pool exclusion, cap 5 vs 4 pool diff, passives-not-locked) · gates 33/33 · 384/384 (+6) · boot PASS runs=4.

**11.5.1 DONE (2026-08-22):** base `CFG.run.maxWeapons` **4→5** (solo runs get 5 standard slots) + pure `weaponCap(n)` in `js/net/coop.js` = base − (N−1), clamped 1..maxPlayers → 1P=5/2P=4/3P=3/4P=2 (synergies never count — existing rule). test-logic: card-blocks re-based to cap 5 (`full` = 5 weapons, `maxed` = 5 maxed weapons, `duo`→`trio` {inferno,tempest,phoenix} — a 2-candidate pool is impossible at cap 5) + 11.5.1 block (3 checks). Gates: 33/33 · 378/378 (+3) · boot PASS runs=4 (level-ups=20).

**11.4 DONE (2026-08-22):** `CFG.coop.leashR` 700 (A2: shared expanded co-op vision radius, from solo 510) + pure `leashClamp(x,y,others,R)` POCS (project into the intersection of radius-R disks — convex, converges; 8-iter cap + early break) in `js/net/coop.js` · host `_coopLeash()` in `_playingUpdate` after p/remote updates (live players only; world-margin re-clamp after projection; `players.length<2` guard = solo invariance; clients never sim → see it via snaps) · `_lights()` player radius = leashR when co-op (every player sees every other; solo keeps 510) · test-logic 11.4 block (8: config expand, no-op/coincident/exact-R unchanged, 1-disk → on-circle, 2-disk → lens, 3-disk 4P worst case) + test-boot E2E (teleport seat 2 to 1.5R/0.75R offset → pairwise ≤ leashR convergence poll over 3P sim) · gates 33/33 · 375/375 (+8) · boot PASS runs=4 (2× consecutive, no WS flakes).

**11.2 DONE (2026-08-22):** host-authoritative sync — `js/net/sync.js` (SNAP_V=1, key tables in CFG order, playerSnap 26 slots r1 + applyPlayerSnap, enemySnap [sid,typeIdx,x,y,hp,maxHp,frame,flags] + E_FLAG_*, pickupSnaps stable pool slots, stateMsg time=r2, unpackState validates v/step/lengths); `conn.js` CoopConn (browser WS wrapper). combat.js/enemies.js/player.js: owner kill-credit (`onKill(e,killer)`), per-player weapon timers on Player, enemies players-array update + `sid`/`seq` + nearestPlayer. game.js co-op: attachNet (browser-only, `sendHello(levelKey, profileFromMeta(meta))`)/`_netMsg`/`_netRoster` (reconcile remotes from per-seat D53 profiles; re-broadcast runstart on mid-run join)/`_remotePlayer` (profile → meta fields)/`_netInput`/`_coopStartRun` (players=[p]+remote, sendRunStart)/`_remoteStep`/`_remoteLevelUps` (auto-pick cardOffers 4-arg)/`_coopBroadcast` (stateMsg(_step++) host+PLAYING)/`_startClientRun` (shared-seed world)/`_clientUpdate` (local axes → sendInput every step, NO sim)/`_interp` (last-2-snapshot arrival-time lerp, `CFG.coop.interpLag`, seat-indexed)/`_netState`/`_netClosed`; solo invariance: every hook guards on `net`/`netRole`. serve.mjs: roster `players:[{id,seat,profile}]` + state/runstart relays (arr caps 4/512/512). UI: #btn-coop + #coop-status + #btn-coop-leave. Gates: 33/33 · 360/360 (+22) · boot PASS runs=4 + **11.2 E2E** (real serve.mjs relay, ephemeral port 0, host+2 client Games over native WS: shared-seed world, state relay v/step, enemy snap applied, client pos tracks host within r1+interp, KeyD keydown-only drives host remote, c2 leave → roster reconciles → run continues). **NEXT = 11.3 difficulty scaling** — apply `coopScale` helper in the host sim: enemy HP/damage × (1+0.33·added players), spawn batch/interval/aliveCap, Wraith boss ramp; config scalars; solo (1P) must stay ×1.0.

**11.1 DONE (2026-08-22):** zero-dep WS upgrade + frame codec in `tools/serve.mjs` — import-safe (entry guard: `resolve(argv[1]) === fileURLToPath(import.meta.url)`; tests import it side-effect-free); `wsAcceptKey` SHA-1 (RFC 6455 vector), `encodeFrame` (unmasked, 7/16/64-bit len), `consumeFrames` (masked client frames, partial reassembly, 64 KB max-payload, fragmented → `bad-opcode` error); `attachCoopRoom(server)` = the one room: first hello opens (host seat 0, levelKey from host hello), join/leave/full/host-leave-close, input/state/runstart relayed to the OTHER seated clients, roster broadcast on join/leave. Pure protocol/room/D53 `profileFromMeta`/`coopScale` in `js/net/coop.js`; `CFG.coop {maxPlayers:4, perPlayer:0.33}`. test-logic 11.1 block (room semantics, profile, codec) + test-boot E2E (real server on ephemeral loopback port 0 + Node-native WS clients: host seat 0 / seats in join order / 5th→full / leave+roster / host-leave→closed / room re-opens). **NEXT = 11.2 game integration** (protocol/relay in place; host browser must run the sim for N players: Game co-op mode + remote `Player`s from D53 profiles, remote inputs applied per step, per-step `state` snapshot broadcast via the relay, shared seed from host `runstart`, client-side interpolation + no local sim; solo invariance guard 11.11 stands).

**13.12 DONE (2026-08-22):** all gates green (30/30 · 316/316 · boot PASS runs=4) — level defs/unlocks/stat tables/scores/FLAVOR in test-logic; M02/M03 real runs + unlock flow + boss + victory + Settings E2E in test-boot; mobile parity = `.lv-card`/`.set-row` min-height 72 px + touch zoom default 0.80 (CSS-verified).

**13.11 DONE (2026-08-22):** `music.js` FLAVOR data + 5 flavor voices on the 10.6 lattice — higan: temple bell D2 + 2.76× inharmonic partial (1/loop), chime pings D5/F5/B4 (3/loop, dark-delay send), taiko base 6/loop → boss mode 8/loop (s8 0/4, 55 Hz, gain ×1.6); drowned: 3 rising bubble chirps (0.6→1.0× over 0.1 s) + 1 whale glide (160→240→150 Hz, lowpass 600, 3.5 s); m01 = no FLAVOR entry → zero flavor oscs (10.6 invariance re-verified 49/60/96/294); muffle = level→lowpass→musicBus (`CFG.audio.muffleOff` 12k pass-through, `muffleM03` 600 Hz, retuned in `startMusic`); gains bell .05/chime .04/taiko .09/bubble .03/whale .06; test-logic: FLAVOR shape block + 4 fake-ctx pumps (m01 / higan / higan-boss / drowned, t0 cutoff excludes margin-scheduled loop-13 bar-0 hits).

**13.10 DONE (2026-08-22):** tints via `gemHeartFor(levelKey)` in `items.js` (GEM_PAL/HEART_PAL; m01 = original palette, unknown key → m01); reskin seam = `game._reskinPickups()` (construct/startRun/previewLevel); banner = game.js detects the newly-unlocked level in `_gameOver` (before/after `isUnlocked` around `recordWin`) and emits 'NEW MAP UNLOCKED' after DAWN BREAKS; screens.js queues concurrent banners (drains after `CFG.ui.bannerMs` in the per-frame `update` — **the drain is real-time/CSS-bound: verified in browser (2.9), gates only verify emission order + once-at-threshold**); game-over row = 'Unlocks' dt/dd with next locked level's name + X/3 wins.



**10.9 Game-wide optimization pass (LAST)** — (a) opt-in update-vs-render + per-stage timers (no shipping cost); (b) worst-case load: all weapons max + synergies + end-run `aliveCap` + Wraith boss; (c) audit: viewport culling (decor culls via `world.cullPad` — verify enemies/projectiles/flames/particles too), pooling (projectiles/bombs/flames arrays, per-frame allocations), DOM text writes (already change-detected — verify), canvas state churn, lighting cost; (d) fix top offenders; (e) tick note MUST include before/after numbers. **Deferred from 10.4:** sprite-ify lighting `createRadialGradient` holes/glow (~24–36/frame), flame `lighter` pass (~130 drawImages/frame — dominant ops noise), cull `combat.draw`/`drawOrbs`. 2.9 user sign-off doubles as the framerate acceptance test. Post-10.5 bench baseline (measure 10.9 against THIS): render ~0.148 ms/frame · drawImage ~804/frame (10.4 was 0.118/678 — spawn band puts more enemies in viewport).

**Pitfalls (active):**
- m02+ decor convention: `put(o,k,s,w,h)` w/h MUST equal the sprite canvas size (draw is unscaled, offset uses w*s); every decor key must exist in `terrain.sprites` (boot `decor.every(d=>d.img)` is the only guard); new m02 tiles use their own rng streams (`mulberry32(4242+i*97)`) — never disturb m01's `mulberry32(4242)` first stream in `buildTerrain` (golden + bit-identity).
- FP exactness: `20*1.56` is NOT `31.2` (lands 31.200000000000003) — diff-scaled stat asserts need epsilon (`near()` in test-logic, explicit in test-boot).
- Profile before perf fixes — the user's multi-hit guess was unverified (actual root: no viewport culling, 10.4); before/after numbers are the contract.
- `cardOffers(weapons, passives, synergies, rng, cap, exclude, ghostOffers)` is 7-arg — a wrong arg count silently shifts `rng`/`exclude` and crashes or mis-filters.
- `applyMeta` does NOT recompute stats — caller: `applyMeta(p, meta); recomputeStats(p); p.hp = p.maxHp;`.
- DoT ticks via `combat.dpsTick` (no flash/knockback; deaths still route through `onKill`) — never `damageEnemy` (white-flickers burning foes every frame).
- Every CFG `icon` name must exist in `buildIcons()` — card DOM draws `icons[def.icon]` unguarded (crash class).
- New canvas API with strict arg requirements → extend the `test-boot` `makeCtx` stub (it validates `arc`/`ellipse` arg counts + radii; everything else is a silent no-op) or the gate passes while the browser throws (Decision 17).
- New one-shot test-boot paths must self-verify: final assert on every one-shot flag (Decision 16).
- Gem SFX still inaudible after 10.8? Raise the recipe peak (within the compressor), don't add layers — GAPS already bounds burst volume.
- Music loop seam is the classic failure — verify pattern math + lookahead wrap in Node, not by ear.
- WS test clients: frames can be delivered batched (multiple messages in one IO task) — a `wait` registered between awaits misses event-queued predicates; poll a message inbox instead (11.1 E2E `mk()` pattern in test-boot). `serve.mjs` stays import-safe (no listen on import) — that's how test-boot drives the real server on an ephemeral port.
- Live-pump E2E ownership asserts: never assert a strict `weaponOwner` key COUNT — remote auto-picks during the pump (`_remoteLevelUps`) legitimately add entries; assert the specific true first-pick keys map to their pickers (11.5 flake fix precedent; 11.6.3 section flake 2026-08-22).
- Co-op E2E at scaled density (11.3): idle seats die inside the 25 s pump and the host parks on LEVELUP (own player levels from the extra kills) — co-op pump loops call `keepAlive()` (revive + `pickCard(0)`); position-tracking asserts must be convergence polls (knockback transients), not one-shots.

## Decisions (binding)

| # | Decision | Rationale |
| --- | --- | --- |
| 1 | Vanilla ESM, zero deps, zero build | Requirement: HTML/CSS/JS only, modules where it helps |
| 2 | Bounded world 4200×3200 + minimap | Finite landmarks + minimap need a bounded map |
| 3 | All audio = Web Audio synthesis | No asset files allowed |
| 4 | Port 47893 only, bind 0.0.0.0 | Reachable outside the sandbox; obscure-port rule |
| 5 | Touch steer = pointer-drag anywhere on canvas (floating stick); `K` dash alias | UX decision; README matches implementation |
| 6 | Lighting = per-light radial gradients over cached half-res canvas; flicker phase = hash of light pos (stateless) | `destination-out` holes per light; cheap at ~15 lights |
| 7 | Screens = per-frame `game.state` poll; bus = transients only | `toMenu()` emits nothing; polling covers auto-pause |
| 8 | Mute owned by `hud.js` (LS `qsurv.mute`, toggled on 'mute'); audio re-reads, never writes LS | Single toggle point (key M + button funnel through Game) |
| 9 | `#touch-ui` gated behind `body.touch` (dash button touch-only; desktop dash = Shift/Space/K/right-click) | Phase 1 hint design |
| 10 | Audio = exactly 2 modules; `sfx.js` owns the lazy `AudioContext` + shared graph | Single graph source; music/amb reuse the SFX noise buffer |
| 11 | `startRun()` sets `input.gesture` | Menu Start is a DOM tap; canvas-only pointer listeners miss it → audio would stay locked on mobile |
| 12 | `serve.mjs` listens unconditionally | argv[1]-vs-`import.meta.url` gate silently no-opped under Git Bash |
| 13 | Touch detect = `pointer: coarse` ‖ `ontouchstart` → `body.touch` + DPR caps (2/1.5) | One detection point drives CSS gating + DPR |
| 14 | Game canvas `alpha:false`; minimap ctx raw 1× (no DPR) | Canvas fully repainted every frame; DPR would break the 264×202 1:1 minimap mapping |
| 15 | Third gate `tools/test-boot.mjs`: Node, stubbed DOM + browser-strict canvas, real `Loop`, full menu→death→Upgrades→victory→menu sim | Import + logic gates passed while boot was dead in the browser |
| 16 | test-boot one-shot paths self-verify (final assert on every flag) | A sub-test that never fired must fail the gate |
| 17 | test-boot ctx stub validates `arc`/`ellipse` arg counts (`ellipse` exactly 7) + radii, negative-tested | 21-site `ellipse()` 6-arg crash passed all gates until the stub enforced it |
| 18 | Spawner batch floor 1→2 | Camped player took zero early damage — run winnable with no input |
| 19 | Synergies = third card kind, single-level, gated on `requires`-all-max, own namespace (NOT counted in `maxWeapons`); `cardOffers` 4-arg | User spec: fused card only once both sources maxed |
| 20 | Meta = pure `core/meta.js`, LS `qsurv.meta.v1`, shards = `floor(score/400)` + 25 victory; `applyMeta` sets multipliers only | Node-testable; recompute split avoids a meta↔player import cycle |
| 21 | Burn/blight DoT via `dpsTick` (no flash/knockback; deaths through `onKill`) | `damageEnemy` would white-flicker burning foes every frame |
| 22 | New-weapon SFX on bus events `pistol`/`boom`/`fire` via `combat.pulse`, tight GAPS | Flamethrower fires ~60×/s; combat stays decoupled from audio |
| 23 | Bomb = 0.55 s parabolic lob → `fuse` pause → AOE (fuse shortens per level) | User spec |
| 24 | Flamethrower fuel model: `fuel` s of fire, lengthy `recharge`, fuel bar above player, per-enemy hit cooldown | User spec: flow-y trail, limited fuel |
| 25 | 10.9 optimization pass slotted LAST | Measure/fix the final feature state; 10.4's harness + root causes feed it |
| 26 | Empty card pool → auto-skip LEVELUP (level still granted) | User-reported softlock; filler cards would violate the exact-description rule |
| 27 | Co-op transport = zero-dep WebSocket upgrade in `tools/serve.mjs` (port 47893 only, 0.0.0.0, LAN); host-authoritative: host runs the 60 Hz sim, clients send inputs, host broadcasts state per step, shared world seed; **web play (internet) = research task 11.13** (Pages is static — cannot host WS rooms; A4) | Port rule (47893 only) + zero-deps rule; the local server already exists and is LAN-reachable |
| 28 | 4 playable characters + hidden **ghost** fallback; each: unique palette/silhouette + **own base stats** (visible in select) + unique default starting weapon (config table; `art/characters.js`); archetypes: glass-cannon mage / tanky lower-medium damage / fast balanced ranger / upper-medium swashbuckler (respec 2026-08-22; D56–D61) | User spec (req 5; revised 2026-08-22) |
| 29 | **Weapon + synergy** exclusivity = per-run, first-pick-wins, one owner per weapon/synergy (owner = only player who may see it or its upgrades); starting weapons pre-owned (no owner registered); passives unaffected (each player holds own copy) (revised 2026-08-22) | User spec (req 4; extended 2026-08-22) |
| 30 | Difficulty ramp = **+33% per added player** (max 3 added, 4 total; ~×2.0 at 4P) on **enemy HP, damage to players, spawn count/on-screen** (batch/interval/alive cap) | User spec (req 2; A1) |
| 31 | Leash = shared **expanded co-op vision radius** — every player held within that radius of every other (all players see all players); new config value (expanded from solo 510) | User spec (req 3; A2) |
| 32 | Level-up picks are **player-scoped**: each pick affects only the picker (no shared buffs); passives NOT locked (anyone may pick); synergy availability follows per-player weapon max | User spec (A3) |
| 33 | Corner → player assignment = **join order** (1st→TL, 2nd→TR, 3rd→BL, 4th→BR) | User spec (req 6c; A5) |
| 34 | Per-character unique starting weapons: **Moonbolt Wand, Wraith Garlic, Aegis Blades, Pyre Lance** — pre-owned, never "picked" (no owner registered); **ghost**: NO default weapon — each ghosted player instead gets 2 UNIQUE starting-weapon card offers (never duplicated across players) (revised 2026-08-22) | User spec (req 5; A6; revised 2026-08-22) |
| 35 | Final boss count = **N Wraiths for N players** (1P=1 … 4P=4); Wraith stats get the same +33%/player ramp as other enemies | User spec (Q7 resolved 2026-08-21) |
| 36 | 11.13 web-play research protocol: verify real-world date BEFORE web research (standalone, non-batchable, non-negotiable step); sources-cited, evidence-based best practices for self-hosted, fully game-state-synced web-game multiplayer; **100% cost-free solutions only** | User spec (2026-08-21) |
| 37 | Roster = **10 weapons** (add Bow & Arrow, Snowball Launcher, Ring of Chain Lightning) + **9 synergies**; all Phase 11 co-op rules apply to the new weapons unchanged | User spec (2026-08-21) |
| 38 | 4 new synergies: **Flaming Arrows** (Bow+Flame), **Heart-Piercer** (Bow+max-HP passive: greater damage + pierce, pierce scales with Bow level), **Blue Flame** (Snowball+Flame: freeze + burn DoT), **Storm Volley** (working name; Pistols+Lightning Ring: strike every 4th shot + proposed chain/shock extras) — single-level, `requires`-all-max per Decision 19 | User spec + invited proposals (2026-08-21) |
| 39 | New statuses (slow→freeze stacks, shock→stun+chain-burst stacks, blue flame = frozen+burning) tick via the Decision 21 pattern (never `damageEnemy` — no white flicker) | Combat pipeline invariant |
| 40 | Synergy cards = **5 levels** (same stage count as standard weapons), remain in the offer pool until their own max, per-level effect scaling (Heart-Piercer pierce, Storm Volley strike scale with the synergy's own level) — supersedes Decision 19's "single-level" | User spec (2026-08-21) |
| 41 | Synergy offer-pool gating = **pair-specific**: a synergy becomes eligible the moment ITS OWN `requires` sources (2 weapons, or weapon+passive) reach max — NEVER gated on all of the player's weapons being maxed | User spec + confirmation (2026-08-21) |
| 42 | Co-op equip cap: each player's max equipable standard weapons = **5 − (N−1)** (1P=5, 2P=4, 3P=3, 4P=2); base `maxWeapons` raised 4→5 so a 4P player can still own 1 weapon pair → paired synergy achievable; **synergy weapons NEVER count toward the cap** | User spec (item 3 + follow-up, 2026-08-21) |
| 43 | Slow/shock status stacks have **5 s per-stack TTL** (independent per-stack expiry; config scalar, expected playtest tuning) | User spec (item 4, 2026-08-21) |
| 44 | **Slot-based enemy roster across levels:** the 6 roles (small chaser/fast flyer/medium chaser/fast chaser/large brute/ranged) + boss keep their mechanics; each level re-skins + re-stats per slot. User-spec skins — Map 02: Oni (brute), Ryū dragon (boss); Map 03: goldfish (flyer), mer-people (medium chaser), stingray (fast chaser), electric eel (ranged), orca (brute), Great White Shark (boss). **Approved (A1/A2):** Map 02 remaining slots = tanuki/hō-ōi/shikome/kitsune/miko; Map 03 small chaser = crab | User spec (2026-08-21); keeps AI/spawner/test surface stable |
| 45 | Level scales: Map 02 = 4200×3200 (same as Map 01), Map 03 = **1.5× area ≈ 5145×3920** (√1.5 ≈ 1.225 linear, aspect preserved) — config scalars (revised A3) | User spec (2026-08-21) |
| 46 | Unlocks: cumulative wins, victory-only (deaths don't count), no reset — Map 02 = 3× Map 01, Map 03 = 3× Map 02; persisted in LS (meta.js pattern) | User spec (2026-08-21) |
| 47 | Level select inline on main menu; locked levels visible + non-selectable + show requirement + X/3 progress; ≥72 px touch targets | User spec (2026-08-21; mobile parity rule) |
| 48 | Mobile view zoom = camera-view factor (default **0.80** → 1/0.80² ≈ 1.56 area; config scalar; menu backdrop too); HUD DOM + minimap stay 1×; gated by existing touch detection (D13); **new Pause-menu Settings section: zoom toggle (0.80 ↔ 1.0, all devices, persisted) + mute toggle — mute REPLACES the HUD `#btn-mute` button (M key + `qsurv.mute` LS unchanged, D8)** (revised A6) | User spec (2026-08-21) |
| 49 | Map 03 foreground particles = small RISING bubbles (replace falling snow/petals; drift upward) | User spec (2026-08-21) |
| 50 | Per-level difficulty chained **×1.25 per level step** (M02 ≈ 1.25× M01, M03 ≈ 1.56× M01) on enemy HP, damage to players, spawn batch/interval/alive cap (same levers as Phase 11 A1) | User spec (A4, 2026-08-21) |
| 51 | A5-approved Phase 13 flavor: per-level high scores (one LS key per level, `v1` = M01), per-level audio/music, per-level gem/heart tints, per-level menu backdrop preview, segmented dragon tail, koi pond (M02) + fish schools (M03), unlock banner, denied blip + card shake, eel lightning sprite, shark belly-flash. **NOT approved (deferred — do not build without asking):** first-clear Soulshard bonus, per-level shard multipliers, per-level victory lines, pufferfish skin, keyboard 1/2/3 select, per-level boss intro moment | User spec (A5, 2026-08-21) |
| 53 | Meta progression (Soulshards + Upgrades, `qsurv.meta.v1`) is **player-specific**: never carried over to, shared with, or applied to other players during co-op sessions; each client applies its own local meta to its own character; run shard earnings are **unified per run — all active participants earn the same amount** (D54); each client still accrues only to its own local LS; host sim receives each player's meta-derived stat profile at join (handshake) for accurate simulation — sim input only, no meta in per-step snapshots | User spec (2026-08-21; earnings clause revised 2026-08-21) |
| 54 | **In-run Soulshards counter + unified co-op earnings:** one unified, live HUD Soulshards counter visible **during runs only** (no per-player counters); all active players in a run receive the **same** Soulshard amount — host sim computes one run-level total and each client accrues it in full to its own local LS (supersedes D53's shard-earnings clause; meta stats/upgrades stay player-specific); counter source = live projected award `floor(score/400)` (+25 on victory) — no economy change (O1=A) | User spec (2026-08-21; O1 resolved 2026-08-21) |
| 52 | Phase order (A7): **10.7/10.8 → Phase 13 → Phase 11 → Phase 12 → 10.9 (LAST, D25) → 2.9** — level framework lands before co-op's shared-world sync (no retrofitting the level key into shipped networking); co-op before weapons (12.8 already extends the 11.12 multi-client sim 7→10 weapons) | Best-practices judgment (A7, 2026-08-21) |
| 55 | **Per-level plumbing (13.1):** level def = source of truth for w/h/margin/weights/boss/diff; `World.generate(seed, levelKey)` syncs level bounds into `CFG.world` (single seam — entity hot paths keep reading `CFG.world`); spawner fns take a `level` param (default m01) scaling batch×diff, interval÷diff, aliveCap×diff; m01 output bit-identical (golden counts 276/335/328 pinned in test-logic, seed 20260820); m02/m03 `layout`/`weights`/`boss` = null until 13.2–13.5 | Best-practices judgment (2026-08-21) |
| 56 | **No sharing in co-op (binding):** every player in a session selects a UNIQUE character (no duplicates; already-selected characters greyed-out in select); every player holds their own cards/effects/upgrades — **no weapon/synergy is ever shared across players** (owner = only picker; 11.5b); character select is ALSO available in solo | User spec (2026-08-22) |
| 57 | **Per-character base stats:** the 4 archetype stats (D28) MUST be clearly detailed/explained/visible in the character select menu; solo runs also use per-character stats (11.11 invariance = no co-op overhead, NOT Phase-10 stat identity) | User spec (2026-08-22) |
| 58 | **Character unlock progression (RESOLVED 2026-08-22):** Soulshard-cost character shop (same Soulshard economy as Upgrades; distinct mechanism from the map 3×-victory unlocks, D46) — **Mage = default** · **Ranger = 1500** · **Warden = 3500** · **Swashbuckler = 7500**; locked cards show cost + visible requirement; spend persisted (meta.js pattern) | User spec (2026-08-22) |
| 59 | **Ghost fallback (binding):** faceless ghost character — NO default weapon, baseline average stats — assigned to ALL players when every co-op lobby member has ONLY the starter character unlocked; on entering the run each gets **2 UNIQUE weapon-card offers (never duplicated across players)** to pick their starting weapon | User spec (2026-08-22) |
| 60 | **Roster modularity (binding):** new characters AND new weapons/synergies must add with no gameplay/combat/sync code changes — data-driven config tables + one art builder each (`art/characters.js` / `art/items.js`) | User spec (2026-08-22) |
| 61 | **Select-UI research step (11.6.0, binding):** VERY BRIEF, evidence-based, **sources-cited** research of current best practices for character-select UI (pop-up submenu vs full-screen select) before implementing; decision defaults to 2b (full/mostly-full-screen select closing on confirm) — solo + co-op select both integrate the greyed-out-taken rule (D56) | User spec (2026-08-22) |
| 63 | **User-input documentation = capture-before-reason (binding process rule, 2026-08-22, v4):** any non-denylisted human-user message → the FIRST tool call of the turn appends to `docs/USER-INPUT-LOG.md` (insert after the `## Entries (newest first)` line; no prior read) — no analysis/planning/research/implementation may precede the write; entry = user's COMPLETE message, verbatim, unmodified, no omissions; after the write, act on it + reference by title (never re-quote). Denylist: **session kick-off prompt (exact text, leading banner optional)**; bare control words (unless answering a pending agent question); commonly-repeated template prompts; explicit user opt-out; agent-side content. **Precedence:** denylist match → never document (doubt tiebreaker does NOT apply to denylist matches — template text is recoverable); template + substantive additions → document; otherwise when in doubt, document (false capture = one line; miss = unrecoverable). Rationale (measured): context recall degrades with token position + total length (Chroma context-rot 18-model study; RULER; lost-in-the-middle); compaction silently drops critical items whose importance surfaces later (Anthropic context engineering). v4 fix: new session erroneously logged the kick-off prompt (tiebreaker out-argued the category-label denylist) → exact-text entry + explicit precedence | User rule (2026-08-22, v4; v3 sources cited in session) |
| 62 | **Character roster resolved (2026-08-22):** **Mage** = original/default sprite (starter; glass-cannon); **Warden** = NEW sprite — LARGER + heavily armored (> original player sprite size, < brute-enemy sprite size) + **SLOWEST move speed of the 4** (tanky, lower-medium dmg); **Ranger** = new fast sprite (balanced); **Swashbuckler** = new agile sprite (upper-medium dmg); **ghost** = sheet-ghost appearance (traditional "sheet") with per-player UNIQUE Pacman-ghost color that dictates that player's in-run UI colors (11.8 channel) | User spec (2026-08-22) |
| 64 | **11.13 multiplayer transport = Tailscale on the user's Synology DS124 NAS (DECIDED 2026-08-22):** NAS joins a free Personal tailnet (≤6 users, Tailscale installed + connected 2026-08-22, user-confirmed) + runs **unmodified** `tools/serve.mjs` (page + room relay, port 47893 only) as a boot-start task; remote players install free Tailscale, join the tailnet, open `http://<NAS-tailnet-IP>:47893` — **zero game-code change**; NAS = relay only (60 Hz sim still runs in the host player's browser; 1 GB/ARM fine — relay-only load); alternatives ranked: B CF Durable Objects relay + input coalescing → C Oracle free VPS (WebRTC rejected); full record: `docs/RESEARCH_FINDINGS.md` §4-5 (Option A2) | User spec (2026-08-22; research 2026-08-22, date-verified-first per D36) |
| 65 | **Selectable run durations (Phase 17):** **5 (default) / 10 / 15 / 20 (max set-time) / ENDLESS** (until death); boss events at **4:00 / 9:00 / 14:00 / 19:00** + **every 5:00 during ENDLESS**; user-confirmed example: 15-min run = exactly 3 bosses (4:00, 9:00, 14:00); 5:00 default = current behavior (solo invariance); per-level persistence; O at 17: 20-min 19:00 confirm, ENDLESS fixed-slots-vs-cadence, co-op N-bosses-per-event, per-level boss per event, ENDLESS victory/high-score semantics | User spec (2026-08-22) |
| 66 | **Passive-start rule (Phase 20):** players start runs with **NO passives** — passives are acquired only via level-up picks (user: also removes multiplayer passives/items overlap issues); character starting weapons unchanged (D34) | User spec (2026-08-22) |
| 67 | **Level-up actions SKIP / BANISH / RE-ROLL (Phase 18):** level-up SCREEN ACTIONS (explicitly NOT equippable items, NOT selectable cards, NOT level-up-able mid-run) — each **initially unlocked via the meta-progression store in the Main Menu**, then **upgraded there for additional uses — max 5 uses/run/each**; player-scoped in co-op (D32 pattern; per-picker banish exclusion); VS-model research: `RESEARCH_FINDINGS.md` §7 (Skip/Reroll/Banish = level-up-screen stats in VS; skip→XP ~66% next level per user / 20% per Reroll-Banish use per wiki; reroll excludes discarded set from redraw only; banish removes item for rest of run); O at 18: skip-XP ratio, banish granularity/owned-item freeze, reroll exclusion, Soulshard cost curve | User spec (2026-08-22) + VS research (2026-08-22, user-corrected) |
| 68 | **Equipment HUD row (Phase 19):** equipped weapon + item icons with per-weapon **level numbers** displayed **beneath the character XP bar**, live all run; **Pyre Lance fuel bar moves beneath the player character** (revises D24's "above the player") | User spec (2026-08-22) |
| 69 | **Extended roster backlog (Phase 21), verbatim PLAN §3.13:** 9 weapons (Tank Cannon · Continuous laser beam · Wolf summon · Rolling Boulder · Web-slingers · Gatling Gun · Baseball Bat · Frog Tongue · Cannonball) + 2 run-equipable items (Mirror Shield · Ice Skates) + 5 characters (Werewolf · Stone Golem · Baseball Player · Giant Toad · Wild West Gunslinger); specifics deferred by user ("document as-is for now") — O's per sub-step; lands via D60 data-driven modularity + Decision 21 status pattern; all Phase 11 co-op rules unchanged | User spec (2026-08-22) |
| 70 | **Playtest defects (Phase 16, user report 2026-08-22):** (1) Map 01 falling snow no longer visible (regression since Phase 13 per-level particles — reproduce/root-cause first); (2) all weapon projectiles appear to originate from the player's FEET → common spawn origin ~mid-torso (config scalar); (3) Pyre Lance flame: **+33%** total length (vs current build), flow/extend **≥2×** faster, must LEAD the aim direction while moving (not trail — user hypothesis: directional momentum/inertia; approach = agent O). **16.3 supersedes 12.1's length/origin/speed scope** (12.1 revised = post-16.3 verification) | User report (2026-08-22) |
| 71 | **Queue revision (2026-08-22):** 11.8 → **Phase 16 defects** → 11.9–11.12 → 11.13 transport impl → **Phase 12** (12.1 revised) → **Phase 20** → **Phase 15** → **Phase 17** → **Phase 18** → **Phase 19** → **Phase 14** → **Phase 21** → 10.9 (LAST) → 2.9 — rationale: user-reported defects before further co-op HUD work (16.2 origin fix also lands before Phase 12 weapons); 20 before 15 (offer-coverage measurement must baseline on the no-starting-passives economy); 15 before 18 (fix the offer pool before adding level-up actions on top); 21 (biggest scope, all-new content) after the economy/UI phases | Best-practices judgment (2026-08-22) |

## Session Log (one line per session, newest first)
- **2026-08-22 — 16.2 projectile spawn origin feet→mid-torso (code + tests + gates; overnight session):** root = `Player`'s `(x,y)` IS the feet (sprite bottom-anchored) and all 5 fire sites used it for aim + spawn → `CFG.combat.spawnOriginFrac` 0.55 + `Player._spawnY()`; wand/axe/pistols/bombs/flame aim AND spawn from mid-torso (bomb lob dist + flame `_flameAng` too); axe return + orbiter center (dmg/knockback/draw) → mid-torso; garlic stays feet-centered (ground pulse, documented); **co-op snapshot-neutral verified** (no projectile key in the sync protocol at all — host-sim-only); **latent-fix: blade orbiters + garlic ring were NEVER drawn** (`combat.draw` read never-assigned `this.player`/`this.orbitT`/`this.garlicT` — garlic radius would have been NaN → browser `arc()` throw) → `draw(ctx, t, players)` per-live-player on sim `_orbitT`/`_garlicT` + client cosmetic clock advance in `_clientUpdate` (remotes: no angle in snapshot → not drawn); test-logic +4 (480/480) + test-boot E2E (5-weapon exact mid-torso origins, blade orbit discrimination on a manually-populated grid — grid only rebuilds in `enemies.update`, first boot run failed `mid hit=false` for exactly this reason; combat.draw blade sprites + 1 garlic arc) · gates 33/33 · 480/480 · boot PASS runs=4 → next **16.3 Pyre Lance flame** (+33% length / ≥2× flow / LEAD-while-moving).
- **2026-08-22 — 16.1 m01 falling-snow regression (reproduce → fix → non-regression; overnight session):** D70 reproduce-first — new test-boot `probeSnow` (ctx-stub path-op deltas across one `snow.draw`; `drawOps` gains `ellipse`) quantified the repro BEFORE the fix: **run-1 m01 in-game snow pass drew `arc=0 ellipse=0 fill=90`** (90 fills on EMPTY paths — `beginPath()` with no path command, silent no-op in canvas AND the stub = invisible) → git forensics: **13.4 `a8c332b` swapped the snow `else ctx.arc(...)` for `else if (bubble) {...}`** (13.2's check only asserted the `snow.kind` ROUTING, never broken; kind routing + motion were fine all along) → fix = snow arc restored exactly as v1.0.0 `03eeac2` (one branch in `Snow.draw`) → non-regression probe at 6 sites (run-1 m01 in-game + m01/m02/m03 menu backdrops + m02/m03 run starts; per-kind counts snow 90 arc/90 fill · petal 90 ellipse/90 fill · bubble 180 arc/90 stroke/90 fill) · gates 33/33 · 476/476 · boot PASS runs=4 (10.4 bench baseline shifts — m01 renders now really draw the 90-flake pass) → next **16.2 projectile spawn origin feet→mid-torso**.
- **2026-08-22 — 11.8 co-op theming (code + tests + gates; overnight session):** per-char UI theming (D62 channel, req 6b/6d) — `CFG.characters[*].accent` + pure `charAccent(key,seat)` in coop.js (ghost → per-seat Pac-Man tint; unknown → starter fallback) · hud.js `applyTheme` → `--char`/`-soft`/`-line`/`-inset`/`-grad` on local roots (`#hud` + `#touch-ui`, change-detected; local key = seat-0/`chars[0]` co-op, `game.charKey` solo) + each seat-panel root on char (re)assignment · main.css: HP fill `.hp-bar .bar-fill` themed (**fixes 11.7 latent: seat-panel fills had NO background**) + dash icon/border/ring on `#btn-dash`/`#btn-dash-hud`/`.seat-dash` with legacy fallbacks · test-logic +7 (476/476) + test-boot E2E (ghost run: local + seat-1/2 = Blinky/Pinky/Inky tints; 3P: Mage local + Warden/Ranger accents + 4th joiner Swash) · gates 33/33 · 476/476 · boot PASS runs=4 → next **Phase 16 playtest defects** (16.1 m01 snow — reproduce FIRST per D70).
- **2026-08-22 — design batch documentation: playtest defects + new phases 16/17/18/19/20/21 (docs only, no code):** user input captured (log: “Batch design input: gameplay/weapon/item/character ideas + 3 bug reports (document only)” + “VS DOES have skip/reroll/banish…” correction) → VS level-up-action research (`RESEARCH_FINDINGS.md` §7: wiki Skip/Banish/Reroll pages + 4 corroborating sources; user-corrected model: level-up-screen STATS, not cards; skip→~66% next-level XP per user / 20% per Reroll-Banish use per wiki; reroll excludes discarded set from redraw only; banish = rest-of-run removal) → new PLAN §3.10 (run durations 5/10/15/20/ENDLESS + boss schedule 4:00/9:00/14:00/19:00 + every 5:00 endless; 15-min = 3 bosses) · §3.11 (SKIP/BANISH/RE-ROLL — meta-store unlock, max 5 uses/run/each, NOT equippable/selectable/level-able) · §3.12 (equipment icons + level numbers under XP bar; fuel bar beneath player — revises D24) · §3.13 (9 weapons + 2 items + 5 characters VERBATIM) + §3.4 passive-start + §3.7 persistence + 3 risk rows → new PROGRESS phases 16 (defects: m01 snow · projectile origin feet→mid-torso · flame +33%/≥2×/LEAD — supersedes 12.1) · 17 (run durations) · 18 (level-up actions) · 19 (HUD) · 20 (passive-start, before 15 for economy baseline) · 21 (extended roster) + D65–D71 + queue revision (11.8 → 16 → 11.9–11.13 → 12 → 20 → 15 → 17 → 18 → 19 → 14 → 21 → 10.9 → 2.9); no code changes, gates not re-run (docs-only).
- **2026-08-22 — 11.7 co-op HUD corners (code + tests + gates; post-compact session):** `#hud-left` = ALWAYS the SEAT-0 panel (solo invariance 11.11) + 3 generated corner panels for seats 1/2/3 = TR/BL/BR (`#hud-seat-1/2/3`) — char name + 2× roster/ghost face (per-seat tint cache) + HP bar/label/low pulse + LV badge + XP fill + dash `--cd` ring, display-only (input buttons follow the LOCAL player); visible count = `min(4, players)` in join order (A5); `#hud.coop` CSS + minimap → bottom-center in co-op; `initHud` returns `{ update, panels }`; test-boot E2E (solo: no coop class + TL = local; 3P: TL = host data + TR Warden/BL Ranger named + HP/LV tracked + BR off; mid-run 4th joiner → BR Swashbuckler; leave → roster 3 + BR hidden) — the failing 4th-joiner assert was a missing explicit `hudHost.update()` before it (`hostG` has no render loop in test-boot; the real app's render hook drives the HUD per frame); gates 33/33 · 469/469 · boot PASS runs=4 → next **11.8 co-op theming**.
- **2026-08-22 — 11.6b exclusivity closure (code + tests + gates; post-compact session):** D29 respec done — synergy first-pick-wins (`cardOffers` synergy branch honors `exclude` — one Set serves weapon+synergy keys, namespaces disjoint; `pickCard` + `_remoteLevelUps` register `kind==='synergy'` level-1 picks into `weaponOwner`) + starting-weapon pre-ownership (seeded in `_applyChar` for remotes incl. mid-run joiners + `_coopStartRun` for the host's own starter → starting weapons excluded from OTHERS' offers, owner keeps own upgrades; `_ownerExclusion` gains `!pl.synergies[k]` guard; solo map never populated → invariance); test-logic +5 (469/469: owned synergy never offered at max sources ×3 seeds, unowned control, rng-parity no-perturb) + test-boot 11.6b E2E in the live 3P char-sync run (pre-ownership wand/garlic/blades → their seats + per-seat exclusion; forced host blight pick → ownership + no self-exclusion; pl1 pool = {phoenix only} with cap+passives maxed — `{wand:5,garlic:5,axe:5}` gates blight+phoenix only); test-boot 11.6.3 ghost-section flake fix (first boot run of the session failed the strict `weaponOwner` count assert — a live pump auto-pick legitimately adds a 5th owner entry; same class as the 11.5 flake fix → assert the 4 true first-picks, not the count); gates 33/33 · 469/469 · boot PASS runs=4 (×2) → next **11.7 co-op HUD corners**.
- **2026-08-22 — 11.6.4 co-op character sync/E2E (code + tests + gates; post-compact session):** D53 join profile carries the selected char (`joinProfile(meta, chars, sel)`); pure `selChar`/`assignChars`/`resolveChars` in `coop.js` (seat-order unique, D56; all-starter → every seat ghost, D59 wins) + host `_applyAssign` (replaces `_applyGhostFlow`: per-seat spawn at run start + mid-run re-assignment — taken pick → next unlocked char, else ghost with fresh seeded deal; host entry patched for seat-0 authority) + `_applyChar`/`buildGhost(color)` per-seat tint + client renders host-assigned char (LS untouched) + taken-select greyed/denied (D56) + `_netState` seat guard (state outruns joiner's roster) + `_applyAssign` no-ghost early-return fixed; test-logic +11 (464/464) + test-boot E2E (3P distinct per-seat stats; mid-run late-joiner mage→swash reassignment, client renders it; greyed-select subtest) + 11.6.3 (b) re-baselined to D56; gates 33/33 · 464/464 · boot PASS runs=4 → next **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.3 ghost fallback flow (code + tests + gates):** D59 all-starter lobby → EVERY seat ghosts (faceless sheet, per-seat Pac-Man tint, baseline 100/×1.0/265, NO starting weapon) + 2 UNIQUE starting-weapon offers per seat, never duplicated — pure `sanitizeChars/allStarterLobby/ghostColor/allocateGhostOffers/joinProfile` in `coop.js`; host `_applyGhostFlow()` (run start + mid-run roster; remotes auto-pick `offers[i][0]`, host entry 2-card LEVELUP, `weaponOwner` per pick, mid-run joiner ghosts in) + client self-detect from `netRoster`; two E2E-caught bugs fixed (ghost seats kept starter wand/stale maxHp → `weapons = {}` + `recomputeStats` in ghost branches; `allStarterLobby` called with already-unwrapped profiles → always-ghosted → callers pass roster entries); test-logic +30 (453/453) + test-boot E2E (a) 3P all-starter + mid-run 4th joiner (4 distinct owners) + (b) fresh-room mixed lobby no-ghost; gates 33/33 · 453/453 · boot PASS runs=4 → next **11.6.4 co-op sync/E2E** + **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.2 character select UI (code + tests + gates):** full-screen 2b select (D61) + D58 Soulshard shop + D57 per-card stats + run-start character application — screens.js select overlay (Character button → #screen-select → Confirm closes; per-card roster art ×2 + HP/dmg/speed + start weapon + desc; locked shows `Locked · cost ◆`, one tap buys + selects; denied blip + shake; wallet line; `taken` hook for 11.6.4 greyed-out) · pure meta.js `loadChars/saveChars/isCharUnlocked/buyChar` + `loadSelectedChar/saveSelectedChar` (`qsurv.chars.v1`; ghost/unknown never unlocked/selectable) · `game.setCharacter/buyCharacter` (persisted + roster art/flash/shadow swap; Player ctor from `roster[charKey]`) · index.html button + screen · css `.char-card` · test-logic +16 (423/423) + test-boot E2E (open/lock/buy+select 3500/reject 7500 > 6499/persist/confirm/restore → runs 1–4 baseline intact) · gates 33/33 · 423/423 · boot PASS runs=4 → next **11.6.3 ghost flow** + **11.6b exclusivity closure**.
- **2026-08-22 — 11.6.0 + 11.6.1 characters: select-UI research + roster config/art/stats (code + tests; O USER-APPROVED 2026-08-22 with 3 revisions: mage 60hp/275sp, ranger 110hp/320sp, warden 230sp):** 11.6.0 = 2 sources cited (Wikipedia *Pac-Man* ghost colors → `CFG.ghostColors`; NN/g *10 Usability Heuristics* #1/#3/#4/#8) → 2b full-screen select confirmed; 11.6.1 = `CFG.characters` table (D58 costs; **O stat values USER-APPROVED with 3 revisions** — mage 60hp/275sp, ranger 110hp/320sp, warden 230sp; final: mage 60/×1.35/275 · warden 150/×0.85/230 · ranger 110/×1.0/320 · swash 90/×1.15/300 · ghost 100/×1.0/265) + `CFG.meta.charKey` + `player.js` charDef/setCharacter/reset-start-weapon/per-char-speed/recomputeStats + 4 new art builders + `buildRoster` (mage bit-identical to original playerFrame) + test rebaseline (solo default now mage → meta-L1 maxHp 80 (60+20); applyCard block → ghost-baseline objects; startWeapons → char-weapon check) + new 11.6.1 test-logic block (23) + test-boot roster section (D62 area mage<warden<brute, tint rebuild) · gates 33/33 · 407/407 (+23) · boot PASS runs=4 (maxHp 80) → next **11.6.2 select UI** + **11.6b exclusivity closure**.
- **2026-08-22 — process rule: user-input documentation, capture-before-reason v4 — kick-off false-capture fix (docs only, no code):** v3 session bug (own, 16:05 turn): user directive "add kick-off prompt to do-not-document list" was LOGGED but the AGENTS.md denylist edit never landed → new session (user snippet) then false-captured the kick-off prompt: denylist had only the category label "kick-off", the message's actionable instructions read as substantive, and the unscoped "when in doubt, document" tiebreaker out-argued the denylist → root cause = uncapped tiebreaker + label-not-text match + first-call append mechanics left to the model (snippet deliberation) → v4 fixes: exact-text kick-off entry (banner-tolerant) + **Precedence** line (denylist match → never document; tiebreaker does NOT apply to denylist matches; template + substantive additions → document) + append mechanic (insert after stable `## Entries (newest first)` line, no prior read) → AGENTS.md #8 + D63 (v4) + log-header mirror. NOT logged: user's diagnostic prompt this turn ("DO NOT DOCUMENT" banner).
- **2026-08-22 — process rule: user-input documentation, capture-before-reason v3 (docs only, no code):** user-directed web research BEFORE rule revision (real-world date verified standalone FIRST: 2026-08-22 15:41 EDT) — evidence: Chroma context-rot 18-model study (focused ~300t ≫ ~113k full history on same question → re-injectable verbatim log; lower needle-question similarity degrades faster → verbatim not paraphrase), RULER (nominal window ≠ effective; ~half of ≥32K-claiming models fail at 32K on harder tasks), lost-in-the-middle (mid-context recall), 2026 arXiv (premature termination vs context length; SLEUTH context dilution + protocol adherence up to +19; RCWT budget displacement), Anthropic context engineering (compaction silently drops critical items; structured note-taking = named mitigation) → rule revised ask-first → **capture-before-reason** (non-denylisted user msg → FIRST tool call = log append, no pre-write reasoning; verbatim no-omissions format; pointer pattern reference-by-title; Context ≤20 words; denylist + pending-question nuance; explicit user opt-out; when-in-doubt=document) → AGENTS.md non-negotiable 8 + D63 (v3) + log-header mirror + format notes; this session's prompts NOT logged per user “DO NOT DOCUMENT” banners.
- **2026-08-22 — process rule: user-input documentation, ask-first v2 (docs only, no code):** user rule (set 14:05, refined 14:18/14:53) — when SUBSTANTIVE human-user input/feedback/responses arrive (directives; decisions/approvals incl. corrections; answers; bug reports + corrections; playtest feedback; phase recommendations), agent IMMEDIATELY asks whether to document before ANY other action; yes → `docs/USER-INPUT-LOG.md` (verbatim, unmodified, timestamped + classified) then act; no → proceed; NEVER ask/document: trivial messages ("continue") + commonly-repeated template prompts (kick-off / closing-handoff prompts, "Stage, commit, push.", "proceed"-family) + agent-side content; unsure → ask → AGENTS.md non-negotiable 8 + D63 (v2) + `docs/USER-INPUT-LOG.md` created (entry #1 = 14:05 rule, meta-line trimmed per feedback; entry #2 = 14:18 scope clarification; 14:53 trigger-v2 input NOT logged per user "No").
- **2026-08-22 — 11.6.1 approvals + Phase 15 defect logged (docs only, no code):** user answered the blocking O's — (1) roster resolved D62: **Mage = original/default sprite (starter)**; Warden = NEW larger heavy-armor sprite (>original, <brute-enemy size) + SLOWEST of the 4; Ranger/Swashbuckler new sprites; ghost = sheet-ghost with per-player UNIQUE Pacman color → in-run UI colors; (2) unlock model D58 RESOLVED = Soulshard shop **Mage default · Ranger 1500 · Warden 3500 · Swashbuckler 7500**; ONLY remaining O = per-char stat values (propose in 11.6.1). NEW DEFECT (user playtest): **Pyre Lance (flamethrower) NEVER in level-up offers in ≥20 recent runs (~24 h)**; pistols frequent, bombs occasional, base weapons fine; (snowball/ring never-seen = Phase 12, unimplemented — expected, NOT the bug) → **Phase 15** (15.1 reproduce/quantify → 15.2 root cause — mechanisms logged UNVERIFIED: 3-of-N pool sample, cap-5 slot-fill blocks late new-weapon offers, exclusion interplay → 15.3 fix → 15.4 regression) queued after Phase 12 (fix once on the final 10-weapon roster). PLAN §3.8 (mapping/unlock/ghost resolved) + D58 rev + D62 + 11.6 checklist + README n/a updated.
- **2026-08-22 — 11.6 RESPEC (docs only, no code):** user answers — NO duplicate characters/weapons (1a–1e), ghost fallback + 2-unique starting-weapon offers (4b), per-char base stats visible in select (1e/4 archetypes), select UI 2a/2b + VERY-brief sources-cited research (2c/2d), solo select confirmed, character unlock meta progression distinct from map unlocks (4a, model = O), character + weapon/synergy roster modularity (1f/5) → D28/D29/D34 revised + D56–D61 new + PLAN §3.8 characters bullet rewritten + §3.4 roster modularity + 11.6 rewritten (sub-steps 11.6.0 research → 11.6.4 sync/E2E) + new 11.6b (synergy exclusivity + starting-weapon pre-ownership) + 11.11 re-read (D57: invariance = no co-op overhead) + README roadmap 11 line. OPEN (block 11.6.1): archetype→weapon mapping + names + stat values, unlock model.
- **2026-08-22 — 11.5 boot-gate flake fix (test-only, no game code):** root-caused boot failure `remote first-pick owns wand` — `reset()` grants `startWeapons:['wand']` with NO `weaponOwner` registration, the 11.5 E2E asserted ownership on ALL gained keys (incl. upgrades), and pump rng-picks leaked into the forced-pick section (cap + garlic-race variants); fix: deterministic section baseline (all players → `{wand:1}`, `weaponOwner={}`) + ownership assert on true first-picks only · gates re-verified 33/33 · 384/384 · boot PASS runs=4.
- **2026-08-22 — session resume (post-compact, docs only):** state verified vs docs (11.1–11.5 done; gates 33/33 · 384/384 · boot runs=4); plan set **11.6 characters** = 11.6.1 roster config+art (`CFG.characters` + 4 builders in `art/characters.js`) → 11.6.2 selection UI + run-start default weapon → 11.6.3 co-op profile def + E2E + solo guard; 3 open decisions (duplicate chars / start-weapon-vs-exclusivity / 11.11 invariance read) proposed w/ defaults, awaiting user.
- **2026-08-22 — 11.5.4 co-op exclusivity E2E + 11.5 close-out (code + gates, step COMPLETE):** test-boot 11.5 E2E in the live 3P run (host real-pick garlic → ownership/no self-exclusion/remote exclusion; passive pick isolation; remote auto-pick ownership + never-garlic; cap ≤3) + PASS-line record · gates 33/33 · 384/384 · boot PASS runs=4 · **11.5 TICKED** → next **11.6 characters** (RESPEC'd 2026-08-22; 11.6.1 answers IN: D58 unlock shop RESOLVED Mage/Ranger 1500/Warden 3500/Swashbuckler 7500 + D62 roster: Mage=original sprite (starter) · Warden=new large armor, slowest · Ranger/Swashbuckler new sprites · ghost=sheet + per-player Pacman color → UI; ONLY O left = per-char stat values) — see 11.6 checklist + D28/D29/D34 rev + D56–D62: archetypes + per-char stats visible in select + co-op unique-character + greyed-out + Soulshard shop unlock + ghost fallback + 2-unique-offers + roster modularity; 11.6.0 = VERY-brief sources-cited select-UI research, default 2b full-screen; sub-steps 11.6.0→11.6.4 in checklist) + **11.6b synergy/starting-weapon exclusivity closure (D29 respec) BEFORE 11.7.** NEW DEFECT tracked as **Phase 15** (user playtest: Pyre Lance never offered in ≥20 runs — root cause pending, 15.1 reproduce first; queued after Phase 12).
- **2026-08-22 — 11.5.2/11.5.3 per-player cardOffers + host weapon ownership (code + gates):** `cardOffers(..., cap, exclude)` (exclusion = other-owned keys never offered new/upgrades; passives never locked; per-picker synergy gating) + `game.js` `weaponOwner` first-picker map (reset per run, registered at both pick sites, persists through leave) + `_ownerExclusion(pl)` + all 3 offer sites pass `weaponCap(players.length)` · test-logic 11.5.2 block (6) · gates 33/33 · 384/384 · boot PASS runs=4 (solo level-ups invariance). NEXT: 11.5.4 co-op multi-client E2E (exclusion + per-picker picks live) + 11.5 tick.
- **2026-08-22 — 11.5.1 base maxWeapons 4→5 + equip-cap helper (code + gates):** `CFG.run.maxWeapons` 5 + pure `weaponCap(n)` in `js/net/coop.js` (D42 cap table) · test-logic card-blocks re-based to cap 5 (trio pool {inferno,tempest,phoenix}) + 11.5.1 block (3) · gates 33/33 · 378/378 · boot PASS runs=4.
- **2026-08-22 — 11.4 player leash (code + gates):** `CFG.coop.leashR` 700 (A2 shared expanded vision radius) + pure `leashClamp` POCS in `js/net/coop.js` (radius-R disk intersection; 8-iter cap) · host `_coopLeash()` per `_playingUpdate` (live only, world re-clamp, solo no-op) · `_lights()` co-op vision = leashR (solo 510 intact) · test-logic 11.4 block (8 checks) + test-boot 3P E2E (1.5R teleport → pairwise ≤ leashR convergence poll) · gates 33/33 · 375/375 · boot PASS runs=4 (×2). Also fixed: 11.2 checklist tick was missing (recorded done in Status/log all along).
- **2026-08-22 — 11.3 difficulty scaling (code + gates):** `enemies.coopS = coopScale(players)` in host sim (set in `startRun` + `_netRoster` mid-run join/leave) · spawner curves take `s` (aliveCap/batch ×s, interval ÷s; solo default `s=1` bit-identical) · `Enemies.spawn` HP/dmg ×coopS incl. boss (Q7) · test-logic 11.3 block (coopScale values/clamp, curve scaling, spawn + boss HP/dmg) → 367/367 · test-boot 3P E2E: `coopS=1.66` armed at `startRun`, every non-boss maxHp = base×S · E2E robustness at the new density: `keepAlive()` (revive seats + `pickCard(0)` — idle host player hits LEVELUP) in every pump loop + position-tracking as convergence poll (knockback transients) · gates 33/33 · 367/367 · boot PASS runs=4.
- **2026-08-22 — 11.2 host-authoritative sync (code + gates):** `js/net/sync.js` (SNAP_V=1: key tables in CFG order, playerSnap 26 slots r1, enemySnap + E_FLAG_*, pickupSnaps stable pool slots, stateMsg time=r2, unpackState) + `conn.js` CoopConn · combat/enemies/player owner kill-credit + players-array refactor · game.js co-op mode (host: remotes from D53 profiles, per-step stateMsg broadcast, mid-run-join reconcile, auto card-pick remote level-ups; client: no sim, sendInput every step, arrival-time interp, dead-player spectate) · serve.mjs roster per-seat profiles + state/runstart relays · UI #btn-coop/#coop-status/#btn-coop-leave · gates 33/33 · 360/360 (+22) · boot PASS runs=4 + 11.2 E2E (real relay ephemeral port 0; host+2 clients; shared seed; client tracks host; KeyD drives host remote; leave → reconcile → run continues). E2E gotcha fixed in-test: pump steps BEFORE the first-state wait (host broadcasts only inside `update()`); sync pumps must `await tick()` to let WS round-trips land.
- **2026-08-22 — 11.1 co-op transport (code + gates):** zero-dep RFC 6455 WebSocket upgrade + frame codec in `tools/serve.mjs` (import-safe entry guard — `resolve(argv[1])` vs `fileURLToPath`; SHA-1 accept; masked/partial/oversized frame handling, 64 KB cap) + `attachCoopRoom` (the one room: first hello opens at host seat 0 with host levelKey, join/leave/full/host-leave-close, input/state/runstart relayed to the OTHER seated clients, roster broadcast) + pure protocol/room/D53 `profileFromMeta`/`coopScale` in `js/net/coop.js` + `CFG.coop {maxPlayers:4, perPlayer:0.33}` · test-logic 11.1 block (room semantics, D53 profile, codec round-trip/partial/oversized, RFC accept vector, scaling clamp) + test-boot E2E: real serve.mjs on ephemeral loopback port 0 + Node-native WS clients (host seat 0 / seats in join order / 5th→full / leave+roster / host-leave→closed / room re-opens) · gates 31/31 · 338/338 · boot PASS runs=4.
- **2026-08-22 — 11.13 web-play research + transport decision (docs only):** binding protocol complete (date-verified FIRST per D36; 8 sources cited; 100%-free; hype-avoidant) → `docs/RESEARCH_FINDINGS.md` (current host-authoritative/relay model validated; CF-DO free-tier math + input-coalescing fix; Tailscale/Oracle facts; WebRTC rejected); follow-up: DS124 NAS hosting verified feasible (ARM/no-Docker caveat, Tailscale-on-Synology official) → **D64: 11.13 transport = Tailscale on the user's DS124 NAS** (installed + connected 2026-08-22, user-confirmed) running unmodified `serve.mjs` as boot-start task; players join the tailnet; next code task = **11.6.4 co-op sync/E2E** (11.13 impl after 11.12).
- **2026-08-22 — 13.13 README + Phase 13 close-out (docs-only):** README **Levels** section (3-map table: Evernight Wood 4200×3200 / Higan 4200×3200 / The Drowned City 5145×3920; chained 3× victory unlocks; rosters + boss names verified against `characters.js` slot re-skins; per-level identity line) + Roadmap Phase 13 line → DONE 2026-08-22. **Phase 13 COMPLETE (13.1–13.13)** — next session: Phase 11 multiplayer (11.1–11.13 spec; 11.13 web-play research = binding protocol step).
- **2026-08-22 — 13.12 Phase-13 gates (verification, no code changes):** all three green — 30/30 · 316/316 · boot PASS runs=4 (M02/M03 real runs, unlock flow, boss, victory, Settings mute/zoom E2E, per-level scores, flavor); mobile parity CSS-verified (`.lv-card`/`.set-row` min-height 72 px, touch zoom default 0.80, safe-area/pause-on-blur invariants untouched).
- **2026-08-22 — 13.11 per-level audio (code + gates):** `music.js` `FLAVOR` data (higan: temple bell D2 + 2.76× inharmonic partial 1/loop, chime pings D5/F5/B4 3/loop, taiko base 6/loop → boss 8/loop s8 0/4 @ 55 Hz ×1.6 gain; drowned: 3 rising bubble chirps 0.6→1.0×, 1 whale glide 160→240→150 Hz lowpass-600 3.5 s/loop) + 5 voices on the 10.6 lattice, m01 = no entry → zero flavor oscs; muffle = level→lowpass→musicBus (`muffleOff` 12k no-op / `muffleM03` 600 Hz, retuned per `startMusic`); `CFG.audio` gains bell .05/chime .04/taiko .09/bubble .03/whale .06; test-logic: FLAVOR shape block (5 checks) + 4 fake-ctx pumps (m01 invariance 49/60/96/294, higan 24 bell oscs/36 chimes/72 taiko, higan-boss 96 taiko, drowned 36 bubbles/12 whales; `t0` cutoff excludes margin-scheduled loop-13 bar-0 hits) — 316/316; gates 30/30 · 316/316 · boot PASS runs=4.
- **2026-08-22 — 13.10 per-level flavor (code + gates):** `items.js` GEM_PAL/HEART_PAL + `gemHeartFor(levelKey)` (m01 palette = original; buildItems drops gem/heart); `game._reskinPickups()` on construct/startRun/previewLevel; `_gameOver` detects the newly-unlocked level (before/after `isUnlocked` around `recordWin`) → 'NEW MAP UNLOCKED' banner after DAWN BREAKS; `screens.js` banner queue (drains after `CFG.ui.bannerMs` 2600 in per-frame `update`; concurrent emits queue instead of clobbering) + game-over `Unlocks` row (next locked level, X/3 wins) · test-boot E2E: synthetic `_gameOver(true)` at wins.m01=2 → banner exactly once + 6th stat row 'The Drowned City — 0/3 wins' (stub DOM: assert via `children`, textContent does NOT aggregate), no dup on 2nd victory, wins/scores state restored for the run flow; pickup reskin asserts on m02/m03 runs + boot-time `gemHeartFor` shape check · gates 30/30 · 303/303 · boot PASS runs=4.
- **2026-08-22 — 13.9 per-level high scores (code + gates):** pure `scoreKeyFor(levelKey)` in `ui/screens.js` — m01 keeps original `qsurv.hiscores.v1` (no data loss), siblings `qsurv.hiscores.<level>.v1`; gameover ranks/saves within the run's level (`game.levelKey`) list; scores screen + clear follow `game.selectedLevelKey` · test-logic 13.9 block (key derivation / m01 separation / in-list ranking, LS stub) + test-boot E2E (m02/m03 victory → own list with run score; m01 list untouched after earlier clear) · gates 30/30 · 303/303 · boot PASS runs=4.
- **2026-08-22 — session resume (post-compact; docs verified, gates re-run green, no code changes):** baseline confirmed 30/30 · 296/296 · PASS boot-sim runs=4 (matches Status); plan: **13.9 per-level high scores** → 13.10 flavor → 13.11 audio within budget → 13.12/13.13 close-out (Phase 13 complete).
- **2026-08-22 — 13.8 mobile view zoom + Pause-menu Settings (code + gates):** camera-view factor — `CFG.zoom` (`touch` 0.80 / `full` 1.0 / LS key `qsurv.zoom.v1`); `meta.js` pure `defaultZoom`/`loadZoom`/`saveZoom` (invalid/absent → device default); `game.js` loads zoom in constructor (touch via `document.body.classList.contains('touch')`, D13) + `resize`→`_applyZoom` (camera view + half-res lighting = canvas/zoom; vignette stays 1× canvas) + public `setZoom` (persists); `render` wraps the frame in a view-space translate so the enlarged view is centered on the canvas (no-op at zoom 1; vignette + minimap outside it = 1×); `index.html` drops `#btn-mute` → new Pause-menu **Settings** rows `#set-zoom`/`#set-mute` (≥72 px) · `hud.js` owns both rows (zoom → `game.setZoom`; mute → `input.queueMute`, M key + `qsurv.mute` LS unchanged, D8; `syncZoom`/`syncMute` on 'mute' bus) · `css` `.set-row` styles + dead `#btn-mute` rules removed · README Play controls synced · test-logic 13.8 block (defaults 0.80/1.0, touch/desktop fallback, LS round-trip both directions, invalid→default) + test-boot E2E (desktop default 1.0 → view=canvas; toggle → 0.80 → view 1600×1000 + persist; toggle back → 1.0; Settings mute row → `qsurv.mute` on/off + `.muted` class) · gates 30/30 · 296/296 · boot PASS runs=4.
- **2026-08-22 — 13.7 level select (code + gates):** `ui/screens.js` `renderLevels` — 3 radio cards on the main menu (palette chip + name + locked-req text + X/3 pips from `game.wins`/`isUnlocked`); locked tap → `denied` blip + `lv-shake` (selection unchanged); unlocked select → `game.selectedLevelKey` + `saveSelectedLevel` (LS `qsurv.level.v1`) + `game.previewLevel` (backdrop regenerates) · `meta.js` pure helpers `loadSelectedLevel`/`saveSelectedLevel` · `game.js` constructor clamps selection to unlocked + seeds `level`/`levelKey` (so backdrop + no-arg `startRun` honor the persisted selection) · `sfx.js` `denied` recipe (descending double blip) · `.screen` gains safe-area padding (mobile parity) · test-logic 13.7 block (defaults/round-trip/invalid-key) + test-boot E2E (3 cards, m01 sel + m02/m03 locked at 0 wins, locked m03 denied blip+shake, simulate 3× m01 wins → m02 unlocked → select m02 → preview+persist, clean reset so run-1..4 flow unaffected) · gates 30/30 · 290/290 · boot PASS runs=4.
- **2026-08-21 — 13.6 unlock persistence (code + gates):** `meta.js` pure helpers `defaultWins`/`loadWins`/`saveWins`/`recordWin`/`isUnlocked` (LS key `qsurv.wins.v1` = CFG.meta.winsKey; meta.js LS pattern) · `game.js` loads wins in constructor + records/saves on victory-only in `_gameOver` (deaths don't count) · test-logic 13.6 block (defaults / increment / 3×-each + chained unlock rules / LS round-trip via stubbed localStorage) · gates 30/30 · 286/286 · boot PASS runs=4.
- **2026-08-21 — 13.5 Map 03 roster (code + gates):** `buildCharacters('m03')` Drowned City slot skins — crab (small chaser), goldfish (bat), mer-people (goblin; merman + mermaid), stingray (wolf), electric eel (cultist; jagged zap sprite, NOT orb), orca (brute) + Great White Shark boss (same charge skeleton; belly-flash frame-1 windup pose) · `shark` in CFG.enemies (wraith base stats, boss) · m03 `weights` (m01 role curve) + boss `shark`@240 `name:'THE GREAT WHITE'` in levels.js (was null) · ×1.56 stats via existing `enemies.diff` (no new code) · test-logic 13.5 block (base/×1.56 stats, banner name, weights, pickType early/late) + test-boot M03 run 4 (skin asserts, spawned ×1.56, 'THE GREAT WHITE AWAKENS', victory; keep-alive via run 3/4 steer branch) · gates 30/30 · 279/279 · boot PASS runs=4.
- **2026-08-21 — 13.4 Map 03 "The Drowned City" art (code + gates):** layoutM03 5145×3920 (kelp/coral/anemone/broken-columns/dome+spire/trident/shipwreck/vent sprites; m03 sand tiles own rng streams `mulberry32(5250+i*131)`) · m03 sky = surface-glow "moon" + 7 god-ray shafts (new optional `sky.godrays`, screen-space pass under lighting) + surface-shimmer near-ridge, empty far-ridge · rising-bubble foreground (D49) via Snow kind `'bubble'` + `CFG.perf.bubbleColor` · 3 open-water fish schools (koi t-driven orbits, 1×1 empty-img seam in world.js) · lights = 8 anemones + city dome · palette skyTop `#04283e`/skyHorizon `#0a4a66` · golden 319 trees/370 decor/370 colliders/1009 decals seed 20260823 · test-boot m03 backdrop block (decor img + size assertion) · m01/m02 goldens hold · gates 30/30 · 269/269 · boot PASS (runs=3).
- **2026-08-21 — 13.3 Map 02 roster (code + gates):** `buildCharacters('m02')` Higan skins tanuki/hō-ōi/shikome/kitsune/oni/miko (same footprints) + segmented-tail Ryū in default set · `enemies.diff` ×1.25 hp/dmg at spawn (A4) · m02 `weights` (m01 role curve) + boss `ryu`@240 `name:'RYŪ'` + name-driven AWAKENS/FALLS banners (m01 text byte-identical) · test-logic 13.3 block (stat scaling / banner names / weights) · test-boot real M02 run 3 (keep-alive + banner capture: skins, ×1.25 stats, Ryū spawn + 'RYŪ AWAKENS', victory) · gates 30/30 · 255/255 · boot PASS (runs=3).
- **2026-08-21 — 13.2 Map 02 "Higan" art (code + gates):** layoutM02 in levels.js (torii-ring landmark + shrine village + pagoda + koi pond + bamboo + lanterns + stone paths + m02 lights) · m02 art pack in terrain.js (cherry/cherryBig/bamboo/torii/shrine/pagoda/lantern/stone-slab/koi+koiPond + m02 tiles/decals on own rng streams) · m02 sky in sky.js (Fuji silhouette + stratus + full moon) · per-level sky cache + palette-driven sky/ground/lighting base (world.js/lighting.js/game.js) · kind-aware foreground petals (particles.js + CFG.perf.petalColor) · tests: test-logic m02 block (golden 274/324/331 seed 20260822) + test-boot M02 menu-backdrop E2E + m01 snow regression · gates 30/30 · 243/243 · boot PASS (m01 golden 276/335/328 intact).

- **2026-08-21 — 13.1 level framework (code + gates):** new `js/world/levels.js` (pure defs: size/diff/menuSeed/foreground/audio/unlock/palette + m01 layout moved verbatim) · `generateWorld(seed, levelKey)` dispatch · per-level spawner weights/boss/diff scaling (D55) · per-level bounds seam in `World.generate` · per-level minimap base + menu-backdrop level hook (last-level preview) · gates 30/30 · 231/231 · boot PASS.
- **2026-08-21 — Phase 14 O1 resolved + placement approved (docs only, no code):** counter source = **A** — live projected award `floor(score/400)` (+25 on victory), no economy change; queue slot approved (after Phase 12, before 10.9 LAST). D54 amended + 14.1 + README + resume notes synced.
- **2026-08-21 — Phase 14 spec'd (docs only, no code):** in-run real-time Soulshards HUD counter (run-only display, single unified view) + unified co-op earnings (all active players in a run receive the same amount; each local LS accrues it in full) → checklist 14.1–14.3 + D54 (revises D53 earnings clause) + PLAN §3.4/§3.8 + README; O1 open: counter source = live projected award vs collectible soul gems.
- **2026-08-21 — 10.7 softlock + 10.8 gem SFX (code + gates):** 10.7 offers computed first at both LEVELUP entry sites — empty pool grants levels silently, stays PLAYING (boot-sim E2E rides the bench all-max roster: multi-queue entry + mid-queue exhaustion, auto-pick gated per `synActive` pattern); 10.8 `gem` sfx recipe (bright blip, GAPS 0.05) + `gem` bus emit at the XP-consume site (boot-sim forced collect fires the event); gates 29/29 · 219/219 · boot PASS.
- **2026-08-21 — session resume (post-compact, docs verified, no code, no other changes):** Phase 13 doc state confirmed in sync (PLAN §3.9 + §3.8/§3.7/§3.1/§5, checklist 13.1–13.13 unchecked, D44–53, README); gates at last-run baselines 29/29 · 219/219 · boot PASS; awaiting user start for 10.7.
- **2026-08-21 — Phase 11 clarification (docs only, no code):** meta progression (Soulshards + Upgrades) is **player-specific** — never carried over to/shared with/applied to other players during co-op; each client applies its own local meta to its own character; shard earnings accrue locally only; host sim gets each player's meta-derived stat profile at join (handshake, sim input only) → D53 + PLAN §3.8 co-op rules + 11.2.
- **2026-08-21 — Phase 13 decisions A1–A8 (docs only, no code):** Map 02 skins approved + M03 small chaser = **crab** (A1/A2); M03 scale → **1.5× area ≈ 5145×3920** (A3); per-level difficulty chained **×1.25** (A4); A5 flavor approved (per-level high scores/audio/gem tints/backdrop preview/dragon tail/koi + fish schools/unlock banner/denied blip) — first-clear bonus, shard multipliers, victory lines, pufferfish, keyboard select, boss intro **deferred**; zoom default **0.80** + new **Pause-menu Settings** (zoom + mute; mute **replaces** HUD `#btn-mute`) (A6); order **10.7/10.8 → 13 → 11 → 12 → 10.9 (last) → 2.9** (A7); map names tentatively approved (A8). Decisions 44–52 revised/added; PLAN §3.8/§3.9 + README synced.
- **2026-08-21 — Phase 13 spec'd (docs only, no code):** 3-map expansion — Map 02 "Higan" (traditional Japan, 4200×3200: sakura petals map-only, Oni brute, Ryū dragon boss) + Map 03 "The Drowned City" (underwater, 8400×6400: goldfish/merfolk/stingray/eel/orca + Great White Shark boss, small rising bubbles foreground) — cumulative-win unlocks (3×, victory-only), main-menu level select with locked-visible-progress, mobile view zoom ≈25% → PLAN §3.9 + checklist 13.1–13.12 + decisions 44–49; candidate embellishments pending user decision.
- **2026-08-21 — 10.6 music remake:** 66 BPM eldritch track in D-dim7 {D, F, Ab, B} (sub-root crawl + detuned-saw drone layer + sparse heartbeat + one lone beat-pair color tone per bar over a dark delay + run-gated filtered-noise texture; wind/howl ambience unchanged, all tuning in `CFG.audio`); seamless-seam proof in test-logic: fake-`AudioContext` clock pumped 12 loops + margin — exact BAR lattice (no gap/double/drift) + exact per-voice counts (213→219 checks; old MUSIC data checks replaced).
- **2026-08-21 — 10.5 spawn distance:** `spawnPoint` → thin band just outside the view edge (4 sides, `spawnPad` 15 px / 8 retries / clamped fallback `spawnFallback` 30 px — all config); root-caused the boot-sim go-menu flake: input edge (dash/card) set on the last PLAYING frame survived `_gameOver`/`toMenu` (both call `clearTransient`, which only cleared keys+sticks) → `_menuUpdate` auto-started a run; fix: `clearTransient` now clears all four edge channels (also correct on blur/tab-switch); harness: run-1 keep-alive iframes (mirrors run-2) + run-2 `pumpUntil` victory (clock freezes in LEVELUP — fixed pumps break); boot-sim 17/17 consecutive PASS.
- **2026-08-21 — Phase 11/12 spec refinement 4 (docs only, no code):** base `maxWeapons` **4→5** (solo runs now get 5 standard weapon slots) → co-op cap table **1P=5, 2P=4, 3P=3, 4P=2**, so a 4P player can still own 1 weapon pair → paired synergy achievable (synergies never count toward the cap); decision 42 revised; 11.5 updated incl. test-logic expectations 4→5.
- **2026-08-21 — Phase 11/12 spec refinement 3 (docs only, no code):** synergies → **5 levels** like standard weapons (supersedes D19 single-level; stay in pool until own max; per-level effect scaling) + pair-specific gating confirmed (offer pool opens the moment the synergy's own 2 sources max — NOT all weapons) + co-op equip cap **4−(N−1)** per player with synergies excluded (base `maxWeapons=4` verified in code) + slow/shock stacks **5 s per-stack TTL** (playtest-tunable); decisions 40–43; PLAN §3.4/§3.8 + README updated.
- **2026-08-21 — Phase 12 spec'd (docs only, no code):** Pyre Lance tune (faster max-velocity ramp, emission origin feet→chest/abdomen, stream +25%) + 3 new weapons (Bow & Arrow, Snowball Launcher — slow stacks→freeze, Ring of Chain Lightning — shock stacks→stun+chain burst) + 4 new synergies (Flaming Arrows, Heart-Piercer, Blue Flame, Storm Volley w/ proposed chain/shock extras) → Phase 12 checklist 12.1–12.8; roster 7→10 weapons, 5→9 synergies; decisions 37–39; README weapon list + roadmap updated.
- **2026-08-21 — Phase 11 spec refinement 2 (docs only, no code):** Q7 resolved (N players = N Wraiths; Wraith stats get the same +33%/player ramp) + 11.13 research protocol made binding (verify real-world date BEFORE web research — standalone, non-batchable, non-negotiable; sources-cited, evidence-based, data/outcomes-driven best practices for self-hosted, fully game-state-synced web-game multiplayer servers; 100% cost-free only); decisions 35 updated + 36 added.
- **2026-08-21 — Phase 11 spec refinement (docs only, no code):** Q1–Q6 answered (A1: 33%/player on HP/damage/spawn-count; A2: leash = shared expanded vision radius; A3: picks player-scoped, passives NOT locked; A4: LAN + web play — Pages can't host WS → 11.13 research; A5: join-order corners; A6: Wand/Garlic/Aegis Blades/Pyre Lance starters) + new boss-clone request → **Q7** (one-per-player vs 1+N); checklist renumbered (11.10 boss, 11.11 solo-invariance, 11.12 gates, 11.13 web play); decisions 27 updated + 30 revised + 31–35 added.
- **2026-08-21 — Phase 11 multiplayer scope doc'd (docs only, no code):** 6 user requests (real-time multiplayer, difficulty scaling per added player, player leash, per-run weapon exclusivity, 4 characters, co-op HUD) → Phase 11 checklist 11.1–11.11 + decisions 27–30; PLAN.md §1/§2/§3.1/§3.8/§5 updated (non-goals now permit the LAN WebSocket room); open questions Q1–Q6 (ramp magnitude, leash radius, passive lock, LAN scope, corner assignment, starter weapons) await user answers; README roadmap line.
- **2026-08-21 — Doc review + state verification (docs only, no code changes):** all three gates re-run green (29/29 · 207/207 · PASS boot-sim); git state documented in Status (Phase 9 + 10.1–10.4 uncommitted since `667364f`); pending items re-verified against code; `isolate-*-v8.log` (2.6 MB V8 crash dump from a crashed node process) catalogued as a safe-to-delete artifact.
- **2026-08-21 — 10.4 multi-hit perf:** permanent worst-case bench in test-boot (`[10.4-bench]`; `DEBUG_BOOT=1` → `[10.4-sec]` section timers, ctx draw-op counters) → root causes: no viewport culling + death-frame list/grid churn (NOT the per-hit floater/`_dot`/flame-scan suspects); fixes: cull rect (`world.cullPad`) through pickups/shadows/Y-sort/particles + `_ai` dead-skip + in-place compaction. Before/after: `r:drawOne` 75.9→53.8 calls/render frame (−29%), `u:enemies` 0.069→0.057 ms/frame (−17%).
- **2026-08-21 — 10.3 mute speaker icon:** inline SVG (speaker + 3 waves; slash + `--danger` off-state, `aria-label` Mute/Unmute); also fixed a latent seed-dependent touch-stick flake (no-op `collidersNear` for the 1 s steer window — the dash test pushes the player into a seed-placed hut).
- **2026-08-21 — 10.2 exact-effect card descs:** pure `cardEffectText(kind,key,level)` (player.js); mandatory `p.card-effect` line first in every card + `p.meta-effect` on Upgrades rows; phoenix desc fixed ('…heal 10 HP every 8 kills.'); +90 logic checks (117→207).
- **2026-08-21 — 10.1 dash indicator (all envs):** `#btn-dash-hud` in `#hud-buttons` (desktop had NO indicator); same change-detected `--cd` as `#btn-dash`; clickable (`queueDash`); boot-sim asserts both `--cd` match mid-dash.
- **2026-08-21 — Phase 9 COMPLETE:** boot-sim extended (meta/Upgrades buy → maxHp 120, all 7 weapons, burn DoT, dash i-frames start+mid, synergy blight draw E2E); README updated; gates 29/29 · 117/117 · PASS.
- **2026-08-20 — Phase 10 scope doc'd (9 user requests, docs only) + Phase 9 re-verified against code (docs only; 2 resume-note errors fixed: synergy test pool, auto-pick vs synergy E2E).**
- **2026-08-20 — Phase 9 implemented:** config scalars + 3 weapon tables + `CFG.synergies`/`CFG.meta`; new `core/meta.js`; player/combat/enemies (synergy namespace, 3 weapons, DoT pipeline, 4-arg `cardOffers`).
- **2026-08-20 — Published:** public repo + Pages live (user asked private; a private repo's Pages is login-walled → public won the "share it" goal; flip-back in Status).
- **2026-08-20 — Playtest fix: `ellipse()` 6-arg boot crash** — 21 call sites gained the missing `rotation` arg; ctx stub hardened (Decision 17), negative-tested.
- **2026-08-20 — Phase 7:** boot-sim coverage (key card pick, all weapons + wand-off window, heart heal, scores/quit, touch stick + dash) — one-shots self-verify (Decision 16).
- **2026-08-20 — Playtest fix: "Start Game does nothing"** — terrain.js boot crash (1 ReferenceError + 6 TDZ self-shadow renames); new gate `test-boot.mjs` (Decision 15); spawner batch floor 1→2 (Decision 18).
- **2026-08-20 — Phase 6 COMPLETE:** `main.js` bootstrap + `tools/serve.mjs`; in-process HTTP driver caught 2 real serve bugs (traversal check, path join); serve listens unconditionally (Decision 12).
- **2026-08-20 — Phase 5 COMPLETE:** `sfx.js` (lazy ctx on gesture, bus graph, 9 recipes) + `music.js` (92 BPM D-minor loop + wind + howls — track to be replaced by 10.6); gesture hook in `startRun` (Decision 11).
- **2026-08-20 — Phase 4 COMPLETE:** `hud.js` + `screens.js` (state-poll screens, Decision 7; mute ownership Decision 8); CSS touch targets ≥72 px.
- **2026-08-20 — Phase 3 COMPLETE:** entities (player/enemies/spawner/combat/pickups/particles) + camera + lighting + game state machine (23 modules, 83 checks).
- **2026-08-20 — Phase 2 COMPLETE:** art/world/minimap modules (13 modules import-clean).
- **2026-08-20 — Full codebase review (docs only):** contracts verified; README steer/dash wording fixed; PLAN dash speed 680→690 (config is the tuning truth).

## Environment & Known Caveats

- **Shell:** Git Bash (sh) on Windows, node v24.11.0. Terminal tool: no shell substitutions (`$VAR`, backticks).
- **No Node `fetch`** in throwaway drivers (undici keep-alive → libuv teardown assert on Windows) → use `node:http` + `keepAlive:false`.
- **No argv[1]-vs-`import.meta.url` gates** in `.mjs` entrypoints (MSYS path normalization).
- **Detached server recipe:** `powershell.exe -NoProfile -Command "Start-Process -FilePath node -ArgumentList 'tools/serve.mjs' -WorkingDirectory 'D:\Apps\Qwen-Survivors' -RedirectStandardOutput 'D:\Apps\Qwen-Survivors\server.log' -RedirectStandardError 'D:\Apps\Qwen-Survivors\server-err.log' -WindowStyle Hidden"` · Stop: `netstat -ano | findstr 47893` → kill the node PID.
- **LAN IP is dynamic** (last observed 192.168.1.101) — serve.mjs prints current URLs; other local IPs are virtual adapters (WSL/ADB/VPN).
- **Runtime artifacts (safe to delete):** `server.log`, `server-err.log`, `prof-out.txt`, `isolate-*-v8.log` (V8 isolate crash dump — node process died, e.g. killed server/test; header names the node.exe build).
- **NOT verified in-sandbox:** the entire in-browser experience (render path, audio output, touch feel) — that is task 2.9. The test-boot canvas stub is browser-strict only for `arc`/`ellipse`/gradients; every other canvas method is a silent no-op.

## How to Resume a Session

1. Read `AGENTS.md`, then this file: Status → Resume Notes (active work). Architecture: `docs/PLAN.md` §3–§4. The code is the API record — read the module before changing it.
2. **Phase 13 COMPLETE (13.1–13.13)** → **Active: Phase 11 multiplayer/co-op** (spec complete 11.1–11.13; room handshake carries the level key; deferred items in D51 — do not build; 11.13 research DONE 2026-08-22 — findings `docs/RESEARCH_FINDINGS.md`; **transport decided D64: Tailscale on DS124 NAS** (unmodified `serve.mjs`; players join tailnet); impl after 11.12) → **Phase 16 playtest defects** (16.1 m01 snow DONE · 16.2 projectile origin DONE 2026-08-22 · next 16.3 Pyre Lance flame +33%/≥2×/LEAD — supersedes 12.1) → 11.9–11.12 → 11.13 transport impl → **Phase 12 weapons** (12.1 REVISED = post-16.3 verification) → **Phase 20 passive-start rule** (no starting passives — D66) → **Phase 15 offer-coverage defect fix** (15.1 reproduce/quantify FIRST on the post-20 economy) → **Phase 17 run durations & boss schedule** (O's need user confirm; PLAN §3.10) → **Phase 18 level-up actions SKIP/BANISH/RE-ROLL** (O's per RESEARCH_FINDINGS §7.3; PLAN §3.11) → **Phase 19 HUD equipment row + fuel bar** (PLAN §3.12) → **Phase 14 in-run shard counter** (14.1–14.3) → **Phase 21 extended roster** (9 weapons + 2 items + 5 characters, VERBATIM PLAN §3.13; O's per sub-step) → **10.9 optimization (LAST)** → **2.9** user browser sign-off (re-verify post-co-op) → tick 2.9 → project DONE. New scope → add to the Master Checklist first.
3. Validate with **all three gates** before any tick: `node tools/check.mjs` (baseline 30 modules) · `node tools/test-logic.mjs` (baseline 316 checks) · `node tools/test-boot.mjs` (baseline `PASS boot-sim`; flow: menu → run 1 death → meta/Upgrades buy → run 2 via `btn-start` (maxHp 120) → all 7 weapons + burn DoT + dash i-frames + synergy blight E2E → boss 4:00 → victory 5:00 → menu → M02 backdrop + m02 real run 3 (Ryū); `DEBUG_BOOT=1` → `[10.4-sec]` timers).
4. On every completion: tick + date + one-liner in the Master Checklist, update Status + this Session Log line — *before* declaring done.
